# 魔兽世界 3.3.5a 客户端＋服务端项目交接提示词

> 用途：将下面“可直接发送给上一个代理的提示词”完整复制到旧 Agent Mode 会话。  
> 当前工作仓库：`https://github.com/wpch3/wow-mecode`，目标分支：`main`。  
> 参考/上游整合源码：`https://github.com/328950225/TrinityCore-NPCBOT-Eluna-zhCN`，目标分支：`NPCBOT-Eluna-zhCN-2026`。  
> 项目方向：基于 World of Warcraft 3.3.5a、TrinityCore、NPCBots、Eluna 和 zhCN 环境，同时进行服务端源码、数据库、Lua、配置以及客户端 DBC/MPQ/UI/模型等修改与优化。

---

## 可直接发送给上一个代理的提示词

```text
请暂停继续开发，先为我们当前的“魔兽世界 3.3.5a 客户端＋服务端魔改项目”执行一次完整、可验证、可迁移的技术交接。

这不是普通摘要。目标是：即使下一个代理完全看不到此前聊天记录，也能仅凭 GitHub 仓库、Workspace 文件和交接文档，准确识别哪一份是最终版本、哪些补丁已经真正安装、哪些只完成了设计或测试、下一步应该修改什么，并且不会重复安装、误用旧版、覆盖成果或破坏数据库与客户端。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一、固定项目背景
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 我们自己上传、用于保存本轮工作成果的 GitHub 仓库是：
   https://github.com/wpch3/wow-mecode
   目标分支：main

2. 我们的参考/上游整合源码是：
   https://github.com/328950225/TrinityCore-NPCBOT-Eluna-zhCN
   目标分支：NPCBOT-Eluna-zhCN-2026

3. 上游基础方向是：
   - World of Warcraft 3.3.5a（客户端具体 Build 必须核实；不要未经检查就假定）
   - TrinityCore 3.3.5
   - NPCBots
   - Eluna Lua Engine
   - zhCN 中文环境

4. 我们实际做的不只是服务端代码，还包括：
   - TrinityCore/NPCBots/Eluna C++ 修改
   - 服务端功能、性能和稳定性优化
   - NPCBot、PlayerBot、游荡 Bot、AI、战斗、交易、羁绊、持久化等功能
   - world/auth/characters 数据库和 SQL
   - worldserver/authserver 配置
   - Eluna Lua 脚本及 C++ 绑定/接入
   - 中文命令、中文文本、编码兼容
   - WoW 3.3.5a 客户端 DBC、MPQ、Lua/XML、Interface/AddOns、模型、贴图、声音或可执行文件兼容修改（以 Workspace 实际文件为准）
   - 客户端与服务端之间的 ID、字段、封包、数值上限和数据同步

5. `wpch3/wow-mecode` 当前可能更像“此前工作区、补丁库、测试代码、诊断记录和上传文件的集合”，不一定等于一份可直接编译的完整 TrinityCore 源码树。你必须验证这一点，不能看到 `src/` 就直接认定它是完整最终源码。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
二、必须生成的交接文件
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请在当前实际工作仓库根目录创建：

1. `PROJECT_HANDOFF.md`
   - 主交接文档，必须完整。

2. `HANDOFF_FILE_MANIFEST.tsv`
   - 文件清单，至少覆盖全部自定义、修改、补丁、SQL、Lua、配置、客户端资源、诊断记录和关键文档。
   - 使用 UTF-8 TSV，列至少为：
     `领域\t分类\t状态\t相对路径\t字节数\tSHA256\t权威等级\t用途/备注`
   - 如果文件数量允许，可覆盖整个 `wow-mecode` 仓库；如果太多，至少覆盖所有非上游原版文件、所有候选最终文件、所有二进制/客户端补丁和所有同名多版本文件。

3. `HANDOFF_GIT_STATE.txt`
   - 保存当前 Git 状态、分支、HEAD、remote、最近提交、未提交文件、未跟踪文件和关键 diff 摘要。
   - 不得包含访问令牌或凭据。

如果 Git 仓库根目录不是 `/home/user`，请确保上述文件位于实际 Git 仓库根目录；同时把 `PROJECT_HANDOFF.md` 再复制一份到 `/home/user/PROJECT_HANDOFF.md`，以便 Workspace 下载时容易找到。两份内容必须相同，并在文档里写明哪一份是权威副本。

不要只在聊天回复里显示内容，必须真正写入文件。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
三、审计原则与禁止事项
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 先审计，再写交接；不要仅凭记忆总结。
2. 回顾当前会话中能访问到的全部聊天内容，特别是用户最后确认的结论、编译报错、运行崩溃、返工和“当前版/最终版”说明。
3. 递归检查 Workspace，但不要浪费时间逐行阅读整个上游数万文件。优先检查：
   - 本会话创建或修改过的文件
   - `wpch3/wow-mecode` 中的自定义内容
   - Git diff、未跟踪文件和近期文件
   - 补丁索引、待办、改动清单、安装说明、测试报告
   - 同名多版本文件
   - 服务端与客户端的实际成品
4. 不得因为某个补丁文件“存在”，就写成“已经安装”。必须分开记录：
   - 已设计
   - 已生成补丁
   - 已复制到实际源码
   - 已编译
   - 已执行 SQL
   - 已部署
   - 已启动验证
   - 已进游戏验证
5. 不得因为测试桩或独立小程序通过，就写成 TrinityCore 全量编译或游戏内测试通过。
6. 对无法确认的状态必须写“待确认”，不能猜测。
7. 除交接文件和清单外，不得修改、重构、删除或格式化项目源码。
8. 不得执行以下破坏性操作：
   - `git reset --hard`
   - `git clean -fd` 或 `git clean -fdx`
   - 强制 checkout/restore 覆盖本地文件
   - 强制 push
   - merge、rebase 或自动解决冲突
   - 擅自更新 submodule
   - 擅自执行 SQL 到真实数据库
   - 覆盖真实客户端 MPQ、DBC、Wow.exe 或 Data 目录
   - 删除 build、日志、备份或诊断文件
9. 不要 commit、push 或创建 PR，除非用户另有明确要求。本任务只生成交接资料。
10. 不要在交接文件中写入数据库密码、IP、令牌、Cookie、私人路径或其他秘密值。
11. 仓库中如存在 `worldserver.conf`、日志、崩溃报告或上传文件，要检查是否含敏感信息；只记录“存在敏感信息及处理建议”，不得把秘密复制进交接文档。
12. 中文文件必须保持 UTF-8 可读。必须记录实际源码所需编码、BOM、CRLF/LF 和 MSVC 中文编译问题；不要擅自统一编码。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
四、先执行的取证检查
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请先定位所有可能的 Git 仓库和源码根目录，然后安全执行或记录等价信息：

- `pwd`
- 查找 Workspace 内的 `.git` 目录
- `git remote -v`
- `git branch --show-current`
- `git rev-parse HEAD`
- `git status --short --branch`
- `git log --oneline --decorate -20`
- `git diff --stat`
- `git diff --name-status`
- `git diff --cached --stat`
- `git ls-files --others --exclude-standard`
- 检查 `.gitmodules`、Git LFS、worktree（如存在）

必须分别记录并区分：

A. `wpch3/wow-mecode` 的本地根目录、分支和 HEAD。
B. 完整 TrinityCore/NPCBots/Eluna 服务端源码的本地根目录、分支、HEAD 和 remotes；如果 Workspace 中根本没有完整源码，明确写出。
C. 上游 `328950225/TrinityCore-NPCBOT-Eluna-zhCN` 的目标分支和我们实际采用的基线提交；若无法确认具体 commit，写“待确认”。
D. 实际运行服务器的源码、编译目录、部署目录和二进制来源；如果不在 Workspace，记录用户需要补充哪些信息。
E. 实际 WoW 客户端目录、版本、Build、语言区域和补丁位置；如果客户端文件不在 Workspace，明确列出缺失项。

不要主动 fetch/merge 上游来改变状态。如果仅为了读取远端信息必须访问网络，可以读取，但不要写入或合并。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
五、对 wow-mecode 仓库的专项分类
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请重点审计当前仓库中实际存在的这些目录或同类目录，并判断其真实角色：

- `tc-bignum/`
- `tc-bignum/补丁库/`
- `tc-bignum/sql/`
- `tc-bignum/lua_scripts/`
- `tc-bignum/conf/`
- `tc-bignum/tools/`
- `tc-bignum/规划/`
- `tc-bignum/流程文档/`
- `tc-bignum/待办总表.md`
- `tc-bignum/未完成想法-总清单.md`
- `src/`
- `fk/`
- `_chk/`
- `_chk2/`
- `_dt/`、`_dt2`
- `t7/`、`t8/`、`t9/`
- `v2/`、`v3/`、`v4/`
- `luatest/`
- `uploads/`

必须把每个目录归类为以下一种或多种：

1. 权威源文件/当前最终版
2. 可重放补丁或安装包
3. 完整服务端源码的一部分/文件快照
4. 客户端补丁或客户端构建输入
5. SQL 数据库迁移
6. Eluna Lua 脚本
7. 配置
8. 编译验证或测试桩
9. 诊断日志/用户上传证据
10. 旧版、备份、归档或已废弃版本
11. 仅规划、尚未实现
12. 状态无法确认

特别注意：

- 不要把 `_chk`、`_chk2`、`t7/t8/t9`、`v2/v3/v4` 中的测试程序或实验副本误判成发布代码。
- 不要把 `uploads/` 中的错误日志、用户上传文件或临时修复误判成权威源文件。
- 不要只根据文件名中的“最终版”“修复版”“当前”判断优先级，要结合聊天最后结论、内容差异、实际源码和测试结果。
- 如果同一功能存在多份 `.cpp/.h/.sql/.md`，必须建立“版本谱系”：旧版 → 修复版 → 当前候选 → 实际已安装版。
- 如果仓库不包含完整可编译服务端源码，要写清楚 `wow-mecode` 是补丁/工作成果仓库，而不是可直接构建的完整服务端仓库。

信息可信度建议按以下顺序判定，并在冲突时记录理由：

1. 实际部署/实际编译所使用的源码和数据库状态
2. 相对于准确上游基线的 Git diff
3. 最新且已验证的改动清单/安装说明
4. 补丁库索引与待办总表
5. 独立测试结果
6. 旧版备份、早期聊天和仅规划文档

如果缺少第 1 或第 2 层证据，不得把功能标成“已完成并部署”。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
六、PROJECT_HANDOFF.md 必须包含的内容
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

请按下面结构生成真实内容，不得留下空模板。

# 1. 项目身份与仓库关系

至少记录：

- 项目名称和目标
- WoW 版本、客户端 Build、zhCN 区域（实际核实）
- 自有工作仓库 URL、分支、HEAD
- 上游整合仓库 URL、目标分支、采用的基线 commit
- 完整服务端源码是否包含在 `wow-mecode`
- `wow-mecode`、完整服务端源码目录、编译目录、部署目录、客户端目录之间的关系
- TrinityCore、NPCBots、Eluna 各自的来源、版本/commit 或“待确认”
- 哪个仓库/目录是最终事实来源

# 2. 当前项目总状态

使用状态标记：

- ✅ 已完成并验证
- 🟢 已安装且编译通过，但尚未完整游戏内验证
- 🟡 已实现或有补丁，但尚未安装/编译/部署
- 🟠 只有设计、文档或测试桩
- 🔴 已知失败、崩溃或必须撤回
- ⚪ 已废弃/被新版本替代
- ❓ 无法确认

说明：

- 当前可以运行到什么程度
- 最近一次服务端编译是否成功
- 最近一次 authserver/worldserver 是否启动成功
- 最近一次客户端是否能登录并进入世界
- 最近一次工作停在哪里
- 当前最高优先级阻塞项
- 下一步唯一推荐动作

# 3. 权威文件地图

提供表格：

| 领域 | 功能 | 权威源/补丁路径 | 实际目标路径 | 当前版本 | 是否已安装 | 是否已验证 | 旧版/冲突路径 | 备注 |

必须覆盖：

- 服务端 C++
- NPCBots/PlayerBot
- Eluna 核心和 Lua
- SQL
- worldserver/authserver 配置
- 客户端 DBC/MPQ/UI/模型/贴图/声音
- 工具和生成脚本
- 测试与诊断
- 规划和待办

明确写出哪些文件是真正应该继续修改的，哪些只能参考，哪些禁止再使用。

# 4. 功能与补丁总矩阵

以 `tc-bignum/补丁库/00-补丁库索引.md`、补丁目录、改动清单、待办和实际源码为线索，整理所有 Axx、Fxx、Gxx 或其他编号功能。

每个功能必须记录：

| 编号 | 功能名 | 目标 | 权威目录 | C++目标文件 | SQL | Lua | 配置 | 客户端文件 | 已复制源码 | 编译 | SQL执行 | 部署 | 游戏内测试 | 当前结论 | 下一步 |

特别要求：

- “存在补丁”与“已安装”必须分开。
- “独立测试通过”与“TrinityCore 编译通过”必须分开。
- “服务端正常”与“客户端显示/交互正常”必须分开。
- 对被多次修复的功能，指出最终有效修复和必须弃用的旧方案。
- 记录此前已证明无效、会崩服、会刷屏、会产生野指针、重复注册或编码损坏的方案，防止重试。

# 5. 服务端 C++ 修改

按模块整理所有实际修改：

- TrinityCore 核心
- NPCBots AI、Bot 数据、命令、管理器、游荡 Bot、招募、队伍、交易、装备、宠物、移动和持久化
- PlayerBot 相关代码
- Eluna 接入、事件、绑定、加载器和 API
- 自定义指令、RBAC、ChatCommand
- 物品、装备、套装、法术、属性、血量、伤害、耐久、速度和数值扩容
- 场景、剧情、对话、服务 NPC
- 性能、线程安全、内存生命周期、指针安全和日志

每个改动至少记录：

- 目标源文件准确路径
- 修改的类/函数/数据结构
- 行为变化
- 为什么修改
- 与上游原版差异
- 与 NPCBots/Eluna 合并时的冲突点
- 编译状态
- 运行风险
- 对 SQL/配置/客户端的依赖

重点检查同名文件是否只是片段，例如 `src/Player.cpp`、`src/Unit.cpp`、`src/bot/*`、`src/bot_ai/*`；必须说明它们是完整文件、提取副本、比较基线还是当前候选，不能让新代理直接覆盖上游源码。

# 6. NPCBots、PlayerBot 与 Eluna 专项状态

分别说明：

## NPCBots

- 采用的 NPCBots 版本/基线
- AI 类和职业 Bot 修改
- Bot 数据表和配置
- 中文命令/名字/文本
- 游荡、固定、招募、召回、队伍、交易、装备、宠物和战斗行为
- 已知崩溃、ASSERT、野指针、加载刷屏和状态机问题
- 哪些修复已经验证，哪些只是分析

## PlayerBot

- PlayerBot 功能是否来自当前上游、额外模块或自定义移植
- 自动上线、交易、组队、踢人、UI/指令等状态
- 与 NPCBots 的边界和冲突
- 是否存在重复代码、重复注册或重复粘贴

## Eluna

- Eluna 核心源码位置和版本
- C++ 接入点和新增 API
- Lua 脚本实际加载目录
- Lua 脚本清单、事件注册和数据库依赖
- reload/启动方式
- 独立 mock 测试与真实 Eluna 运行测试的区别
- 中文编码、Lua 错误日志和加载状态

# 7. SQL 与数据库迁移

分别按 `auth`、`characters`、`world` 数据库整理，至少记录：

- 每个 SQL 文件路径和用途
- 正确执行顺序
- 目标数据库
- 是否幂等
- 是否需要备份
- 是否已经实际执行
- 执行日期、环境和结果（能确认时）
- 回滚方法或“无回滚脚本”
- 表、字段、索引、RBAC、command、locale、NPCBots 数据变化
- 自定义 ID 范围和冲突检查
- 字符集、排序规则、UTF-8/GBK/中文乱码风险
- SQL 与客户端 DBC、服务端枚举、C++ 字段宽度之间的关系

不得根据 SQL 文件存在就判断数据库已经更新。如果没有数据库快照或查询结果，标为“待在实际数据库验证”。

对于大数值/字段扩容，必须专门记录：

- MySQL 字段类型变化
- C++ 类型（有符号/无符号、32/64 位）
- 运算中的截断、溢出和格式化
- 客户端 3.3.5a 封包/显示上限
- DBC 字段限制
- 已验证的安全范围
- 仍无法突破的协议或客户端墙

# 8. 服务端配置与部署

记录真实环境（无法确认则待确认）：

- Windows/Linux
- MSVC/GCC/Clang 版本和架构
- CMake 版本、生成器和选项
- Boost、OpenSSL、MySQL/MariaDB 等依赖版本
- x64/x86
- Debug/Release/RelWithDebInfo
- 完整 CMake、编译、安装命令
- authserver/worldserver 二进制路径
- worldserver.conf/authserver.conf 和自定义 `.conf` 加载方式
- maps/vmaps/mmaps/dbc 位置和版本
- Lua 脚本部署路径
- 日志和崩溃转储位置

说明 `tc-bignum/conf/worldserver.conf.d/` 等配置文件哪些已被加载、哪些只是候选。记录配置键名和推荐值，但隐藏密码与连接串。

# 9. 客户端修改与客户端构建链

必须单独作为一级重点审计。记录实际存在或缺失的：

- 客户端版本、Build、zhCN/enUS 等区域
- 客户端根目录和 Data/locale 目录
- DBC 修改：表名、记录 ID、字段变化、源 CSV/SQL/脚本、导出工具和生成结果
- MPQ 补丁：文件名、加载顺序、区域目录、内部路径、打包工具、压缩方式、是否加密
- Interface、FrameXML、GlueXML、Lua/XML、AddOns
- M2、WMO、BLP、ADT、音效和其他资源
- Wow.exe 或其他二进制补丁（如有）
- 客户端缓存清理要求
- 客户端实际成品是否已上传到 GitHub/Workspace
- 因 GitHub 大小限制未上传的文件，其本地路径、文件名、大小和 SHA256

对每个客户端改动说明：

| 功能 | 客户端源文件 | 生成工具/命令 | 输出文件 | MPQ内部路径 | 服务端依赖 | 测试结果 | 风险 |

如果 Workspace 中只有客户端工具说明或规划、没有真正的 DBC/MPQ/客户端文件，必须明确写：

“目前客户端部分只有文档/工具/规划，实际客户端补丁未包含在当前仓库，不能视为已交付。”

不得把服务端 SQL 修改当作客户端 DBC 已同步。

# 10. 客户端—服务端一致性矩阵

对所有会同时影响客户端和服务端的功能建立矩阵：

| 功能 | 服务端 C++ | world/auth/characters SQL | Eluna/Lua | 配置 | DBC | MPQ/UI/模型 | 协议/封包限制 | 当前一致性 | 验证步骤 |

重点覆盖：

- 自定义物品、装备、套装、法术和技能
- 自定义 NPC、Gossip 和文本
- 属性、血量、伤害、耐久、等级或人数上限
- 模型、显示 ID、种族、职业
- 中文字符串和命令
- NPCBot/PlayerBot UI 或客户端交互
- 任何需要同时更新数据库、服务端枚举和客户端 DBC 的 ID

记录所有 ID 冲突、客户端显示异常、封包截断和服务端正常但客户端错误的情况。

# 11. 编码、中文与换行规范

结合此前实际问题记录：

- 哪些 C++/SQL/Lua 文件是 UTF-8、UTF-8 BOM、GBK 或其他编码
- MSVC 是否启用 `/utf-8`
- 中文字符串是否曾变为 `?`、乱码或触发 C2447 等编译问题
- 哪些文件不得自动转码
- SQL 客户端和数据库连接字符集
- Lua、DBC、MPQ 内文本编码要求
- CRLF/LF 对 `.sh`、`.bat`、CMake 和 Windows 编译的影响
- 已验证的安全编辑流程

# 12. 构建、测试与验证证据

严格区分以下验证层级：

1. 静态检查
2. Python/Shell 工具检查
3. 单文件语法检查
4. mock/独立 C++ 测试
5. CMake 配置成功
6. TrinityCore 全量编译成功
7. authserver 启动成功
8. worldserver 启动成功
9. 数据库更新成功
10. 客户端登录成功
11. 角色进入世界成功
12. 功能游戏内实测成功
13. 长时间/多人/多 Bot 稳定性测试成功

用表格记录：

| 日期 | 功能 | 验证层级 | 命令/步骤 | 环境 | 结果 | 日志路径 | 是否足以结案 |

未真正运行的测试写“未运行”。日志中的旧错误必须标记是否已被后续修复。

# 13. 已知崩溃、编译错误和无效方案

汇总此前出现过的：

- MSVC Cxxxx/LNKxxxx 错误
- CMake/架构/依赖错误
- authserver/worldserver 启动失败
- 登录、切角色、招募、交易、移动、镜像、宠物、队伍等崩溃
- ASSERT、野指针、生命周期、重复注册
- Bot 发呆、卡位、不追击、刷屏或状态错误
- Eluna 加载失败
- 客户端闪退、MPQ 不生效、模型消失、DBC 不匹配
- 中文乱码或源码被问号破坏

每个问题记录：

- 症状
- 根因证据
- 尝试过的方案
- 哪些方案无效或危险
- 当前有效修复
- 最终验证程度
- 相关文件和日志

不要把“写了根因分析文档”自动等同于“问题已解决”。

# 14. 需求、设计决定与不可破坏事项

记录用户已经确认的：

- 最终玩法方向
- 哪些功能必须保留
- 哪些功能已经暂停、撤回或被替代
- 性能、稳定性与玩法之间的取舍
- NPCBots 与 PlayerBot 的定位
- 客户端魔改范围
- 数值上限和协议限制的接受程度
- 中文化要求
- 文件组织和补丁编号规范

列出新代理绝对不能做的事，例如：

- 不得用上游原版文件整份覆盖已修改文件
- 不得重复应用同一补丁
- 不得执行旧版 SQL
- 不得把测试桩复制进生产源码
- 不得擅自改变自定义 ID
- 不得在没有备份时覆盖数据库或客户端
- 不得忽略客户端与服务端同步
- 不得把“规划”写成“已完成”

# 15. 未完成任务与优先级

按以下优先级整理：

- P0：会丢数据、崩服、无法编译、无法启动、无法登录或可能覆盖成果
- P1：核心功能尚未完成或客户端/服务端不一致
- P2：稳定性、性能、Bot AI 和体验优化
- P3：新玩法、剧情、模型、现代化和长期规划

每项必须包含：

- 任务编号和名称
- 当前证据状态
- 前置依赖
- 应读取的文件
- 应修改的权威文件
- 禁止使用的旧文件
- SQL/配置/客户端依赖
- 具体下一步
- 完成标准
- 验证步骤
- 是否需要先询问用户

# 16. 下一位代理的接手顺序

给出不少于 10 步的具体接手流程。至少包括：

1. 阅读 `PROJECT_HANDOFF.md`。
2. 校验 `HANDOFF_FILE_MANIFEST.tsv` 中的关键文件和 SHA256。
3. 确认 `wow-mecode/main` 的 HEAD。
4. 找到完整 TrinityCore/NPCBots/Eluna 源码；找不到则先询问用户，不得从片段重建。
5. 确认准确上游基线 commit。
6. 区分补丁库、实际源码、测试桩、上传日志和旧版。
7. 验证当前 P0 问题，而不是立即写代码。
8. 对任何 SQL 先做只读检查和备份计划。
9. 对客户端改动先确认 Build、locale、MPQ 加载顺序和备份。
10. 修改前创建独立分支或备份，并取得用户确认。
11. 每次只处理一个功能，记录 C++/SQL/Lua/配置/客户端联动。
12. 按验证层级逐步验证，不得用 mock 结果替代全量编译和游戏内测试。

# 17. 需要用户补充的外部资料

单独列出未包含在 GitHub/Workspace 中但继续工作必需的内容，例如：

- 完整服务端源码
- `.git` 历史或准确 commit
- 当前编译目录和 Visual Studio/CMake 环境
- 实际 auth/world/characters 数据库结构或只读导出
- 当前 worldserver/authserver 配置（脱敏）
- 实际运行日志和崩溃转储
- 实际 WoW 客户端 Build
- 客户端 Data、MPQ、DBC、Interface 和补丁成品
- 未上传的大文件及 SHA256

说明每项资料缺失会影响什么判断。

# 18. 安全与敏感信息检查

检查仓库中的配置、日志和 `uploads/` 是否可能包含：

- 数据库用户名/密码
- 外网 IP、内网 IP、域名
- GitHub Token
- API Key
- 真实本地用户名和绝对路径
- Cookie、会话或私人信息

不得在文档中复制具体值。只写：

- 涉及文件路径
- 风险等级
- 建议删除、脱敏、轮换凭据或重写 Git 历史

# 19. 可直接给新代理的继续提示词

在 `PROJECT_HANDOFF.md` 最后生成一段针对当前真实状态的提示词。它必须包含：

- 两个 GitHub 仓库及正确分支
- 当前权威文件位置
- 当前 P0 任务
- 需要先验证的事实
- 禁止覆盖/重复应用的内容
- 第一个推荐操作
- 需要向用户询问的问题

不能只写泛泛的“请继续项目”。

# 20. 交接完整性声明

最后明确声明：

- 是否看完了可访问聊天上下文
- 是否检查了整个 `wow-mecode` 仓库
- 是否找到完整服务端源码
- 是否检查了 Git 状态
- 是否确认上游基线 commit
- 是否找到真实客户端文件
- 是否检查 SQL/Lua/配置
- 是否实际编译或测试
- 哪些状态无法确认
- 哪些文件可能未进入 GitHub/Workspace
- 是否建议用户重新下载最新 Workspace

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
七、交接文件写作质量要求
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 必须写实际路径、函数、类、SQL 名称、补丁编号、命令和状态，不能只写概括。
2. 不能留下“稍后填写”“根据情况补充”等空模板。
3. 同一功能有多版本时，必须指出唯一权威候选；无法判断就列出候选差异并标“待用户确认”。
4. 不得根据文件修改时间单独判断新旧，因为 ZIP 解压和 Git 上传可能改变时间。
5. 不得根据文档标题中的“最终版/当前版”单独判断。
6. 任何“已完成”都必须附证据层级。
7. 任何“已部署”都必须指出部署环境和验证依据。
8. 任何“客户端已完成”都必须指出实际 DBC/MPQ/资源文件和游戏内结果。
9. 任何“数据库已完成”都必须指出实际数据库查询或执行证据；否则标待验证。
10. 完成后重新读取三个交接文件，检查 Markdown、TSV、中文编码和路径是否正确。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
八、完成后的聊天回复格式
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

完成后，请只给我一份简洁确认，包含：

1. `PROJECT_HANDOFF.md` 的准确路径、行数和文件大小。
2. `HANDOFF_FILE_MANIFEST.tsv` 的准确路径和记录数量。
3. `HANDOFF_GIT_STATE.txt` 的准确路径。
4. 是否找到完整 TrinityCore 服务端源码。
5. 是否找到实际客户端 DBC/MPQ/客户端成品。
6. 当前认定的权威工作目录。
7. 当前 P0 问题和第一个推荐动作。
8. 无法确认的关键事项。
9. 仓库是否发现潜在敏感信息（不得显示秘密值）。
10. 明确提醒我：现在应该重新下载最新 Workspace，确保交接文件进入备份。

现在开始审计并创建这些文件。不要继续实现其他功能。
```

---

## 使用完成后的操作

1. 等旧代理确认三个文件已经真正写入 Workspace。
2. 在 Workspace 面板打开 `PROJECT_HANDOFF.md`，检查其中是否有实际路径和状态，而不是空模板。
3. **重新下载一次 Workspace**；旧 ZIP 不包含新创建的交接文件。
4. 将最新版 Workspace 或 `wow-mecode` 仓库交给新代理。
5. 给新代理的第一句话应是：

```text
请先完整阅读 PROJECT_HANDOFF.md，并校验 HANDOFF_FILE_MANIFEST.tsv 与 HANDOFF_GIT_STATE.txt。不要从头重建，不要把 src/ 片段当作完整 TrinityCore 源码，不要重复应用补丁或执行 SQL。先告诉我两个仓库、准确基线、权威文件、P0 问题以及你准备采取的第一步，得到确认后再修改。
```

## 安全提醒

`wow-mecode` 是公开仓库，而且包含配置/日志/上传资料类文件。请让旧代理检查是否存在数据库密码、服务器地址、Token 或私人路径。如果曾上传真实凭据，应立即轮换凭据，并考虑删除敏感文件及清理 Git 历史。交接文档只能记录“变量名称和风险”，不能复制真实秘密值。
