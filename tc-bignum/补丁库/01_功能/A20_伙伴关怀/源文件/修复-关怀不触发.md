# 修复：残血时 bot 不给食物（已测试验证）

> 用户反馈：「还是没有在我残血时帮助我，我怀疑是被跟随系统限制了」
> 结论：**不是跟随系统，是我写的两个 bug**
> 验证：`compile_test/caretest2.cpp` **14/14 通过**（先复现bug再验证修复）

---

## 一、先排除跟随距离

我在关怀循环里写的是 `GetDistance(master) > 30.0f` 就 return。

而 bot 默认跟随距离（`bot_ai.cpp:852`）远小于 30 码，
**正常跟随时不可能超过。所以跟随系统是清白的。**

---

## 二、Bug 1（致命）：bot 的背包永远是空的

`RestockBot()` 是唯一往虚拟背包放东西的函数，
但我**只在"玩家升级"这一个分支里调用了它**：

```cpp
if (_lastMasterLevel && curLevel > _lastMasterLevel)
{
    ...
    sBotCompanionMgr->RestockBot(...);   // <- 只有升级才补货
    return;
}
```

**后果**：招募一个bot -> 背包空 -> `FindInInventory` 返回 0 -> 什么都不发生。
**除非你恰好带着它升了一级，否则它一辈子拿不出东西。**

**测试已复现**：
```
[复现 Bug1: 冷启动背包为空]
  【旧版】残血却什么都不给 <- 这就是用户遇到的bug
  【旧版】背包确实是空的
```

---

## 三、Bug 2：残血时几乎总在战斗中

```cpp
if (me->IsInCombat() || master->IsInCombat())
    return;
```

**残血的时刻 99% 在战斗中**，脱战要 5-6 秒，那时你早自己吃了。
设计初衷是"打架时不啰嗦"，但把最需要帮助的时刻也排除了。

---

## 四、改动清单（4 处，全在 bot_ai）

### 改动 1：`bot_ai.h` 加两个成员

**找到你之前加的那 4 个成员**（在 `bool Feasting() const;` 附近）：

```cpp
    uint32 _careTimer;
    uint32 _careChatTimer;
    uint8  _lastMasterLevel;
    bool   _masterWasDead;
```

**在它们下面再加两个**：

```cpp
    bool   _careStocked;         // step34修复: 是否已做过初始补货
    uint32 _careRestockTimer;    // step34修复: 定期补货计时
```

### 改动 2：`bot_ai.cpp` 构造函数初始化

**找到你之前加的初始化**（`bot_ai.cpp:169` 附近）：

```cpp
    _careTimer        = urand(20000, 40000);
    _careChatTimer    = urand(60000, 120000);
    _lastMasterLevel  = 0;
    _masterWasDead    = false;
```

**下面加两行**：

```cpp
    _careStocked      = false;
    _careRestockTimer = 0;
```

### 改动 3：`CommonTimers` 里递减新计时器

**找到你之前加的**（`bot_ai.cpp:18534` 附近）：

```cpp
    if (_careTimer > diff)          _careTimer -= diff;      else _careTimer = 0;
    if (_careChatTimer > diff)      _careChatTimer -= diff;  else _careChatTimer = 0;
```

**下面加一行**：

```cpp
    if (_careRestockTimer > diff)   _careRestockTimer -= diff; else _careRestockTimer = 0;
```

### 改动 4：`UpdateCompanionCare` 函数体（核心）

这是主要改动。**用下面的版本替换掉整个函数**：

```cpp
void bot_ai::UpdateCompanionCare(uint32 diff)
{
    // ---- 前置：总开关 ----
    if (!BotCfg::IsCompanionCareEnabled())
        return;

    // ---- 前置：必须是有主人的 bot ----
    if (IAmFree() || !master || !master->IsInWorld())
        return;

    // ---- 前置：临时bot不参与 ----
    if (IsTempBot())
        return;

    // ---- 前置：bot自己要活着 ----
    if (!me->IsAlive())
        return;

    // ============================================================
    //  修复1：补货。原来只在"玩家升级"时补，导致背包一直是空的。
    //  改成：首次见面就补 + 每10分钟补一次。
    // ============================================================
    if (!_careStocked || _careRestockTimer == 0)
    {
        _careStocked = true;
        _careRestockTimer = 600000;   // 10分钟
        sBotCompanionMgr->RestockBot(me->GetGUID().GetCounter(), master->GetLevel());
    }

    // ---- 复活关心 ----
    if (!master->IsAlive())
    {
        _masterWasDead = true;
        return;                     // 主人死着的时候不啰嗦
    }
    else if (_masterWasDead)
    {
        _masterWasDead = false;
        std::string txt = sBotCompanionMgr->PickText(CARE_TYPE_REVIVE, _botclass);
        if (!txt.empty())
        {
            BotWhisper(txt);
            _careTimer = urand(30000, 60000);
        }
        return;
    }

    // ============================================================
    //  修复2：战斗中【不再直接return】。
    //  因为残血的时刻几乎总在战斗中，等脱战5-6秒玩家早自己吃了。
    //  改成：战斗中保留"给食物/给水"，只禁止闲聊。
    // ============================================================
    bool const inCombat = me->IsInCombat() || master->IsInCombat();

    // ---- 修复3：战斗中距离放宽（打起来容易拉开）----
    if (me->GetDistance(master) > (inCombat ? 60.0f : 30.0f))
        return;

    // ---- 升级祝贺 ----
    uint8 curLevel = master->GetLevel();
    if (_lastMasterLevel && curLevel > _lastMasterLevel)
    {
        _lastMasterLevel = curLevel;
        std::string txt = sBotCompanionMgr->PickText(CARE_TYPE_LEVELUP, _botclass);
        if (!txt.empty())
        {
            BotWhisper(txt);
            _careTimer = urand(20000, 40000);
        }
        // 等级变了，能给的东西也该升级
        sBotCompanionMgr->RestockBot(me->GetGUID().GetCounter(), curLevel);
        return;
    }
    _lastMasterLevel = curLevel;

    // ---- 普通关怀：受冷却限制 ----
    if (_careTimer)
        return;

    uint32 botGuid = me->GetGUID().GetCounter();

    // ---- 优先级 1：你饿了 ----
    if (GetHealthPCT(master) < BotCfg::GetCompanionCareHealthPct())
    {
        std::string from;
        uint32 itemId = sBotCompanionMgr->FindInInventory(botGuid, CARE_TYPE_FOOD, curLevel, from);
        if (itemId && TryGiveItemToMaster(itemId, 1, CARE_TYPE_FOOD, from))
            return;
    }

    // ---- 优先级 2：你渴了 ----
    if (master->GetMaxPower(POWER_MANA) > 1 &&
        GetManaPCT(master) < BotCfg::GetCompanionCareManaPct())
    {
        std::string from;
        uint32 itemId = sBotCompanionMgr->FindInInventory(botGuid, CARE_TYPE_DRINK, curLevel, from);
        if (itemId && TryGiveItemToMaster(itemId, 1, CARE_TYPE_DRINK, from))
            return;
    }

    // ---- 优先级 3：你穷了（战斗中不给钱，不合时宜）----
    if (!inCombat)
    {
        uint32 moneyThreshold = BotCfg::GetCompanionCareMoneyThreshold();
        if (moneyThreshold && master->GetMoney() < moneyThreshold)
        {
            uint32 give = BotCfg::GetCompanionCareMoneyGive();
            if (give)
            {
                master->ModifyMoney(int32(give));

                std::string txt = sBotCompanionMgr->PickText(CARE_TYPE_MONEY, _botclass);
                if (!txt.empty())
                {
                    char goldbuf[32];
                    snprintf(goldbuf, sizeof(goldbuf), "%u", give / 10000);
                    size_t pos = txt.find("{gold}");
                    if (pos != std::string::npos)
                        txt.replace(pos, 6, goldbuf);
                    BotWhisper(txt);
                }
                _careTimer = BotCfg::GetCompanionCareCooldown();
                return;
            }
        }
    }

    // ---- 优先级 4：闲聊（只在脱战）----
    if (!inCombat && !_careChatTimer)
    {
        std::string txt = sBotCompanionMgr->PickText(CARE_TYPE_CHAT, _botclass);
        if (!txt.empty())
        {
            BotWhisper(txt);
            _careChatTimer = BotCfg::GetCompanionChatCooldown();
            _careTimer     = BotCfg::GetCompanionCareCooldown() / 2;
        }
    }
}
```

---

## 五、改之前先确认问题（30秒）

```sql
SELECT * FROM `characters`.`npcbot_inventory`;
```

- **返回 0 行** -> 确认 Bug1，背包确实空的
- **有数据** -> 是别的问题，把结果发我

**再确认物品池加载成功**：

```sql
SELECT care_type, COUNT(*) FROM `world`.`npcbot_care_item` GROUP BY care_type;
```

应该看到 `1`（食物）和 `2`（水）各有几条。
**如果这里是空的**，说明 01 号 SQL 的 JOIN 一个都没匹配上
（你的 item_template 里没有那些 itemid），那才是根本问题。

---

## 六、验证

```
[ ] 编译，招募一个bot
[ ] 查 SELECT * FROM characters.npcbot_inventory;  -> 应该有 2 行（食物+水各5个）
[ ] 打怪掉到50%血以下
    预期：【战斗中就会】收到食物 + 一句话
[ ] 连续消耗5次后
    预期：给不出来了（包空了），10分钟后自动补货
```

---

## 七、测试

```bash
cd patches/step34_伙伴关怀/compile_test
g++ -std=c++17 -O0 -Wall -Wextra -o caretest2 caretest2.cpp
./caretest2
```

**14/14 通过**。这次是**先写测试复现bug，再改代码**：

```
[复现 Bug1] 【旧版】残血却什么都不给      <- 复现了用户的问题
[验证修复] 【新版】冷启动就能给食物
[复现 Bug2] 【旧版】战斗中残血也不给
[验证修复] 【新版】战斗中残血会给食物
[战斗中不闲聊] 战斗中满血->不闲聊 / 脱战满血->闲聊
[距离阈值] 战斗中45码在范围 / 脱战45码超范围
[核心保证] 补货5个正好给5次就没了
```

---

## 八、我的错误

**把"补货"挂在了升级事件上** —— 升级是低频事件，而背包为空是初始状态。

上一版单测 56/56 全过，却没发现这个bug，因为
**我只测了 `RestockBot` 函数本身，没测"从招募到第一次给东西"的完整链路。**

**教训：单元测试必须覆盖【冷启动状态】，不只是函数正确性。**
