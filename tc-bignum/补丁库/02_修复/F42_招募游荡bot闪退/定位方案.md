# F42 · 招募游荡bot 闪退（战士，无服务端提示）

> 2026-08-09
> 用户：「编译好了，但是招募后还是会闪退，这次招募的是战士，
> 没有出现任何服务端提示就闪退了」

---

## 一、先说清 F39 那段为什么搜不到

你说搜不到这段：

```cpp
if (!mmover && IsWanderer() && me->IsInCombat())
```

**因为那是 F39【要你新增】的代码，不是原有代码。**

F39 的改动2 是：找到 `bot_ai.cpp:18367` 这行原文

```cpp
            Unit const* mmover = !IAmFree() ? master : nullptr;
```

**在它下面插入**一整段新代码（就是含 `IsInCombat()` 的那段）。

**如果你没装 F39 改动2，那 F41 的改动3 自然无从改起。**

### 现在怎么办

**直接装 F41 的最终版**（等于 F39改动2 + F41改动3 合并）：

搜索原文（`bot_ai.cpp:18367`，全文件唯一）：

```cpp
            Unit const* mmover = !IAmFree() ? master : nullptr;
```

在它**下面**插入：

```cpp
            // F41: 游荡bot 是自由身，mmover 恒为 nullptr，
            //   导致非战场情况下拿不到任何移动目标 -> 看见怪也不上前。
            //   不加 IsInCombat() 判断 —— 中立怪不主动攻击，
            //   bot 永远进不了战斗状态，加了这个条件就永远不执行。
            if (!mmover && IsWanderer())
            {
                if (Unit* victim = me->GetVictim())
                {
                    float const wantDist = HasRole(BOT_ROLE_RANGED) ? 25.0f : 1.5f;
                    if (me->GetDistance(victim) > wantDist)
                        GetInPosition(true, victim);
                }
            }
```

**F39 的改动1（hasRealMaster）还是要装**，那个防的是假 master 解引用。

---

## 二、闪退：我需要日志，不能再猜

### 为什么不能猜

我已经错了三次（F37/F38/F40），每次都是"读源码找可疑点然后赌一把"。
**这次招募的是战士 —— 战士没有镜像分身，所以 F40 的判断对这次不适用。**

坑表已记：**用户报崩溃，第一句话就要日志。**

### 我查到的可疑点（供参考，不作为结论）

`bot_ai.cpp:281-321 SetBotOwner()` 全程**没有清除 `_wanderer` 标记**：

```cpp
:303   if (newowner->GetBotMgr()->AddBot(me) & BOT_ADD_FATAL)
:309   spawned = false;
:311   unit_flags2 &= ~(UNIT_FLAG2_ALLOW_ENEMY_INTERACT);
:318   master = newowner;
       // 【全程没有 _wanderer = false】
```

而整个源码里**只有 `SetWanderer()`，没有 `UnsetWanderer()`**：

```
bot_ai.cpp:248     SetWanderer();
bot_ai.cpp:20412   void bot_ai::SetWanderer()
bot_ai.h:196       void SetWanderer();
（没有 UnsetWanderer）
```

**推论（未验证）**：招募后 bot 变成"既有 master、又是 wanderer"的混合状态。
此时 `bot_ai.cpp:18342` 那个 `if (IsWanderer() || ...)` 会成立，
里面 `:18348 master->IsAlive()` 拿到的是**真 master**（没问题），
但其他依赖 `IsWanderer()` 的分支可能用错逻辑。

> **这只是可疑点，不是结论。** 必须用日志确认。

---

## 三、请你做这两件事

### 第1件：拿崩溃日志（关键）

```
D:\TC-Build\bin\RelWithDebInfo\Crashes\
```

找**最新**的 `worldserver.exe_[日期时间].txt`，
把**开头到第一个 Call stack 结束**贴给我（约 40 行）。

**怎么看**（坑表记过）：
- 只看**第一个** Call stack
- 找带**我们文件名**的那行（`bot_ai.cpp` / `botmgr.cpp` / `Map.cpp`）
- 忽略 `boost::asio` / `Nt*` / `Rtl*` / `BaseThreadInitThunk`

### 第2件：如果没有 Crashes 目录

说明崩溃日志没开。在 `worldserver.conf` 里搜：

```ini
CrashLog.Enable = 1
```

没有这一行就加上，重启后再复现一次。

**或者**：直接看控制台窗口最后几行
（闪退前的输出，即使没有 ASSERT 也可能有线索）。

---

## 四、临时规避（等日志期间可以先用）

如果急着玩，有两个绕法：

### 绕法1：别招募游荡bot，用固定bot

```
.npcbot spawn <entry>      直接生成一个新bot（不是游荡的）
```

固定bot 走的是另一条路径，不会碰到这个问题。

### 绕法2：招募前先让它脱离游荡状态

目前**没有现成命令**能做这个（因为没有 `UnsetWanderer`）。
如果日志确认是 wanderer 标记的问题，我会给你加一个。

---

## 五、如果日志显示是 wanderer 标记问题

**预案**（等日志确认后再装）：

在 `bot_ai.cpp:309` 那行

```cpp
    spawned = false;
```

**下面**加：

```cpp
    // F42: 招募后必须解除游荡状态。
    //   上游只有 SetWanderer() 没有反向操作，
    //   导致 bot 变成"既有master又是wanderer"的混合态。
    if (_wanderer)
    {
        _wanderer = false;
        _travel_node_cur = nullptr;      // 清掉路点，防止野指针
        _travel_node_last = nullptr;
    }
```

> **变量名待确认** —— 我需要先看 `bot_ai.h` 里 `_wanderer` 和路点成员的
> 确切名字和访问段。**等日志确认方向后我再实查给准确代码。**

---

## 附：实查位置

```
# ===== 招募流程 =====
bot_ai.cpp:281      bool bot_ai::SetBotOwner(Player* newowner)
bot_ai.cpp:296          if (!IAmFree()) -> 已有主人则拒绝
bot_ai.cpp:303          newowner->GetBotMgr()->AddBot(me)
bot_ai.cpp:309          spawned = false;
bot_ai.cpp:311-312      清 UNIT_FLAG2_ALLOW_ENEMY_INTERACT
bot_ai.cpp:318          master = newowner;
                        ^^^ 全程没有清 _wanderer

botmgr.cpp:867      BotAddResult BotMgr::AddBot(Creature* bot)
botmgr.cpp:869-870      ASSERT(bot->IsNPCBot()) / ASSERT(GetBotAI() != nullptr)
botmgr.cpp:939          ASSERT(!bot->GetCreator());       <- 硬断言
botmgr.cpp:942          bot->SetCreator(_owner);
botmgr.cpp:948          bot->SetFaction(_owner->GetFaction());
botmgr.cpp:951          SetBotOwner(_owner)
botmgr.cpp:958          TeleportBot(bot, _owner->GetMap(), _owner)
botmgr.cpp:969          SetBotCommandState(BOT_COMMAND_FOLLOW, true)
botmgr.cpp:971          AddBotToGroup(bot)

# ===== creator 清理（F40 相关，确认没破坏）=====
botmgr.cpp:746      bot->SetCreator(nullptr);    在 CleanupsBeforeBotDelete 里
                    所以 :939 的 ASSERT 正常情况下能过

# ===== wanderer 标记 =====
bot_ai.cpp:248      SetWanderer();           spawnId==0 时自动成为游荡bot
bot_ai.cpp:20412    void bot_ai::SetWanderer()
bot_ai.h:196        void SetWanderer();      public(53段)
（全源码无 UnsetWanderer）
```
