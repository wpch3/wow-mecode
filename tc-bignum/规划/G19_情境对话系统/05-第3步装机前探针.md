# G19 第3步 · 装机前探针（必须先跑）

> 2026-08-18
> 你的 grep 结果显示：**你的源码基线和 G19第3步文档（04号）假设的不一样。**
> 直接照 04 号装会搜不到原文、或者下标错位导致读错字段。
> **先跑这个探针，我拿到真实原文后给你精确到行的补丁。**

---

## 一、为什么必须先探针（不是我啰嗦）

你的 grep 暴露了三处与 04 号文档不符：

| 项 | 04号文档假设 | 你的实际 | 影响 |
|---|---|---|---|
| `PickText` 实现位置 | `bot_companion.cpp:189` | **`:684`** | 差 495 行，说明文件被大改过 |
| `CompanionText` 字段 | 无 `BotRace` | **`.h:53` 已有** | A42第2步已装 |
| `BotRace` 出现次数 | 1 处 | **两个 struct 各一个**（`:53` / `:108`） | 有两套台词系统 |

另外我注意到 **`bot_companion.cpp:273` 有 `score += 100; // 种族最优先`** ——
这正是 **A42修复（PickGiftText 权重 种族100/职业50/物品20）的特征**。

**如果确认属实，第二批就不用装 A42修复了，它已经在里面。**

---

## 二、探针命令（Git Bash 里跑，一次性）

打开 **Git Bash**，整段复制粘贴执行：

```bash
cd /d/TrinityCore/src/server/game/AI/NpcBots && {
echo "=============== A. PickText 实现全文 ==============="
sed -n '680,760p' bot_companion.cpp

echo "=============== B. LoadCareTexts 的 SELECT 和赋值 ==============="
sed -n '38,70p' bot_companion.cpp

echo "=============== C. CompanionText 结构体 ==============="
sed -n '44,66p' bot_companion.h

echo "=============== D. PickGiftText 评分段（确认A42修复是否已装）==============="
sed -n '255,285p' bot_companion.cpp

echo "=============== E. 头文件 private 段 ==============="
grep -n "private:\|_texts\|_giftTexts\|unordered_map" bot_companion.h

echo "=============== F. bot_companion.cpp 的 include ==============="
grep -n "#include" bot_companion.cpp

echo "=============== G. bot_ai.cpp 的关键 include ==============="
grep -n "#include" bot_ai.cpp | grep -iE "DBCStores|GameTime|Map\.h|bot_companion"

echo "=============== H. _careChatTimer 所有赋值 ==============="
grep -n "_careChatTimer" bot_ai.cpp

echo "=============== I. bot_ai.h 的 public 段锚点 ==============="
grep -n "IsWanderer\|SetWanderer" bot_ai.h
} 2>&1 | tee /d/probe_g19.txt
```

跑完结果会同时**显示在屏幕上**，并存到 **`D:\probe_g19.txt`**。

**把输出贴给我**（或者把 `D:\probe_g19.txt` 传上来）。

---

## 三、我拿到之后会做什么

```
1. 用【A段】的真实原文，重写 PickText 的替换补丁
   -> 保证你能 Ctrl+F 搜到，不会"搜不到原文"

2. 用【B段】确认 SELECT 的字段顺序和下标
   -> 加 scene/zone_id/map_id/cooldown 后，下标要重排，错一位就读错字段

3. 用【C段】确认 CompanionText 现有成员
   -> 决定新字段插在哪一行之后

4. 用【D段】确认 A42修复是否真的已装
   -> 已装就从第二批里划掉，省一次改动

5. 用【F/G段】确认 include
   -> GameTime.h（冷却要用 GameTime::GetGameTime()）
      DBCStores.h（GetCurrentScene 要用 sAreaTableStore）
   缺哪个我告诉你加哪个

6. 用【H段】确认 _careChatTimer 有几处
   -> 04号文档说"2-3处"，实际几处要看你的

7. 用【I段】确认 bot_ai.h 的 public 段位置
   -> GetCurrentScene() 声明要插在 public 段里，插错会编译失败
```

---

## 四、这一批的最新状态（A42修复可能要划掉）

| 项 | 原计划 | 现状 |
|---|---|---|
| **F40** 剑圣镜像登出闪退 | 要装 | **已实查完毕，可以直接装**（见下文） |
| **G19第3步** 场景感知 | 要装 | **等这个探针** |
| **A42修复** 礼物台词权重 | 要装 | **疑似已装**（`:273 score += 100 种族最优先`），探针D段确认 |

---

## 五、F40 不用等探针，现在就能装

F40 的两处改动我已经在上游 `4e8762e` 上**逐行实查完毕，原文完全匹配**：

```
botmgr.cpp:803        CleanupsBeforeBotDelete(guid, removetype);     <- 改动1插这上面
botmgr.cpp:795-796    已有的 SetCreator(nullptr)，在 DungeonBot 分支（走不到登出路径）
Map.cpp:1065-1073     if (obj->IsInGrid()) ... else { 只打日志 }      <- 改动2
Unit.h:1242           void SetOwnerGUID(ObjectGuid owner);      public  [是]
Unit.h:1261           void SetCreator(Unit* creator);           public  [是]
Creature.h:391        Unit* GetBotsPet() const;                        [返回Unit*不是Creature*]
```

**两个函数都是 public，编译没问题。**

具体改法见 `补丁库/02_修复/F40_剑圣镜像导致登出闪退/根因与修复.md`
的「改动1」和「改动2」，**原文和行号都是准的，可以直接照做**。

Lua 那步（改动3）你说文件存在，那三处一起做完再编译。

---

## 六、为什么不让你直接照 04 号文档装

**因为会失败，而且是那种"改了一半才发现搜不到"的失败。**

举个具体的：04 号文档让你搜

```cpp
QueryResult result = WorldDatabase.Query("SELECT id, care_type, bot_class, bot_race, text, emote, weight FROM npcbot_care_text");
```

但你的 `bot_companion.cpp:56` 是 `t.BotRace = fields[3]`，
说明 SELECT 里 `bot_race` 确实在第 4 位——这部分对得上。

**可 `PickText` 在 `:684` 而不是 `:189`**，中间差了近 500 行，
说明文件结构和文档写的时候完全不同了（多了 gift_text 那一套）。
整函数替换如果贴错位置，会把别的函数覆盖掉。

**先看清楚，再动手。这一步花你 1 分钟，能省掉一次编译失败。**
