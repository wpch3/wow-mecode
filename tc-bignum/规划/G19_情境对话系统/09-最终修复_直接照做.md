# G19 第3步 · 最终修复（两步，直接照做）

> 2026-08-18
> 你的自检已给出精确答案：**bot_companion.cpp 删第 775-827 行**。
> 本文不再有任何"搜索/在下面加"的模糊说法，全部是完整块或精确行号。

---

# 第1步 · `bot_companion.cpp` 删除第 775-827 行

## 做法（VS 里最快）

```
1. Ctrl+G  ->  输入 775  ->  回车        （光标跳到第775行）
2. 按 Home 键                            （光标到行首）
3. 按住 Shift，再 Ctrl+G 输入 828 回车   （选中 775-827 整块）
   —— 或者按住 Shift 一直按 ↓ 到第 828 行行首
4. Delete
```

## 删完的验收（这两行必须紧挨着）

```cpp
    return chosen->Text;
}

// ---------------------------------------------------------------------------
//  按等级挑物品。挑等级段最匹配的（尽量给好的）。
// ---------------------------------------------------------------------------
CompanionItem const* BotCompanionMgr::PickItem(uint8 careType, uint8 level) const
{
```

`return chosen->Text;` 和 `}` 后面，**空一行，直接就是 `// ----` 那条注释线**。

## 如果不放心手动删，用这条命令（自动备份）

Git Bash 里跑：

```bash
cd /d/TrinityCore/src/server/game/AI/NpcBots
python - << 'PYEOF'
import shutil
f = 'bot_companion.cpp'
shutil.copy(f, f + '.bak')                      # 先备份
lines = open(f, encoding='utf-8', errors='ignore').read().split('\n')

# 安全校验：确认775行确实是孤立的 {，827行确实是 }
assert lines[774].strip() == '{',  f'第775行不是 {{，实际是 {lines[774]!r}，已中止'
assert lines[826].strip() == '}',  f'第827行不是 }}，实际是 {lines[826]!r}，已中止'

del lines[774:827]                              # 删除 775-827（下标 774~826）
open(f, 'w', encoding='utf-8', newline='').write('\n'.join(lines))

d = open(f, encoding='utf-8', errors='ignore').read()
print('删除完成，已备份为 bot_companion.cpp.bak')
print('括号统计: { =', d.count('{'), ' } =', d.count('}'), ' 差值 =', d.count('{') - d.count('}'))
PYEOF
```

> 有 `assert` 双重校验，行号对不上会直接中止不动文件。
> 原文件备份成 `bot_companion.cpp.bak`。

---

# 第2步 · `bot_companion.h` 把 `_giftTexts` 加回去

## 完整替换块（这是你现在的样子）

**找到这三行**：

```cpp
    // A37 第3步：礼物反馈台词
    // G19第3步：botGuid -> (台词id -> 冷却到期时间戳)
    std::unordered_map<uint32, std::unordered_map<uint32, time_t>> _textCooldowns;
```

**整块替换成这四行**：

```cpp
    // A37 第3步：礼物反馈台词
    std::vector<CompanionGiftText> _giftTexts;
    // G19第3步：botGuid -> (台词id -> 冷却到期时间戳)
    std::unordered_map<uint32, std::unordered_map<uint32, time_t>> _textCooldowns;
```

**区别只有一行**：第 2 行插入了 `std::vector<CompanionGiftText> _giftTexts;`

## 改完后，整个 private 段应该和下面一字不差

```cpp
private:
    BotCompanionMgr() { }
    ~BotCompanionMgr() { }
    BotCompanionMgr(BotCompanionMgr const&) = delete;
    BotCompanionMgr& operator=(BotCompanionMgr const&) = delete;

    // careType -> 台词列表
    std::unordered_map<uint8, std::vector<CompanionText>> _texts;
    // careType -> 物品列表
    std::unordered_map<uint8, std::vector<CompanionItem>> _items;
    // botGuid -> 背包
    std::unordered_map<uint32, std::vector<CompanionInvEntry>> _inventories;
    // A37 第3步：礼物反馈台词
    std::vector<CompanionGiftText> _giftTexts;
    // G19第3步：botGuid -> (台词id -> 冷却到期时间戳)
    std::unordered_map<uint32, std::unordered_map<uint32, time_t>> _textCooldowns;
    // A37 第5步：索要台词
    std::vector<CompanionRequestText> _requestTexts;
};

#define sBotCompanionMgr BotCompanionMgr::instance()

#endif // BOT_COMPANION_H_
```

**直接对照着改，改完长得和这个一模一样就对了。**

---

# 第3步 · 编译前跑这一条总自检

Git Bash 里整段复制：

```bash
cd /d/TrinityCore/src/server/game/AI/NpcBots && {
echo "===== 括号平衡（三个都必须是 0）====="
python -c "
for f in ['bot_companion.h','bot_companion.cpp','bot_ai.cpp']:
    d=open(f,encoding='utf-8',errors='ignore').read()
    diff=d.count('{')-d.count('}')
    print(f'  {f:22s} {{={d.count(chr(123)):5d}  }}={d.count(chr(125)):5d}  差值={diff}  {\"OK\" if diff==0 else \"<<<<< 有问题\"}')
"
echo "===== 关键符号 ====="
echo -n "  _giftTexts(须>=1)     : "; grep -c "_giftTexts" bot_companion.h
echo -n "  _textCooldowns(须1)   : "; grep -c "_textCooldowns" bot_companion.h
echo -n "  PickGiftText(须>=1)   : "; grep -c "PickGiftText" bot_companion.h
echo -n "  Scene;(须1)           : "; grep -c "Scene;" bot_companion.h
echo -n "  fields[10](须1)       : "; grep -c "fields\[10\]" bot_companion.cpp
echo "===== PickText 定义必须只有 1 处 ====="
grep -n "BotCompanionMgr::PickText" bot_companion.cpp
echo "===== 旧函数残留必须为 0 ====="
echo -n "  pool[0]->Text 出现次数: "; grep -c "pool\[0\]->Text" bot_companion.cpp
echo "===== GetCurrentScene 必须顶格（须有输出）====="
grep -n "^uint8 bot_ai::GetCurrentScene" bot_ai.cpp
}
```

## 期望结果

```
===== 括号平衡（三个都必须是 0）=====
  bot_companion.h        {=  xx   }=  xx   差值=0  OK
  bot_companion.cpp      {= xxx   }= xxx   差值=0  OK
  bot_ai.cpp             {=xxxx   }=xxxx   差值=0  OK
===== 关键符号 =====
  _giftTexts(须>=1)     : 1
  _textCooldowns(须1)   : 2        <- 1个注释 + 1个成员声明，2 也正常
  PickGiftText(须>=1)   : 1
  Scene;(须1)           : 1
  fields[10](须1)       : 1
===== PickText 定义必须只有 1 处 =====
684:std::string BotCompanionMgr::PickText(uint8 careType, uint8 botClass, uint8 botRace/* = 0*/,
===== 旧函数残留必须为 0 =====
  pool[0]->Text 出现次数: 0        <- 这是旧函数独有写法，必须是 0
===== GetCurrentScene 必须顶格（须有输出）=====
18xxx:uint8 bot_ai::GetCurrentScene() const
```

**只要这几项全对，就可以编译了。**

---

# 第4步 · 编译

**清理解决方案 → 重新生成**（改了头文件结构，必须全量重编）。

配置确认是 **RelWithDebInfo**，不是 Debug。

---

## 附：这次三个根因的最终状态

| 组 | 问题 | 状态 |
|---|---|---|
| 组1 | `bot_companion.h` 丢了 `_giftTexts` | 本文第2步修（加1行） |
| 组2 | `bot_companion.cpp:775-827` 旧函数残留 | 本文第1步修（删53行） |
| 组3 | `GetCurrentScene` 插错位置 | **你已改对** |
| 组4 | LNK1181 | 上面修好自动消失 |
