-- ============================================================
--  step21 世界指令 RBAC 权限
--  71012 = Command: world tools
--
--  注意：你的 SQL 客户端只执行光标所在的那一条语句，
--        这个文件只有一条，直接把光标放上去执行即可。
-- ============================================================
REPLACE INTO auth.rbac_permissions (id, name) VALUES (71012, 'Command: world tools')
