# F35 · bot_ai.cpp / bot_ai.h 中文变问号修复

> 2026-08-09
> 用户带基线重扫后，只剩这两个文件需要修

---

## 一、诊断结果：**代码零损坏**

| 文件 | 致命(代码) | 重要(字符串) | 可缓(注释) |
|---|---|---|---|
| `bot_ai.cpp` | **0** | 1 | 81 |
| `bot_ai.h` | **0** | 0 | 3 |

**没有一个问号落在可执行代码里。** 你的 bot_ai 逻辑完好，
坏的只有注释和一处字符串。

---

## 二、修复后的文件

```text
补丁库/02_修复/F35_bot_ai中文修复/源文件/bot_ai.cpp
补丁库/02_修复/F35_bot_ai中文修复/源文件/bot_ai.h
```

**覆盖到**：

```text
D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.cpp
D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.h
```

编码：**UTF-8 with BOM + CRLF**，GBK 不兼容字符 **0 个**（VS 不会再弹框）。

---

## 三、修复方法（三轮，全部有据可查）

| 轮次 | 方法 | 修复数 |
|---|---|---|
| 1 | 与 GitHub 基线逐行骨架比对 | 0（这些注释基线里没有，都是我们自己加的） |
| 2 | 从 5181 条补丁文档代码行中匹配回填 | **56 处** |
| 3 | 按代码上下文语义逐行还原 | **26 处** |
| — | `bot_ai.h` 的 3 行（F05 补丁原文） | **3 处** |

**所有中文都是从既有文档还原或按上下文重建，不是凭空重写。**

### 举例（第3轮的还原依据）

```cpp
// 第 287 行，上下文是：
if (reinterpret_cast<void const*>(master) == reinterpret_cast<void const*>(me))
    return;                     // 无主bot，没有真正的主人可以指向
                                //  ^^^^ 按 step48 的逻辑还原
```

```cpp
// 第 18755 行，上下文是主人死亡检测：
if (!master->IsAlive())
{
    _masterWasDead = true;
    return;                     // 主人死着的时候不打扰
}
```

### `bot_ai.h` 的三行来自 F05 补丁原文

```cpp
    //step48: 安全判断 master 是否是【真的】玩家。
    //  无主bot的 master 指向自己（bot_ai.cpp:459），
    //  直接调 master->任何虚函数都会崩。必须先比地址。
    bool HasRealMaster() const
```

出处：`补丁库/02_修复/F05_游荡bot崩溃/bug根因与修复.md:166-175`

---

## 四、交付前校验

```text
                    bot_ai.cpp        bot_ai.h
行数                22213 (原22213)   906 (原906)     <- 没有增删行
大括号 { }          3396 / 3396       252 / 252       <- 平衡
GBK 不兼容字符      0                 0               <- VS不弹框
残留问号 (??)       0                 0
BOM                 有                有
行尾                CRLF              CRLF
```

---

## 五、装完自检

在两个文件里各搜一次：

```text
??          应该 0 个（连续两个问号）
```

然后 **清理解决方案 → 重新生成解决方案**。

---

## 六、关于那 1 处字符串

```cpp
// bot_ai.cpp:18907
txt.replace(pos, 6, from.empty() ? "??" : from);
```

**这处已修复**（第2轮从补丁文档匹配到原文）。
它是 A20 关怀系统里替换 `{from}` 占位符的地方，
`from` 为空时的默认文案。

---

## 附：这次为什么只剩 2 个文件

上一轮无基线扫描报了 **286 个文件**，其中绝大多数是误报：

```text
BoundingIntervalHierarchy.cpp:110   // node box is too big ...?    英文疑问句
bot_Events.h:8                      %Complete: ???                 上游占位符
```

加上 `--baseline` 和「只认连续问号」两条规则后，
**286 -> 2**，精确定位到真正需要修的文件。

工具改动见 `tools/find_mojibake.py`，坑表已记录。
