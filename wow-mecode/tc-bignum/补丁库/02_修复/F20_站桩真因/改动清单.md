# F20 改动清单 —— 一行

> 文件：`D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.cpp`
>
> **F19 的三处改动都留着，这次是再加一处。**

---

## 改动：`bot_ai.cpp:4310`

**Ctrl+F 搜**（这个字符串在文件里出现 4 次，**要找带 `/ 2` 的那个**）：

```cpp
    uint8 followdist = IAmFree() ? BotMgr::GetBotFollowDistMax() / 2 : master->GetBotMgr()->GetBotFollowDist();
```

> **怎么确认找对了**：它的下面几行应该有
> `if (IsWanderer() && me->GetMap()->GetEntry()->IsBattlegroundOrArena())`
>
> 另外三处是 `GetBotFollowDistMax()` **不带 `/ 2`**，不要改那些。

**改成**（整行替换）：

```cpp
    uint8 followdist = IAmFree() ? (IsWanderer() ? BotMgr::GetBotFollowDistMax() : BotMgr::GetBotFollowDistMax() / 2) : master->GetBotMgr()->GetBotFollowDist();
```

---

## 贴完自检

```bash
cd /d/TrinityCore/src/server/game/AI/NpcBots

echo "--- 1. 新写法（应输出 1）---"
grep -c 'IsWanderer() ? BotMgr::GetBotFollowDistMax() : BotMgr::GetBotFollowDistMax() / 2' bot_ai.cpp

echo "--- 2. 旧写法残留（应输出 0）---"
grep -c 'IAmFree() ? BotMgr::GetBotFollowDistMax() / 2 :' bot_ai.cpp

echo "--- 3. F19 的三处还在吗（应输出 1 1 1）---"
grep -c '#include "Config.h"' bot_ai.cpp
grep -c 'NpcBot.WanderingBots.PeaceMode' bot_ai.cpp
grep -c 'BotMovement(BOT_MOVE_POINT, target, nullptr, IsWanderer())' bot_ai.cpp
```

**期望：`1 / 0 / 1 1 1`**

---

## 编译

`game` 项目，**生成解决方案**。不用重跑 CMake。

---

## 为什么这次能行

### 原来的死区

```
CanBotAttack      (bot_ai.cpp:3657)  用 100 码  -> "能打"
_getTargets       (bot_ai.cpp:4310)  用  50 码  -> "60码外丢目标"
CalculateAttackPos(bot_ai.cpp:5330)  用 100 码  -> "算走位"

           60 码 ~ 100 码 = 【锁定但不追】的死区
```

### 改完之后

```
_getTargets 对 wanderer 也用 100 码 -> 阈值 110 码
三处对齐，死区消失
```

### 上游硬上限

`botcommon.h:45  BOT_MAX_CHASE_RANGE = 120`

`bot_ai.cpp:4341` 还有一道 `me->GetDistance(mytar) < BOT_MAX_CHASE_RANGE`。

**110 < 120，卡在允许范围内，不会打架。**

---

## 验证

```
1. 找一个中立生物（30-100 码距离）
2. 观察游荡bot
3. 【应该】直接走过去打，不再站着不动
```

**如果还不动**，说明距离超过 110 码，或者是别的原因（比如目标在室内 LOS 完全断），
把当时的距离和场景告诉我。

---

## 已验证

| 项 | 结果 |
|---|---|
| g++ 真编译 | **0 错误 0 警告** |
| 逻辑验证 | wanderer 得到 100（阈值110码），非wanderer 仍是 50 |
| `botmgr.h:210 GetBotFollowDistMax()` | 恒定返回 100 |
| `botcommon.h:45 BOT_MAX_CHASE_RANGE` | 120（110 未越界） |
| 该字符串在文件中出现次数 | 4 次，只有 `:4310` 带 `/ 2` |
