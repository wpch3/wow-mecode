# G17-B1R4 室内实时安全清理修复

状态：`B1R3_MOUNT_CONTROL_RUNTIME_PASS / INDOOR_CLEANUP_RUNTIME_FAIL / ROOT_CAUSE_PROVEN / B1R4_FIX_IMPLEMENTED / GCC14_PASS / WINDOWS_PENDING`

用户已确认地面坐骑与飞行坐骑接管、起飞和降落均正常，证明B1R3权威座位修复真实有效。当前剩余B1安全门失败是进入室内后Vehicle不解除。

精确根因：B1此前只在`PlayerScript::OnUpdateZone`调用`IsBlockedArea()`。精确上游`Player.cpp`显示，同一zone内只有area变化时只调用`UpdateArea()`；VMap indoor/outdoor状态变化更不保证zone变化。`OnUpdateZone`因此不会在普通建筑入口稳定触发，已有的室内判断本身正确但没有执行机会。

B1R4在活动Vehicle AI中加入250ms有界安全检查，连续复用同一服务器权威`IsBlockedArea()`策略。命中室内、城市、禁飞区、副本或战场后先设置单次清理锁，再执行带坠落保护的`CleanupPlayer()`。原`OnUpdateZone`仍保留为快速防线。

前像：`94ff80334783e8883f0811a1a7f76595d91b729cc43684f00273abb9d955628b`

后像：`e9418704731a2d9cd5119cc2024079a2326802796d00bf24e88928dd17ea7059`

本批不修改第三技能、爬升、着陆或B2移动。用户报告的“三技能方向反向、结束后空中行走姿态、不能继续升降”已登记，按用户要求留到完整移动技能优化时统一修复。

Windows包`G17B1R4_Continuous_Indoor_Safety_Windows_20260823.zip`已完成全部离线与解压门；30086字节，SHA-256=`d037912734a1bdcc1d6dc0a6a0bfc0e3668df6361b30ee6ee587fbe32ae85cd1`。

本批不改SQL、不改客户端、不改R5。Windows构建与室内Runtime仍待用户，不得提前标PASS。
