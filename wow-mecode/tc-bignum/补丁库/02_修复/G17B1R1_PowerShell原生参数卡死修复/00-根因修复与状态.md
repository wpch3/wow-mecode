# G17-B1R1 PowerShell原生参数卡死修复

状态：`ROOT_CAUSE_PROVEN / SOURCE_EDITS_ZERO / FIX_OFFLINE_VALIDATED / REAL_WINDOWS_RERUN_PENDING`

旧结果停在`PYTHON=...Python312\python.exe`，没有NATIVE_SELFTEST或SOURCE_APPLY。源码SHA仍是R1前像`10a7002...9b2f45`，故旧卡死没有源码、构建、SQL或客户端写入。

根因是PowerShell函数形参`$Args`与大小写不敏感的自动变量`$args`冲突。真实PowerShell探针确认：调用旧函数并传入三个原生参数后，函数内`$Args.Count=0`；`cmd.exe`可因此无`/d /c`进入交互等待。

R1安装器与回滚器均改用`$NativeArgs`和全显式`-FilePath/-NativeArgs/-Prefix`绑定，保留Windows PowerShell 5.1 native stderr安全捕获。

离线门槛已通过：双PS AST、真实native stdout/stderr/exit、含空格和引号参数、B1 8项测试、source Apply/幂等/Rollback、包级、SHA、确定性ZIP和解压复验。

唯一新ZIP：`G17B1R1_Native_Runner_Fix_Windows_20260823.zip`，32052字节，15文件，SHA-256=`c94394177b188843c7ed79a989fe46f7397233bf9103d2fc622ecf79f4c77cc8`。

用户先关闭旧窗口，无需回滚；旧B1包禁止重复。只运行新`01_Install_Build_G17B1R1.cmd`。真实Windows构建和B1 Runtime仍为PENDING。
