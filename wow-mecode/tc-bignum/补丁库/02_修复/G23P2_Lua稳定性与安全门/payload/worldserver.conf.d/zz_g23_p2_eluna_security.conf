# G23-P2 Eluna reload security override
# Loaded after the main worldserver.conf by the existing worldserver.conf.d chain.
# 0 allowed ordinary players to invoke reload eluna; 3 restricts it to administrators.
Eluna.ReloadSecurityLevel = 3
