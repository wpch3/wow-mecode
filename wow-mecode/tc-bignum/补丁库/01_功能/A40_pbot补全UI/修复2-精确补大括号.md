精确修复：在第 209 行补一个 }

================================================================
 已确认：就差一个 }
================================================================

你贴的 161-236 行里，TryAcceptGroup 的结尾是这样：

    (你文件的第 209 行)
        "|cff00ff00[%s]|r 接受了你的组队邀请。", bot->GetName().c_str());
    (第 210 行)  空行
    (第 211 行)  // ------------------------------------------------------------------
    (第 212 行)  //  公会邀请
    (第 213 行)  // ------------------------------------------------------------------
    ...
    (第 216 行)  static void TryAcceptGuild(Player* bot)

【第 209 行和第 210 行之间少了 }】

================================================================
 修法：一行搞定
================================================================

文件 D:\TrinityCore\src\server\scripts\Commands\pbot_autoaccept.cpp

Ctrl+G 跳到 209 行。

搜索这一整行（全文件唯一）：

        "|cff00ff00[%s]|r 接受了你的组队邀请。", bot->GetName().c_str());

在它【下面】新起一行，输入一个：

}

================================================================
 改完应该长这样
================================================================

    if (leader && leader->GetSession())
        ChatHandler(leader->GetSession()).PSendSysMessage(
            "|cff00ff00[%s]|r 接受了你的组队邀请。", bot->GetName().c_str());
}

// ------------------------------------------------------------------
//  公会邀请
// ------------------------------------------------------------------
static void TryAcceptGuild(Player* bot)
{

【那个 } 必须单独一行、顶格不缩进】

================================================================
 顺便修一下被破坏的缩进（可选，不影响编译）
================================================================

你的文件里有几行缩进丢了，虽然能编译，但容易看错。

第 196 行附近：

    现在是：
        if (!group->AddMember(bot))                 // Group.h:209 public
        return;                                      <<< 少了缩进

    应该是：
        if (!group->AddMember(bot))                 // Group.h:209 public
            return;

第 204-205 行：

    现在是：
        if (leader && group->IsLeader(bot->GetGUID()) && leader != bot)
        group->ChangeLeader(leader->GetGUID());      <<< 少了缩进

    应该是：
        if (leader && group->IsLeader(bot->GetGUID()) && leader != bot)
            group->ChangeLeader(leader->GetGUID());

第 207-208 行：

    现在是：
        if (leader && leader->GetSession())
        ChatHandler(leader->GetSession()).PSendSysMessage(    <<< 少了缩进

    应该是：
        if (leader && leader->GetSession())
            ChatHandler(leader->GetSession()).PSendSysMessage(

第 200-203 行的注释也丢了缩进：

    现在是顶格的：
    // A40: bot 不该当队长。
    // Player.cpp:24268 规定队长不能被踢...

    应该缩进 4 格：
        // A40: bot 不该当队长。
        // Player.cpp:24268 规定队长不能被踢...

这些【不影响编译】，但建议顺手改好，
因为顶格的注释看起来像"函数外面"，容易误判结构。

================================================================
 整段正确版（想省事就整段替换）
================================================================

如果懒得一处处改，直接把你文件里
从「    // GroupHandler.cpp:265」到「static void TryAcceptGuild」之前
的所有内容，替换成下面这段：

    // GroupHandler.cpp:265
    if (!group->AddMember(bot))                 // Group.h:209 public
        return;

    group->BroadcastGroupUpdate();              // Group.h:350 public

    // A40: bot 不该当队长。
    // Player.cpp:24268 规定队长不能被踢，bot 当了队长你就没法用右键菜单
    // 把它移出队伍（.pbot kick 能绕过，但那不算"完整UI"）。
    // 顺带：bot 当队长还会导致改不了副本难度、转不了团队。
    if (leader && group->IsLeader(bot->GetGUID()) && leader != bot)
        group->ChangeLeader(leader->GetGUID());

    // 给邀请人一个反馈，让他知道bot接了
    if (leader && leader->GetSession())
        ChatHandler(leader->GetSession()).PSendSysMessage(
            "|cff00ff00[%s]|r 接受了你的组队邀请。", bot->GetName().c_str());
}

<<< 最后这个 } 是 TryAcceptGroup 的函数结尾，千万别漏

================================================================
 改完自检
================================================================

VS 里按 Ctrl+M 然后 Ctrl+O（折叠所有）。

应该看到所有函数【顶格平级排列】：

    static void TryAcceptGroup(Player* bot) {...}
    static void TryAcceptGuild(Player* bot) {...}
    static void TryAcceptDuel(Player* bot) {...}
    ...

如果 TryAcceptGuild 及后面的函数【有缩进】，
说明它们还在 TryAcceptGroup 的大括号里面，} 没补对。

================================================================
 已验证
================================================================

g++ 双向复现：

    故意漏一个 }  -> a function-definition is not allowed here
                     （就是 MSVC 的 C2601）
    补回去        -> 全部错误消失

你那 20 多条错误会一次性全部解决。
