# G17-B0原生御龙载具：开发状态与设计

更新时间：2026-08-23

状态：`G17R1_VEHICLE_RUNTIME_PASS / G17R2_WINDOWS_PASS_USER_CONFIRMED / PURE_FLIGHT_59961_RUNTIME_FAIL_NOT_HERE / G17R2A_DIAGNOSTIC_NEXT / B1-B6_NOT_IMPLEMENTED`

## 已完成的草案实现

- 新增单一`cs_dragonriding.cpp`，同一`AddSC_dragonriding_commandscript()`注册CommandScript、PlayerScript、VehicleAI和4个SpellScript；
- `.dragon summon/dismiss/status/help`；
- 复用原生Ruby Drake的模型、非零`VehicleId`和可控座位，world SQL用临时表按当前schema克隆，拒绝覆盖外来entry `1000171`；
- 载具原生独立技能条：Flame Breath 9573、Burst of Speed 55215、Jump Jets 52197、Parachute 53208；
- energy 100，龙息/加速/爬升分别消耗20/30/25；
- 爬升由SpellScript仅对自定义龙拦截默认Jump效果并调用`MoveTakeoff`；着陆用`MoveLand`，到地后自动离车；
- 死亡、主动离车、下线、释放、换图、进入城市/室内/禁飞Area/副本/BG/竞技场都进入慢落和清理路径；
- SQL提供install/check/rollback，回滚只删除ScriptName确认属于G17-B0的自定义数据。

## 已自动验证

- 在上游`NPCBOT-Eluna-zhCN-2026`浅克隆HEAD `4e8762ee2b00948fa103d0cd1afd78ccdf4364fb`上，把草案源文件加入Commands并注册loader；
- 使用GCC 14/C++20和完整工程头文件执行单翻译单元`-fsyntax-only`，结果PASS；
- 静态合同测试覆盖4技能、三项扣能、VehicleAI、生命周期、禁区、SQL碰撞门、精确动作条映射和owned-only回滚；
- 2026-08-22自检发现首版爬升只做25 Energy预检但没有成功施法后扣能，已补`AfterCast`扣能并加入三技能扣能计数回归；
- 同轮将entry碰撞门改为NULL-safe，并让install/check同时验证非零VehicleId、四槽精确映射与飞行移动行；
- 使用用户探针捕获的`CombatAI.h/ScriptMgr.h/Unit.h/Map.h`覆盖参考头文件再次执行翻译单元语法检查，PASS；
- 真实loader结构安装器本地Check→Apply→Check→Rollback→Check及两个负例PASS，预检Apply锁定负例PASS。

这不等于用户Windows源码编译PASS，也不等于游戏运行PASS。

## Windows真实前像已锁定

用户已通过GitHub提交`cc3c2194ea3e910a95129941a4a8f0c15cf4fb90`回传完整结果。原ZIP为73265字节、SHA-256 `ece3d768d3d46f7e573a76ac0527d439ab932c0b831ed923f3de6c600ddaee0c`，CRC/路径安全PASS。

结果确认loader为PRE、新cpp缺席、wp声明/调用唯一、7个API/CMake/schema上下文可锁定。真实loader前像已经原样收入`original/windows_preimage_20260822/`，结构后像、安装器、回滚和world只读探针均已生成；不会修改`ScriptLoader.h`或`CMakeLists.txt`。

## Windows真实源码预检已通过

用户通过GitHub提交`db131492aef1fde77f731c08fe046205071b3b16`回传结果；1914字节，SHA-256 `724d58ee39a09767fb0ccb054b4b23c906d73e9e2ef499a61cf66ea3f78314b0`。结果确认SelfTest、2个负例和真实源码只读Check均PASS，最终`G17B0_SOURCE_STATE=READY_TO_APPLY`、`G17B0_CHECK_SOURCE_EDITS=0`，SelfTest/Check rc均为0。

这个CMD已经关闭，禁止要求用户重复。

## live world v2已经通过

用户通过GitHub提交`abd3c2179136f19231112d6177f0f42c2d1e6285`回传50行结果；原件17472字节、SHA-256 `af4a6031f4895767c34faf1211bb5ba8d026039beefa0d8cd79b52fe5a8a470e`。离线锁定验收PASS：

```text
DATABASE=world
DATABASE_VERSION=8.0.46
SOURCE_ENTRY=27756
SOURCE_VEHICLE_ID=70
TARGET_1000171_ROWS=0
CANDIDATE_SCRIPT_BINDINGS=0
G17B0_DB_PREIMAGE_READY=True
G17B0_DB_PROBE_ACCEPTANCE=PASS
```

v1的1046和1271均关闭，DB探针禁止重复。world表无候选entry/ScriptName冲突；基础DBC技能实际施放仍留给编译后Runtime，不在此冒充PASS。

## Windows真实Source Apply已经通过

用户附件1668字节、SHA-256 `772f4007cd2199ec6829bbb2bdef3e11a976c072c2c1ad7ae6b31210d3bc5ca4`。真实PRE→POST写入2项：loader为`5502e5b4...5cc`，payload为`c9535dca...99bd`，loader备份存在且7上下文写后不变；最终`ALREADY_APPLIED`、wrapper PASS。Source Apply禁止重复。

## world v2错误关闭与v3真实PASS

旧v2 01真实返回`Illegal mix of collations (utf8mb4_unicode_ci,IMPLICIT) and (utf8mb4_0900_ai_ci,IMPLICIT)`。根因是world文本列与连接用户变量的同级IMPLICIT比较；首个风险点在`START TRANSACTION`和永久DML前。v2关闭并禁止重跑，先在原连接执行`ROLLBACK;`。

```text
G17B0_World_Install_v3_20260822.zip
size=11819
sha256=a0625d4d0c3098ab88d1ff3e9380fbc80bc504771a4a55f7b2776bc80d2fd254
```

v3真实02结果492字节、SHA-256 `182a1878251ea7d4cb6af76a4721028a57750f7de47acb1eff6793a31eaef32a`；唯一数据行为`G17B0_WORLD_CHECK_PASS`，world/source/target/actions/movement/scripts精确为`world,1/1,1/1,4/4,1,4/4`。不可变验收器PASS，world阶段正式关闭且禁止重复。

## 构建v1/v2工具错误与当前NativeSafe v3

v1在PS1加载阶段ParserError，未执行。v2真实日志1430字节、SHA `49dfcfe0...c567`：源码门PASS且CMake开始，但没有`CMAKE_EXIT`、vcxproj成员门或MSBuild开始。全局Stop策略把native stderr包装成ErrorRecord并提前catch；因此MSBuild没有运行，真实Build仍为`NOT_RUN`。v1/v2禁止重跑。

```text
G17B0_Windows_Build_v3_ASCII_NativeSafe_20260822.zip
size=9665
sha256=114054be599fcaec840b7fc9798d020f5e58cd5b7b11bcbf06a509498eacf946
```

v3真实结果6396字节、SHA `58c40872...952e`；native自测、CMake Exit0、vcxproj命中1、MSBuild Exit0、fresh obj 1、新EXE/PDB全部通过。新worldserver.exe为36759040字节、SHA `59491e97...5881`。不可变验收器输出`G17B0_WINDOWS_BUILD_V3_ACCEPTANCE=PASS`，构建阶段关闭。

该v3中的唯一C4018告警位于energy正数cost的有符号/无符号比较；它不影响旧v3链接。G17-R1已修复并完成新的Windows构建，真实结果`DRAGONRIDING_C4018_HITS=0`，因此该告警在当前R1二进制中已关闭。

## Runtime真实失败与G17-R1修复

用户已执行最小Runtime：`.dragon help`和`.dragon status`正常，status为`INACTIVE area_allowed=true`；`.dragon summon`回报“进入可控座位失败”。完整worldserver日志没有命令期G17/Vehicle细节，因此旧Runtime判定为`FAIL_ENTER_CONTROLLABLE_SEAT`，不再是NOT_RUN。

源码复核已锁定根因：`Unit::EnterVehicle()`经46598光环进入`Vehicle::AddPassenger()`后，会把`VehicleJoinEvent`异步加入玩家事件队列；旧G17-B0在事件运行前同步检查`GetVehicleBase()`并立刻销毁龙，所以形成确定性的假失败。独立G17-R1已完成源码修正：按实际`VEHICLE_SEAT_FLAG_CAN_CONTROL`选座、250ms延迟验证车辆/座位/charm控制权、补齐INFO/ERROR座位布局和结果日志，并修复C4018。新源码SHA为`10a7002...9b2f45`；用户真实Windows Apply、CMake、vcxproj成员、MSBuild和fresh OBJ均PASS，`worldserver.exe`由`59491e97...b5881`变为`e74d9304...08148`，C4018命中0。此构建阶段已关闭，禁止重复；后续用户已确认`.dragon summon`和4技能Runtime PASS。

同轮确认始祖幼龙等纯飞行坐骑还受3.3.5客户端`Spell.dbc`的`AttributesExD 0x04000000`本地施法门约束；G17-A服务端放行不能让客户端必然发送这类直接坐骑施法。G17-R1提供客户端专用窄范围补丁器，只对Mounted/Fly/Mounted Flight Speed行清除客户端该bit，服务器DBC保持原样继续执行G17-A安全边界。用户真实build-12340 zhCN DBC已精确修改110行并命中10个标准始祖幼龙法术，verify PASS且服务器DBC未改；当时输出只在staging。后续未提供客户端安装结果文件，因此当前实际安装状态只能写UNKNOWN，不能断言尚未安装或已经安装。

服务端修复包：`tc-bignum/规划/G17_飞行与移动/G17R1_Runtime_Fix_Windows_20260822.zip`，31645字节，SHA-256 `f4eb4e20dc4b65eba29f2784df8eedc000733dca9bdd52a1c9c09c1806de4d8c`，已在用户Windows构建PASS且禁止重复。客户端真实MPQ安装包为`G17R1_Client_MPQ_Install_Windows_20260822.zip`，454641字节，SHA-256 `f59513e9...0e5ec`；它只接受空闲字母槽并可精确回滚。用户尚未回传`G17R1_CLIENT_MPQ_INSTALL_RESULT.txt`，因此客户端安装门保持UNKNOWN，不能从车辆Runtime反推已安装。

## 2026-08-23真实Runtime与G17-R2

用户已确认`.dragon summon`正常，4个载具技能全部可以使用。因此G17-R1的异步入座、可控座位、charm控制权、原生车辆动作条和四技能链正式记为Runtime PASS。体验结论不是“御龙完成”：当前飞行过慢，没有分档、俯冲蓄能、惯性、滑翔或极速反馈，移动和攻击技能还混在同一页。

红色始祖幼龙59961在湿地安全户外仍真实返回“这里无法召唤坐骑”。源码复核定位到`SpellInfo::CheckLocation()`严格分支：已计算`g17OldWorldAllowed=true`，却仍无条件调用`player->CanFlyInZone()`；旧世界返回false并抵消G17放行。非严格分支已正确使用G17策略，二者不一致。

独立G17-R2只修复这个服务端严格门：当G17旧世界安全策略成立时，同时替代原版`IsFlyable()`与`CanFlyInZone()`两个谓词；G17关闭、外域/诺森德及室内、城市、禁飞、竞技场、实例、黑名单边界不变。前镜像SHA为`537e5c35...e50755`，后镜像SHA为`73d52ac0...cb9e2`，本地10项自动测试与C++17策略编译PASS。用户现已确认`G17R2_WINDOWS_BUILD_RESULT=PASS`，但59961在湿地仍失败，新提示为“无法在这里使用”。由于未回报R2日志marker且客户端MPQ安装报告一直缺失，当前不能把新提示武断归因于同一严格门；先执行R2A只读分流，再决定客户端加载修复或R3服务端后续门。

## 通用御龙与战斗坐骑后续范围

已锁定`G17_通用御龙与战斗坐骑总设计_20260823.md`：所有坐骑（包括地面坐骑）自动接入、模型/类型识别与覆盖、服务器权威会话、5档速度和1200%极速、动量/俯冲/拉升/滑翔/失速/安全着陆、玩家/移动/攻击独立技能页、猛兽/龙类/魔法/机械/通用战斗体系、骑乘移动读条与脱战吃喝、骑乘/战斗/自动寻路/固定不消失模式、伤害信用、PvE/PvP倍率、网络节流和异常恢复。

实施顺序固定为R2→B1全坐骑接管→B2动量高速→B3独立战斗页→B4玩家骑乘施法→B5自动寻路/固定→B6客户端体验和压力加固。不能把全部功能塞进当前R2。

## 明确不声称

- G17-R1车辆入座、控制权、动作条与4技能Runtime已PASS；这只证明载具骨架，不证明完整御龙体验；
- G17-R1客户端MPQ实际安装仍UNKNOWN；R2已修的strict根因真实存在，但新“无法在这里使用”是否来自客户端或后续服务端门仍待R2A；
- G17-R2 Windows包装器结果已由用户确认PASS，但59961召唤/起飞/水平移动/降落Runtime仍FAIL；不能把Build PASS扩写成功能PASS；
- 全坐骑接管、分档1200%高速、动量/重力/滑翔、独立攻击页、玩家骑乘施法、自动寻路和固定模式均未实现；
- 任何B1-B6功能在源码、Windows构建与Runtime证据齐全前不得标记完成。
