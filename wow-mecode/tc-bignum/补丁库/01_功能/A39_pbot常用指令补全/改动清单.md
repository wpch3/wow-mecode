# A39 `.pbot` 常用指令补全

上一轮我在 F25 文档里**只列了清单没写代码**，是我的疏忽。这里补上完整实现。

新增 4 个指令：

| 指令 | 作用 |
|---|---|
| `.pbot kick [角色名]` | 把 pbot 移出队伍（自动处理"队长踢不掉"） |
| `.pbot invite <角色名\|all>` | 主动把 pbot 拉进你的队伍 |
| `.pbot follow <角色名\|all>` | 让它跟着你走 |
| `.pbot stay <角色名\|all>` | 让它停在原地 |

---

## 前置：先修 C2027

如果你已经装了 F25 的 `.pbot kick`，**必须先加这行 include**，
否则本文档的代码也会报同样的错。

文件：`D:\TrinityCore\src\server\scripts\Commands\cs_playerbot.cpp`

搜索原文（约 51 行）：

```cpp
#include "Map.h"                // Map::IsDungeon / IsBattlegroundOrArena / GetInstanceId
```

替换为：

```cpp
#include "Group.h"              // Group::IsMember / IsLeader / ChangeLeader / AddMember
#include "Map.h"                // Map::IsDungeon / IsBattlegroundOrArena / GetInstanceId
#include "MotionMaster.h"       // MoveFollow / MoveIdle
```

### 为什么需要

```text
Player.h:64  class Group;      <- 只有【前向声明】
Player.h     没有 include "Group.h"（已实查）
```

前向声明只能声明指针，**不能调用成员函数** -> C2027。

---

## 改动：在 accept 分支后面加 4 个新分支

文件同上。

搜索原文（约 794 行）：

```cpp
        if (s0 == "accept" || tok[0] == "接受")
```

找到**这个 if 代码块的闭合 `}`**，在它**后面**整段插入下面全部内容。

```cpp
        // ---------- kick 踢出队伍 ----------
        if (s0 == "kick" || tok[0] == "踢出" || tok[0] == "移出")
        {
            Group* grp = player->GetGroup();
            if (!grp)
            {
                handler->SendSysMessage("|cffff0000 你不在队伍里|r");
                handler->SetSentErrorMessage(true);
                return false;
            }

            std::vector<ObjectGuid> toKick;
            for (PBotEntry const& e : g_pbots)
            {
                if (!grp->IsMember(e.CharGuid))
                    continue;
                if (tok.size() >= 2 && Lower(e.CharName) != Lower(tok[1]))
                    continue;
                toKick.push_back(e.CharGuid);
            }

            if (toKick.empty())
            {
                handler->SendSysMessage("|cffff0000 队伍里没有匹配的 pbot|r");
                handler->SetSentErrorMessage(true);
                return false;
            }

            uint32 kicked = 0;
            for (ObjectGuid kguid : toKick)
            {
                if (grp->IsLeader(kguid))
                {
                    grp->ChangeLeader(player->GetGUID());
                    handler->SendSysMessage("|cffffff00 bot 是队长，已把队长转给你|r");
                }
                Player::RemoveFromGroup(grp, kguid, GROUP_REMOVEMETHOD_KICK, player->GetGUID(), "pbot kick");
                ++kicked;
            }

            snprintf(buf, sizeof(buf), "|cff00ff00 已把 %u 个 pbot 移出队伍|r", kicked);
            handler->SendSysMessage(buf);
            return true;
        }

        // ---------- invite 拉进队伍 ----------
        if (s0 == "invite" || tok[0] == "邀请" || tok[0] == "拉人")
        {
            if (tok.size() < 2)
            {
                handler->SendSysMessage("|cffff0000 用法: .pbot invite <角色名|all>|r");
                handler->SetSentErrorMessage(true);
                return false;
            }

            bool wantAll = (Lower(tok[1]) == "all" || tok[1] == "全部");
            uint32 added = 0;
            uint32 failed = 0;

            for (PBotEntry const& e : g_pbots)
            {
                if (!wantAll && Lower(e.CharName) != Lower(tok[1]))
                    continue;

                Player* p = ObjectAccessor::FindPlayer(e.CharGuid);
                if (!p || !p->IsInWorld())
                {
                    ++failed;
                    continue;
                }
                if (p->GetGroup())
                {
                    ++failed;
                    continue;
                }

                Group* grp = player->GetGroup();
                if (!grp)
                {
                    grp = new Group();
                    if (!grp->Create(player))
                    {
                        delete grp;
                        handler->SendSysMessage("|cffff0000 创建队伍失败|r");
                        handler->SetSentErrorMessage(true);
                        return false;
                    }
                    sGroupMgr->AddGroup(grp);
                }

                if (grp->IsFull())
                {
                    handler->SendSysMessage("|cffff0000 队伍满了|r");
                    break;
                }

                if (grp->AddMember(p))
                    ++added;
                else
                    ++failed;
            }

            snprintf(buf, sizeof(buf), "|cff00ff00 拉进队伍 %u 个|r|cffff0000  失败 %u 个|r", added, failed);
            handler->SendSysMessage(buf);
            return true;
        }

        // ---------- follow 跟随 ----------
        if (s0 == "follow" || tok[0] == "跟随" || tok[0] == "跟着")
        {
            if (tok.size() < 2)
            {
                handler->SendSysMessage("|cffff0000 用法: .pbot follow <角色名|all>|r");
                handler->SetSentErrorMessage(true);
                return false;
            }

            bool wantAll = (Lower(tok[1]) == "all" || tok[1] == "全部");
            uint32 cnt = 0;

            for (PBotEntry const& e : g_pbots)
            {
                if (!wantAll && Lower(e.CharName) != Lower(tok[1]))
                    continue;

                Player* p = ObjectAccessor::FindPlayer(e.CharGuid);
                if (!p || !p->IsInWorld())
                    continue;
                if (p->GetMapId() != player->GetMapId())
                    continue;

                p->GetMotionMaster()->Clear();
                p->GetMotionMaster()->MoveFollow(player, 2.0f, float(M_PI));
                ++cnt;
            }

            snprintf(buf, sizeof(buf), "|cff00ff00 %u 个 pbot 开始跟随|r", cnt);
            handler->SendSysMessage(buf);
            return true;
        }

        // ---------- stay 原地待命 ----------
        if (s0 == "stay" || tok[0] == "待命" || tok[0] == "原地")
        {
            if (tok.size() < 2)
            {
                handler->SendSysMessage("|cffff0000 用法: .pbot stay <角色名|all>|r");
                handler->SetSentErrorMessage(true);
                return false;
            }

            bool wantAll = (Lower(tok[1]) == "all" || tok[1] == "全部");
            uint32 cnt = 0;

            for (PBotEntry const& e : g_pbots)
            {
                if (!wantAll && Lower(e.CharName) != Lower(tok[1]))
                    continue;

                Player* p = ObjectAccessor::FindPlayer(e.CharGuid);
                if (!p || !p->IsInWorld())
                    continue;

                p->GetMotionMaster()->Clear();
                p->GetMotionMaster()->MoveIdle();
                ++cnt;
            }

            snprintf(buf, sizeof(buf), "|cff00ff00 %u 个 pbot 原地待命|r", cnt);
            handler->SendSysMessage(buf);
            return true;
        }
```

---

## 还要加一个 include（invite 用到 GroupMgr）

搜索原文：

```cpp
#include "Group.h"              // Group::IsMember / IsLeader / ChangeLeader / AddMember
```

替换为：

```cpp
#include "Group.h"              // Group::IsMember / IsLeader / ChangeLeader / AddMember
#include "GroupMgr.h"           // sGroupMgr->AddGroup (invite 建新队伍时用)
```

---

## 更新帮助文本

搜索原文：

```cpp
        handler->SendSysMessage("  .pbot accept <角色名|all>       立刻处理挂起的邀请");
```

替换为：

```cpp
        handler->SendSysMessage("  .pbot accept <角色名|all>       立刻处理挂起的邀请");
        handler->SendSysMessage("|cff00ff00--- 队伍与移动（A39）---|r");
        handler->SendSysMessage("  .pbot invite <角色名|all>       把它拉进你的队伍");
        handler->SendSysMessage("  .pbot kick [角色名]             把它移出队伍（不填=全部）");
        handler->SendSysMessage("  .pbot follow <角色名|all>       让它跟着你走");
        handler->SendSysMessage("  .pbot stay <角色名|all>         让它原地待命");
```

---

## 用法

```text
.pbot invite all          把所有已上线的 pbot 拉进队伍
.pbot invite 鲤鱼         只拉这一个
.pbot follow all          全部跟着你走
.pbot stay all            全部停下
.pbot kick                踢出队伍里所有 pbot
.pbot kick 鲤鱼           只踢这一个

中文别名：
.pbot 邀请 all  /  .pbot 踢出  /  .pbot 跟随 all  /  .pbot 待命 all
```

---

## 已实查的 API

```text
Group.h:185   public 段起点
Group.h:192   bool Create(Player* leader)                        public
Group.h:205   bool AddInvite(Player* player)                     public
Group.h:209   bool AddMember(Player* player)                     public
Group.h:210   bool RemoveMember(ObjectGuid, RemoveMethod const&, ObjectGuid, char const*)  public
Group.h:211   void ChangeLeader(ObjectGuid guid)                 public
Group.h:228   ObjectGuid GetLeaderGUID() const                   public
Group.h:240   bool IsLeader(ObjectGuid guid) const               public

Player.h:1593 static void RemoveFromGroup(Group*, ObjectGuid, RemoveMethod, ObjectGuid, char const*)  public
Player.h:64   class Group;   <- 只有前向声明，所以必须 include Group.h

MotionMaster.h:153  void MoveIdle()                              public
MotionMaster.h:156  void MoveFollow(Unit* target, float dist, ChaseAngle angle, MovementSlot slot = MOTION_SLOT_ACTIVE)  public

MovementDefines.h:99-101
    struct ChaseAngle { ChaseAngle(float angle, float _tolerance = M_PI_4); ... };
    <- 单参构造，所以 MoveFollow(player, 2.0f, float(M_PI)) 能隐式转换

SharedDefines.h:3731-3736  enum RemoveMethod : uint8
    GROUP_REMOVEMETHOD_KICK = 1
    <- 在 shared 目录，cs_playerbot.cpp 已 include Common.h 能间接拿到

Position.h:193  uint32 GetMapId() const                          public
```

---

## 已做的验证

```text
g++ -std=c++20 -fsyntax-only -Wall -Wextra   通过
g++ -std=c++20 -o t && ./t                    通过

3 项断言：
  kick   -> 踢掉2个 + 队长自动转移 + 队伍只剩自己
  follow -> MotionMaster 进入跟随模式
  stay   -> MotionMaster 进入 Idle 模式
```

---

## 设计说明

### 为什么 `invite` 用 `AddMember` 而不是 `AddInvite`

`AddInvite` 是**发出邀请**，需要对方点"接受"。
pbot 有 A27 的自动接受，理论上可以走这条路，但：

- 要等下一个 1 秒轮询，慢
- 依赖 `PBOT_AUTO_GROUP` 开关，关了就不生效

`AddMember` 是**直接加入**，立刻生效，不依赖任何开关。
GM 命令直接拉人是合理的。

### 为什么 `follow` 的角度用 `M_PI`

`ChaseAngle` 的 `RelativeAngle`：`0` = 正前方，`M_PI` = 正后方。
跟随应该在**你身后**，所以用 `M_PI`。

`MovementDefines.h:103` 的注释原文：

```text
float RelativeAngle; // we want to be at this angle relative to the target (0 = front, M_PI = back)
```

### `follow` 为什么要判同地图

`MoveFollow` 跨地图会出问题（目标不在同一个 Map 里，寻路直接失败）。
所以先 `GetMapId()` 比对，不同就跳过。想跨地图先用 `.pbot come`。

### `kick` 不填角色名 = 全部

和其它指令不一样（它们要求必须填），因为踢人最常见的场景就是"全清"。
填了名字就只踢那一个。

---

## 已知限制

- `follow` 是**一次性指令**，bot 走到你身边后如果你跑远了它会继续跟，
  但重新登录 / 传送后会失效，要再打一次
- `stay` 只是停止移动，**不会让 bot 停止战斗**
- 真正的"跟随AI"（自动战斗、自动喝药）属于 PlayerBot 战斗AI 范畴，
  是另一个大工程（`未完成想法-总清单.md` 里的长线项）
