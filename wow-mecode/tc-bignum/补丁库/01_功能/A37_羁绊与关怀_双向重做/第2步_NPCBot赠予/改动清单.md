# A37 第2步：NPCBot 赠予通道

按你之前确认的三条：**混合处置**（能用的当场用 / 用不了的存仓库）+ **要拒绝机制** + **先做 NPCBot**。

**共 5 处改动，全部代码块带围栏，可直接复制。**

---

## 这一步做完你能做什么

```text
右键bot -> [赠予物品]
             |
             +-- 列出背包里所有可赠予的物品（宽松过滤）
             +-- 点一个 -> 物品真实转移给bot
             +-- bot 说一句话反馈
             +-- 不合适的物品 bot 会【拒绝】并说明理由
```

物品去向：**存进 bot 银行**（`characters_npcbot_gear_storage`，玩家可取回）。
第3步再加「食物当场吃掉」的分支。

---

## 动手前的 6 项实查（已全部完成）

| # | 查什么 | 结论 |
|---|---|---|
| 1 | 银行上限 | `botdatamgr.cpp:3987` `BotCfg::GetGearBankCapacity()`，返回 0 = 无限 |
| 2 | 要不要改上游 | 要，但只在 `bot_ai.cpp` 加分支，不动现有逻辑 |
| 3 | **物品转移正确姿势** | `bot_ai.cpp:9513-9514` **先 Deposit 再 MoveItemFromInventory**，顺序不能反 |
| 4 | 落库时机 | `Player.cpp:19592` `Player::SaveToDB` 里自动调 `SaveNpcBotStoredGear` |
| 5 | PlayerBot 钩子 | 第6步再用，本步不涉及 |
| 6 | 装备耐久 | 第5步再用，本步不涉及 |

### 实查中抓到的两个坑（差点写错）

```text
1. ITEM_SUBCLASS_FOOD 不存在
   真名是 ITEM_SUBCLASS_FOOD_DRINK  (ItemTemplate.h:324)

2. ItemTemplate::Flags 是【数组】不是标量
   ItemTemplate.h:763  inline bool HasFlag(ItemFlags flag) const { return (Flags[0] & flag) != 0; }
   必须写 proto->HasFlag(ITEM_FLAG_CONJURED)，不能写 proto->Flags & ...
```

---

## 改动一 / 共5处 —— botgossip.h 加两个 sender

文件：`D:\TrinityCore\src\server\game\AI\NpcBots\botgossip.h`

### 搜索原文（约 148-151 行，枚举结尾）

```cpp
    GOSSIP_SENDER_SCAN,
    GOSSIP_SENDER_DEBUG,
    GOSSIP_SENDER_DEBUG_ACTION,
};
```

### 替换为

```cpp
    GOSSIP_SENDER_SCAN,
    GOSSIP_SENDER_DEBUG,
    GOSSIP_SENDER_DEBUG_ACTION,
    GOSSIP_SENDER_GIFT_MENU,
    GOSSIP_SENDER_GIFT_ITEM,
};
```

---

## 改动二 / 共5处 —— bottext.h 加文本ID

文件：`D:\TrinityCore\src\server\game\AI\NpcBots\bottext.h`

### 搜索原文（约 437 行，最后一个）

```cpp
    BOT_TEXT_OWNER                          = 70700, //"Owner"
```

### 替换为

```cpp
    BOT_TEXT_OWNER                          = 70700, //"Owner"
    BOT_TEXT_GIVE_GIFT                      = 70801, //"<Give a gift>"
    BOT_TEXT_GIFT_ACCEPTED                  = 70802, //"Thank you!"
    BOT_TEXT_GIFT_REJECT_QUEST              = 70803, //"I can't take quest items."
    BOT_TEXT_GIFT_REJECT_BOUND              = 70804, //"That's soulbound to you."
    BOT_TEXT_GIFT_REJECT_CONJURED           = 70805, //"Conjured items won't last."
    BOT_TEXT_GIFT_REJECT_BAG                = 70806, //"I don't need bags."
    BOT_TEXT_GIFT_REJECT_LIMITED            = 70807, //"That will expire soon."
    BOT_TEXT_GIFT_NOTHING_TO_GIVE           = 70808, //"You have nothing to give me."
```

> 用 70801+ 是因为上游已排到 70700，留 100 个空位给上游更新。

---

## 改动三 / 共5处 —— bot_ai.cpp 加过滤器和分类器

文件：`D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.cpp`

### 第 1 步：找到这个函数（约 20412 行）

```cpp
void bot_ai::SetWanderer()
{
    if (IAmFree())
    {
        _wanderer = true;
        if (botPet)
            botPet->GetBotPetAI()->SetWanderer();
    }
}
```

### 第 2 步：在它的**右大括号之后**整段粘贴

> 如果你已装 A38 锚点，那段代码也在这里，**接在 A38 后面**即可，先后无所谓。

```cpp
enum BotGiftKind : uint8
{
    BOT_GIFT_KIND_FOOD                  = 1,
    BOT_GIFT_KIND_DRINK                 = 2,
    BOT_GIFT_KIND_POTION                = 3,
    BOT_GIFT_KIND_EQUIP                 = 4,
    BOT_GIFT_KIND_RARE                  = 5,
    BOT_GIFT_KIND_JUNK                  = 6,
    BOT_GIFT_KIND_GENERIC               = 7
};

enum BotGiftReject : uint8
{
    BOT_GIFT_OK                         = 0,
    BOT_GIFT_REJECT_QUEST               = 1,
    BOT_GIFT_REJECT_BOUND               = 2,
    BOT_GIFT_REJECT_CONJURED            = 3,
    BOT_GIFT_REJECT_BAG                 = 4,
    BOT_GIFT_REJECT_LIMITED             = 5
};

uint8 bot_ai::CheckGiftItem(Item const* item) const
{
    ItemTemplate const* proto = item->GetTemplate();
    if (!proto)
        return BOT_GIFT_REJECT_BAG;
    if (proto->Class == ITEM_CLASS_QUEST || proto->Bonding == BIND_QUEST_ITEM)
        return BOT_GIFT_REJECT_QUEST;
    if (item->IsSoulBound() || item->IsBoundAccountWide())
        return BOT_GIFT_REJECT_BOUND;
    if (proto->HasFlag(ITEM_FLAG_CONJURED))
        return BOT_GIFT_REJECT_CONJURED;
    if (proto->InventoryType == INVTYPE_BAG)
        return BOT_GIFT_REJECT_BAG;
    if (proto->Duration != 0)
        return BOT_GIFT_REJECT_LIMITED;
    return BOT_GIFT_OK;
}

uint8 bot_ai::ClassifyGiftItem(Item const* item) const
{
    ItemTemplate const* proto = item->GetTemplate();
    if (!proto)
        return BOT_GIFT_KIND_GENERIC;
    if (proto->Quality >= ITEM_QUALITY_EPIC)
        return BOT_GIFT_KIND_RARE;
    if (proto->Quality == ITEM_QUALITY_POOR)
        return BOT_GIFT_KIND_JUNK;
    if (proto->Class == ITEM_CLASS_CONSUMABLE)
    {
        if (proto->SubClass == ITEM_SUBCLASS_FOOD_DRINK)
            return BOT_GIFT_KIND_FOOD;
        if (proto->SubClass == ITEM_SUBCLASS_POTION)
            return BOT_GIFT_KIND_POTION;
        return BOT_GIFT_KIND_GENERIC;
    }
    if (proto->Class == ITEM_CLASS_WEAPON || proto->Class == ITEM_CLASS_ARMOR)
        return BOT_GIFT_KIND_EQUIP;
    return BOT_GIFT_KIND_GENERIC;
}

uint32 bot_ai::GetGiftRejectText(uint8 reason) const
{
    switch (reason)
    {
        case BOT_GIFT_REJECT_QUEST:     return BOT_TEXT_GIFT_REJECT_QUEST;
        case BOT_GIFT_REJECT_BOUND:     return BOT_TEXT_GIFT_REJECT_BOUND;
        case BOT_GIFT_REJECT_CONJURED:  return BOT_TEXT_GIFT_REJECT_CONJURED;
        case BOT_GIFT_REJECT_BAG:       return BOT_TEXT_GIFT_REJECT_BAG;
        case BOT_GIFT_REJECT_LIMITED:   return BOT_TEXT_GIFT_REJECT_LIMITED;
        default:                        return BOT_TEXT_GIFT_REJECT_BAG;
    }
}
```

---

## 改动四 / 共5处 —— bot_ai.h 加声明

文件：`D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.h`

### 搜索原文（约 196 行，public 段内）

```cpp
    void SetWanderer();
```

### 替换为

```cpp
    void SetWanderer();
    uint8 CheckGiftItem(Item const* item) const;
    uint8 ClassifyGiftItem(Item const* item) const;
    uint32 GetGiftRejectText(uint8 reason) const;
```

> 如果你装了 A38，这里已经有一串 `HasAnchor()` 等声明，
> 把上面 3 行**加在它们后面**即可。

---

## 改动五 / 共5处 —— bot_ai.cpp 加菜单和处理

文件：`D:\TrinityCore\src\server\game\AI\NpcBots\bot_ai.cpp`

### 5-1 加菜单入口（约 7833 行）

搜索原文：

```cpp
                AddGossipItemFor(player, GOSSIP_ICON_TALK, LocalizedNpcText(player, BOT_TEXT_GIVE_CONSUMABLE), GOSSIP_SENDER_USEITEM, GOSSIP_ACTION_INFO_DEF + 1);
            }
```

替换为：

```cpp
                AddGossipItemFor(player, GOSSIP_ICON_TALK, LocalizedNpcText(player, BOT_TEXT_GIVE_CONSUMABLE), GOSSIP_SENDER_USEITEM, GOSSIP_ACTION_INFO_DEF + 1);
            }
            AddGossipItemFor(player, GOSSIP_ICON_MONEY_BAG, LocalizedNpcText(player, BOT_TEXT_GIVE_GIFT), GOSSIP_SENDER_GIFT_MENU, GOSSIP_ACTION_INFO_DEF);
```

> 注意这行加在 `}` **外面** —— 让特殊职业（死亡骑士等）也能收礼物。

### 5-2 加两个处理分支

搜索原文（约 9496 行）：

```cpp
        case GOSSIP_SENDER_EQUIPMENT_BANK_DEPOSIT_ITEM:
```

在这一行的**前面**整段插入：

```cpp
        case GOSSIP_SENDER_GIFT_MENU:
        {
            subMenu = true;
            uint32 page = action - GOSSIP_ACTION_INFO_DEF;
            uint32 items_per_page = BOT_GOSSIP_MAX_ITEMS - 3;
            uint32 counter = 0;
            uint32 can_add_count = 0;
            uint32 k = 0;

            for (uint8 i = INVENTORY_SLOT_ITEM_START; i != INVENTORY_SLOT_ITEM_END && can_add_count <= items_per_page; ++i)
            {
                Item const* pItem = player->GetItemByPos(INVENTORY_SLOT_BAG_0, i);
                if (!pItem || CheckGiftItem(pItem) != BOT_GIFT_OK)
                    continue;
                ++k;
                if (k <= page * items_per_page)
                    continue;
                ++can_add_count;
                if (counter >= items_per_page)
                    continue;
                ++counter;
                std::ostringstream name;
                _AddItemLink(player, pItem, name);
                AddGossipItemFor(player, GOSSIP_ICON_CHAT, name.str(), GOSSIP_SENDER_GIFT_ITEM, GOSSIP_ACTION_INFO_DEF + pItem->GetGUID().GetCounter());
            }

            for (uint8 i = INVENTORY_SLOT_BAG_START; i != INVENTORY_SLOT_BAG_END && can_add_count <= items_per_page; ++i)
            {
                Bag const* pBag = player->GetBagByPos(i);
                if (!pBag)
                    continue;
                for (uint32 j = 0; j != pBag->GetBagSize() && can_add_count <= items_per_page; ++j)
                {
                    Item const* pItem = player->GetItemByPos(i, j);
                    if (!pItem || CheckGiftItem(pItem) != BOT_GIFT_OK)
                        continue;
                    ++k;
                    if (k <= page * items_per_page)
                        continue;
                    ++can_add_count;
                    if (counter >= items_per_page)
                        continue;
                    ++counter;
                    std::ostringstream name;
                    _AddItemLink(player, pItem, name);
                    AddGossipItemFor(player, GOSSIP_ICON_CHAT, name.str(), GOSSIP_SENDER_GIFT_ITEM, GOSSIP_ACTION_INFO_DEF + pItem->GetGUID().GetCounter());
                }
            }

            if (counter == 0 && page == 0)
                BotWhisper(LocalizedNpcText(player, BOT_TEXT_GIFT_NOTHING_TO_GIVE), player);

            if (page > 0)
                AddGossipItemFor(player, GOSSIP_ICON_TALK, LocalizedNpcText(player, BOT_TEXT_PREVIOUS_PAGE), GOSSIP_SENDER_GIFT_MENU, action - 1);
            if (can_add_count > items_per_page)
                AddGossipItemFor(player, GOSSIP_ICON_TALK, LocalizedNpcText(player, BOT_TEXT_NEXT_PAGE), GOSSIP_SENDER_GIFT_MENU, action + 1);
            AddGossipItemFor(player, GOSSIP_ICON_CHAT, LocalizedNpcText(player, BOT_TEXT_BACK), GOSSIP_SENDER_MAIN, GOSSIP_ACTION_INFO_DEF + 1);
            break;
        }
        case GOSSIP_SENDER_GIFT_ITEM:
        {
            ObjectGuid::LowType itemGuidLow = action - GOSSIP_ACTION_INFO_DEF;
            Item* item = player->GetItemByGuid(ObjectGuid::Create<HighGuid::Item>(itemGuidLow));
            if (!item)
            {
                BOT_LOG_ERROR("npcbots", "GOSSIP_SENDER_GIFT_ITEM: item {} not found on player {}!", itemGuidLow, player->GetName());
                break;
            }

            uint8 reject = CheckGiftItem(item);
            if (reject != BOT_GIFT_OK)
            {
                BotWhisper(LocalizedNpcText(player, GetGiftRejectText(reject)), player);
                return OnGossipSelect(player, me, GOSSIP_SENDER_GIFT_MENU, GOSSIP_ACTION_INFO_DEF);
            }

            if (!BotDataMgr::CanDepositBotBankItemsCount(player->GetGUID(), 1))
            {
                BotWhisper(LocalizedNpcText(player, BOT_TEXT_BANK_IS_FULL), player);
                return OnGossipSelect(player, me, GOSSIP_SENDER_GIFT_MENU, GOSSIP_ACTION_INFO_DEF);
            }

            BotDataMgr::DepositBotBankItem(player->GetGUID(), item);
            player->MoveItemFromInventory(item->GetBagSlot(), item->GetSlot(), true);

            BotWhisper(LocalizedNpcText(player, BOT_TEXT_GIFT_ACCEPTED), player);
            me->HandleEmoteCommand(EMOTE_ONESHOT_BOW);

            return OnGossipSelect(player, me, GOSSIP_SENDER_GIFT_MENU, GOSSIP_ACTION_INFO_DEF);
        }
```

---

## 台词入库 SQL

文本 ID 要写进 `world.creature_text` 才有中文。跑同目录的 `台词.sql`。

---

## 编译

改的是 `game` 项目，**不用重跑 CMake**（无新增 .cpp）。

---

## 验证

1. 右键任意自己的 bot -> 应看到 **`<Give a gift>`** 菜单项
2. 点进去 -> 列出背包里可赠予的物品（任务物品/绑定物品/魔法造物/背包**不会出现**）
3. 点一个物品 -> bot 鞠躬 + 说"Thank you!" -> **物品从背包消失**
4. 右键 bot -> `装备` -> `银行` -> **应该能看到刚送的东西**
5. 下线再上线 -> 银行里东西还在（`Player::SaveToDB` 已落库）

### 拒绝机制验证

背包里放一个**任务物品**，它**不该出现在列表里**。
（列表已过滤，所以正常玩不到拒绝提示；那几条文本是给作弊/竞态兜底的）

---

## 已做的验证

- 过滤器 `CheckGiftItem` + 分类器 `ClassifyGiftItem`
  已用 `g++ -std=c++20 -Wall -Wextra` **真编译 + 真运行**通过
  断言：任务物品拒 / 魔法造物拒 / 背包拒 / 食物收 / 分类正确（食物·珍稀·垃圾·装备）/ 列表过滤正确
- **实查纠正了两个会导致编译失败的常量名**（见上文"实查中抓到的两个坑"）
- 物品转移姿势照抄上游 `bot_ai.cpp:9513-9514`，顺序一致
- 所有 API 已实查访问段：

```text
botdatamgr.h:282  CanDepositBotBankItemsCount        public(203段)
botdatamgr.h:286  DepositBotBankItem                 public(203段)
botdatamgr.h:287  SaveNpcBotStoredGear               public(203段)
bot_ai.h:539      BotWhisper                         public
bot_ai.h:672      _AddItemLink                       private(590段) -- 在 bot_ai.cpp 内部调用，OK
Player.h:1156     MoveItemFromInventory              public
ItemTemplate.h:763 HasFlag(ItemFlags)                public
```

- 落库链路已实查：

```text
Player.cpp:19592     Player::SaveToDB -> SaveNpcBotStoredGear
botdatamgr.cpp:4039  DELETE FROM characters_npcbot_gear_storage WHERE guid = ...
botdatamgr.cpp:4043  item->SaveToDB(trans)
botdatamgr.cpp:4044  item->DeleteFromInventoryDB(trans)
botdatamgr.cpp:4045  INSERT INTO characters_npcbot_gear_storage
```

---

## 本步【没做】的（第3步再加）

- 食物/药水**当场吃掉**的分支（混合处置的另一半）
- 从 `npcbot_gift_text` 表读**随机台词**（现在是固定一句）
- 羁绊计分、写 `npcbot_gift_log`
- bot 主动索要
