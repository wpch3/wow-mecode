# G23-P0 真实AoE Loot与Eluna/Lua基线执行入口

更新时间：2026-08-22  
状态：首次报告已完整回传；总门槛False已确定为`game/Loot`与真实`game/Custom`的固定路径不兼容。v1永久关闭、禁止重跑；当前转`05-G23-P0R真实路径与Eluna扩展补充取证.md`  
性质：只读；本次未改源码、config、Lua或数据库，未进入复现/修复

## 0. 当前现场（先读）

用户已正确执行一次并按失败门槛停止：

```text
active PID=5304
exe sha256=07a8f95259a6d46ab5d46720e5b919fc5cc60f7e81086b81e2748e7d1e1a86c1
report=g23_baseline_20260822_001210.txt
report sha256=c17aac62a3b6f0370b4f441fbcedbb742cc6d8159b93f04d8c8a2cb75b1d6dbe
source/config/lua/database edits=0
G23_BASELINE_CAPTURE_PASS=False
```

这表示fail-closed生效，不是发生回滚或写坏文件。完整报告随后已回传，确认False仅由v1固定检查`game/Loot`、而真实文件位于`game/Custom`导致。当前不要再次执行本文件命令；转到05号P0R入口补齐真实源码、`.ext`和最新日志。

## 1. 为什么先做这一小步

“自动拾取偶发失灵”可能来自三条不同链：

1. A02 `CustomAoELoot` C++与`LootHandler.cpp`触发入口；
2. NPCBot/真人组队的原版分金和拾取权限；
3. Eluna/Lua部署、重载、事件注册或脚本异常。

仓库静态检查只能给出风险候选，不能直接认定根因。本步一次锁定Windows真实三源码、运行conf、活动worldserver、`Eluna.ScriptPath`、实际部署Lua全文、Eluna API上下文和近期相关日志；报告内包含所需真实源码全文，因此不再要求单独复制或上传原件。

## 2. 首次执行说明（已执行，当前仅作审计，禁止重跑）

把以下脚本下载/复制到Windows上传目录：

```text
C:\Users\Administrator\Downloads\workspace\uploads\probe_g23_baseline.py
```

仓库源文件：

```text
tc-bignum\规划\G23_Lua功能与自动拾取可靠性\probe_g23_baseline.py
```

锁定值：

```text
SHA-256=e68ecb389b898330575cbc1d03ab70ba51e8f72c8bde249c9ca45d80ec319d93
```

## 3. Windows PowerShell 5.1一次性执行

保持当前worldserver运行即可；不要停服、不要重启、不要`.reload eluna`。在Windows PowerShell 5.1粘贴完整原子块：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Workspace = "C:\Users\Administrator\Downloads\workspace"
    $Uploads = Join-Path $Workspace "uploads"
    $Probe = Join-Path $Uploads "probe_g23_baseline.py"
    $ExpectedProbe = "e68ecb389b898330575cbc1d03ab70ba51e8f72c8bde249c9ca45d80ec319d93"
    $Source = "D:\TrinityCore"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Exe = Join-Path $RunDir "worldserver.exe"
    $ExpectedExe = "07a8f95259a6d46ab5d46720e5b919fc5cc60f7e81086b81e2748e7d1e1a86c1"
    $Out = Join-Path $Uploads ("g23_baseline_" + (Get-Date -Format "yyyyMMdd_HHmmss") + ".txt")

    foreach ($Path in @($Python, $Probe, $Exe)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "找不到必需文件：$Path"
        }
    }
    foreach ($Path in @($Source, $RunDir)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Container)) {
            throw "找不到必需目录：$Path"
        }
    }

    $ProbeHash = (Get-FileHash -LiteralPath $Probe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ProbeHash -ne $ExpectedProbe) {
        throw "G23探针SHA不符：实际=$ProbeHash，预期=$ExpectedProbe"
    }

    $Processes = @(Get-CimInstance Win32_Process -Filter "Name='worldserver.exe'")
    if ($Processes.Count -ne 1) {
        throw "活动worldserver必须恰好1个，实际=$($Processes.Count)"
    }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe) {
        throw "活动运行目录exe不是F44R1锁定新二进制：实际=$ExeHash"
    }
    $ProcessPath = [string]$Processes[0].ExecutablePath
    if (-not $ProcessPath -or -not [System.IO.Path]::GetFullPath($ProcessPath).Equals(
        [System.IO.Path]::GetFullPath($Exe), [System.StringComparison]::OrdinalIgnoreCase)) {
        throw "活动worldserver路径不等于锁定RunDir：PID=$($Processes[0].ProcessId) PATH=$ProcessPath"
    }
    Write-Host "[OK] active PID=$($Processes[0].ProcessId); exe sha256=$ExeHash"

    & $Python $Probe --self-test
    if ($LASTEXITCODE -ne 0) {
        throw "G23探针self-test失败，退出码=$LASTEXITCODE"
    }

    & $Python $Probe $Source $RunDir $Out
    if ($LASTEXITCODE -ne 0) {
        throw "G23真实只读基线失败，退出码=$LASTEXITCODE；保留完整输出，不要继续复现或修改"
    }
    if (-not (Test-Path -LiteralPath $Out -PathType Leaf)) {
        throw "G23报告未生成：$Out"
    }

    $Report = Get-Content -LiteralPath $Out -Raw -Encoding UTF8
    foreach ($Marker in @(
        "G23_PROBE_SCHEMA=1",
        "G23_REQUIRED_SOURCE_FILES=3/3",
        "G23_AOE_HOOK_MARKERS_READY=True",
        "G23_SOURCE_FILE_EDIT_COUNT=0",
        "G23_CONFIG_EDIT_COUNT=0",
        "G23_LUA_FILE_EDIT_COUNT=0",
        "G23_DATABASE_EDIT_COUNT=0",
        "G23_BASELINE_CAPTURE_PASS=True"
    )) {
        if (-not $Report.Contains($Marker)) {
            throw "G23报告缺少门槛：$Marker"
        }
    }

    $ReportHash = (Get-FileHash -LiteralPath $Out -Algorithm SHA256).Hash.ToLowerInvariant()
    Write-Host "[OK] G23_P0_WINDOWS_BASELINE_PASS=True"
    Write-Host "[OK] report sha256=$ReportHash"
    Write-Host "[OK] report path=$Out"
    Write-Host "[STOP] 到此为止；不要Apply、不要编译、不要reload，回传这一份g23_baseline时间戳报告。"
}
```

## 4. 成功门槛

终端必须同时出现：

```text
[OK] G23 probe positive fixture and read-only postcheck passed.
[OK] Missing-source negative fixture did not false-pass.
[OK] Sensitive/unrelated config values are not captured.
[OK] G23 baseline probe self-test passed.
[OK] G23 source/config/lua/database edits=0
[OK] G23_BASELINE_CAPTURE_PASS=True
[OK] G23_P0_WINDOWS_BASELINE_PASS=True
```

然后只回传：

```text
C:\Users\Administrator\Downloads\workspace\uploads\g23_baseline_时间戳.txt
```

报告已内嵌真实三源码和已部署Lua全文，不再单独上传原件。

## 5. 当前不要执行的事

- 不重复F44R1 Check、Apply/postcheck、编译或启动验证；
- 不先把`AoELoot.Range`继续调大；
- 不用Lua重写自动拾取；
- 不执行`.reload eluna`，避免在基线捕获前改变Lua state；
- 不先改`AoELoot.MaxCorpses`或组队规则；
- 不运行自动拾取复现矩阵，先让报告裁决真实触发链和部署路径；
- 不把仓库静态风险候选写成真实根因。

收到报告后才生成最小复现矩阵；矩阵会明确区分“游戏内实际命令”和“观察结果文本模板”，任一关键阻断项失败即停止。
