# G17-B3R11 + F45R1 交付记录（2026-08-27 深夜 II）

## 用户报告与验证
- ✅ **B3R10 用户验证成功："八个御龙术技能已经出来了"**（八格全满＋切页第 8 格）
- F45 播报加 conf 无效（`AoELoot.Announce = 0` 后仍打印，且队里 bot 复读"任务进度：群体拾取F45…"）
- 新需求：陆地坐骑飞行时"双脚蹬个不停"——要求停下或正常

## F45R1（F45R1_FINAL.zip，5909B，SHA 07f469c2...0936）
版本兼容源码静默：
- V1（有开关版）：`GetBoolDefault("AoELoot.Announce", true)` → `false`（不依赖 conf）
- V2（无开关版）：括号/字符串感知地整条替换播报语句为注释（循环上限 8 处）
- 只动播报不动拾取逻辑；备份到 uploads\F45R1_Backup_*；可回滚
- 自测：门控/无门控/已静默/幂等 四例 PASS
- bot 复读判定为跟随系统消息，静默后应消失（若仍有→另有来源，回报复读原文）

## G17-B3R11（G17B3R11_FINAL.zip，228630B，SHA 07020629...f196c，34/34 自检）
陆地坐骑飞行动画修复：
- 根因：fork 源码注明 AnimTier::Swim 客户端不处理、Fly 层对无飞行动画模型回退地面层
  → BEAST/GENERIC 模型移动时播放跑步循环（蹬腿）
- 修复：飞行期间对 BEAST/GENERIC 原型 `SetEmoteState(EMOTE_STATE_STAND)`（=26）
  ——表情状态覆盖非自身单位的移动动画（脚本 NPC 滑行同原理），双腿定住；
  落地/退出 `EMOTE_ONESHOT_NONE` 恢复。飞行模型原型（龙/机械/魔法）不受影响
- payload 520696ee（PRE=B3R10 199cff4a，回滚 B3R6 3fdb46e8，16 镜像白名单）

## 安装顺序（一次编译）
1. 关 worldserver → 01_Install_F45R1.cmd（改源码） → PASSED
2. 01_Install_Build_G17B3R11.cmd（统一 MSBuild） → 首行 G17B3R11_BUILD=520696ee → PASSED
3. 重启 → 播报消失＋陆地坐骑飞行腿停

## 路线图（用户"剩下的任务"确认中）
技能差异化设计已立项：`G17_技能差异化设计_990029起_20260827.md`（五种施法形态×5原型，
读条/引导/位移/姿态/终结，全部对照真实法术原型，B3R12+C12 交付）

---

# 补充（同日）：B3R11 r1a 编译修复 ＋ C12 UI 框适配

## B3R11 r1 真实编译失败（用户报告）
- `cs_dragonriding.cpp(1902): error C2065 "ARCHETYPE_BEAST"/"ARCHETYPE_GENERIC": 未声明的标识符`（×2）
- 根因：AI 类在 `G17Dragonriding` 命名空间之外，`UpdateFlightEmote` 里的 ARCHETYPE_* 裸用
  （别处的裸用有局部 `using namespace` 所以能编）。
- r1a 修复：加 `G17Dragonriding::` 前缀，其余不变；payload `c5c4c332`，
  PRE=r1 `520696ee`（用户盘上已应用未编译的镜像，安装器白名单识别），
  回滚仍为 B3R6；35/35 自检。
- 附带确认：F45R1 在用户机器上走了 V2（无开关版整条移除）——编译日志中
  `CustomAoELoot.cpp(161) warning C4100 'player' 未引用参数` 即移除播报后的
  无害残留（说明其版本确无 conf 开关，conf 无效原因坐实）。

## C12 客户端魔改 v2（G17C12_FINAL.zip，75d25fa4...3b74，25/25 自检）
- 用户报告：八个技能后 UI 框太小装不下（皮肤美术按 6 按钮尺寸绘制）。
- 方案（缩图标适配框）：客户端 `VehicleMenuBar.lua` 的
  `VehicleMenuBar_UpdateActionBars` 增加一行
  `VehicleMenuBarActionButtonFrame:SetScale(0.75)`（守卫 `>6`）——
  8×62px 按钮等比缩放后正好回到 6 按钮占位宽度；点击/键位/冷却不受影响。
- 可调：payload lua 中 0.75 改值即可；xml（按钮7/8）与 DBC 原样透传。
- 前置：C11 已装 ✓；幂等。

## 安装顺序（本批）
1. B3R11（r1a）：关 worldserver → 01_Install_Build_G17B3R11.cmd → 首行
   G17B3R11_BUILD=c5c4c332 → PASSED → 重启
2. C12：关 WoW → 01_Install_G17C12.cmd → PASSED → 重启客户端

---

# 补充 II（2026-08-27 深夜 III）：G17-B3R12 无目标隐藏冷却修复＋技能差异化#1/#2

## 用户报告（B3R11 r1a 验收）
- ✅ 编译 PASS；✅ 飞行不再乱蹬腿
- 🔴 重大bug：无目标按战斗技→"内置冷却"生效（提示技能没准备好）但 UI 无冷却显示→卡住不能用
- 🟡 第 7 格"换错"（飞行页出现攻击技=生成器交叉填充）
- 指令：耐心稳定无失误，同步制作技能差异化

## B3R12（G17B3R12_FINAL.zip，46d9b602...60c8，38/38 自检）
payload 3d501d9b（PRE=B3R11r1a c5c4c332；回滚 B3R6；18 镜像白名单）：

1. **无目标隐藏冷却修复（根因闭环）**：
   - 根因链：无目标按技→载体施法在核心层"成功"→核心对成功施放施加 GCD/内置冷却
     →脚本层 ExecuteCombatSkill 早退（打印提示、不加 G17 冷却、不发 UI 封包）
     →"内置冷却存在＋UI 无显示"＝卡死假象
   - 修复：spell_g17_combat_skill::CheckCast 预检目标（完整解析链：显式目标→
     骑手选中→龙当前目标；槽 2 防御技与 triggered 自动战斗免检）→无目标时
     施法干脆失败（SPELL_FAILED_BAD_TARGETS 红字），零消耗零冷却
2. **技能差异化 #1/#2**（第一批）：
   - 990029 突袭·俯冲打击（战斗页@7）：首个范围技——目标 8 码内全体伤害
     （原型 War Stomp 45；服务端 DBC 追加＋world SQL 绑定＋SpellScript AoE）
   - 990030 御风姿态（移动页@7）：姿态开关——+15% 转向＋回能加倍（原型
     Aspect of the Cheetah 5118；AI 状态 ACTION_WIND_STANCE）
   - 安装器一条龙：源码→服务端 DBC 追加→world SQL→MSBuild（B3R1 已验证模式）
3. **第 7 格页面纯粹化**：移动页=御风姿态、战斗页=突袭（移除生成器/俯冲交叉）

## 待验收
1. 无目标按战斗技→红字"需要正确的目标"，立刻重按不卡
2. 移动页 7=御风姿态（开/关有聊天提示、转向变化）
3. 战斗页 7=突袭（目标周围群伤）
4. 其余不变

---

# 补充 III：B3R12 安装器热修（$ServerDbc 未定义）

## 用户报告
`G17B3R12_WINDOWS_BUILD_ERROR=usage: append_g17b3r12_spells.py [-h] [--version] [--input INPUT]` → DBC 步骤拿空输入，FAIL（源码 apply 本身已 PASS，盘上已是 3d501d9b）。

## 根因
拼装安装器时 `$Appender/$Sql` 加了但 **`$ServerDbc` 漏加**（第一版加在 $RunDir 定义之前会拿到空父路径，第二版干脆漏了）→ `--input` 后是空串 → argparse 打 usage 拒绝。属安装器门仿真测试覆盖不到的 PowerShell 变量作用域类缺陷（工具层哈希都对）。

## 修复（同包热更）
1. `$ServerDbc = Join-Path $RunDir "dbc\Spell.dbc"` 移到 `$RunDir` 定义之后正确添加
2. 过时横幅顺手修正：B3R6_BUILD=r1_perf_fix → G17B3R12_BUILD=r12_targetfix_variety；
   RUNS_SQL/RUNS_DBC=False → True（本包真跑 SQL 和 DBC）
3. PS 静态门＋38/38 包自检复验 PASS；G17B3R12_FINAL.zip 重建

## 用户操作
重新下载 G17B3R12_FINAL.zip → 关 worldserver → 重跑 01_Install_Build_G17B3R12.cmd →
首行应显示 G17B3R12_BUILD=r12_targetfix_variety → PASSED。
（源码已在盘上=3d501d9b，安装器会识别为 ALREADY_APPLIED 直通；DBC+SQL+编译会补跑。）
