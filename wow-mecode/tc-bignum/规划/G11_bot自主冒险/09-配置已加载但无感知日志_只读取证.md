# G11 第2步：配置已加载但无感知日志——只读取证

> 日期：2026-08-20
> 证据：用户上传 `conf完整日志.txt`（749行）
> 总体操作入口：`tc-bignum/00-当前整体安装步骤_单文件入口.md`
> **状态纠正：本文件记录的是749行“启动截取”阶段。随后v1探针在Server.log找到至少20条G11命中，只是格式缺陷隐藏正文；不能再写成运行日志0次。v1已停用，当前以10号复盘和 `probe_g11_runtime_v2.py` 为准。**
> 当前原则：不重装、不重编译、不做SQL；用v2输出原始日志并判定human最小验收。

## 一、日志已经证明什么

已确认：

- `g11_perception.conf` 被additional config加载；
- worldserver为 `bignum-mod` / `ae60f5b6f6ff+` / Windows AMD64 / RelWithDebInfo；
- `NPCBots config loaded`、`NPCBots system enabled`；
- 运行时加载179条 `npcbot care texts`；
- 建立750只wandering bot生成任务；
- `World initialized in 0 minutes 13 seconds`并进入ready。

因此基础启动成功，G19数据库内容也进入了运行时。

## 二、日志没有证明什么

全文中以下标记均为0次：

```text
[G11-PERCEPTION]
kind=
seen=
readonly=
```

`Loaded additional config file ...g11_perception.conf` 只证明文件被解析，不等于已经证明：

1. 运行时读到 `Enable=1`；
2. 启动的是含G11标记的实际exe；
3. `Logger.npcbots`允许INFO输出；
4. 真人靠近的是 `IsWanderer() && IAmFree()` 的自由游荡未招募bot；
5. 上传日志覆盖了真人靠近后的测试窗口。

本文件主要是启动阶段日志，没有足够证据区分上述原因。

## 三、G11只会在哪些bot上记录

第2步源码的前置条件是：

```text
Enable=1
ReadOnly=1
IsWanderer()
IAmFree()
alive
in world
not teleporting
```

固定NPCBot、随行/已招募NPCBot、死亡或传送中的bot都不会打印G11日志，这是预期行为，不代表代码失效。

## 四、当前唯一下一步（已由后续证据更新）

v1已停用。运行同目录的：

```text
probe_g11_runtime_v2.py
```

后续同一PID 40秒真人刺激已得到28条human完整日志、0缺字段和明确PASS，本文件的“无感知日志”历史断点已经彻底结束。当前只归档完整报告并核对用户指出的两项差异。

## 五、当前分支判断

用户已确认Enable/ReadOnly、活动exe路径、exe标记、INFO logger及自由游荡未招募NPCBot资格；v1报告又证明至少20条日志命中。因此现在不再重复这些分支，也不猜改源码。只检查v2报告中的原始行、human计数、缺字段数及最终PASS。

SQL与本问题无关，不执行G16 SQL `12`/`14`。PBot不属于当前human最小验收前置。

## 六、后续证据已修正本文件的暂定判断

用户运行v1探针后，第7节出现20行 `Server.log` 路径。这代表至少20条 `[G11-PERCEPTION]` 匹配已经存在；v1只是没有显示Line正文。旧探针结论和本文件标题不得继续作为当前状态。

无需重复self-test或第9C步。上传已经生成的完整时间戳报告即可。
