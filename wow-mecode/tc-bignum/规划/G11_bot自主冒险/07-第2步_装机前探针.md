# G11 第2步：装机前只读探针

> 日期：2026-08-19
> **状态：用户真实探针已完整回传，本步骤完成。** 已确认 `bignum-mod` / `ae60f5b6f6ffe9a0426dfeb6e227712de7d6c8b7`、四个目标源码哈希与全部锚点。不要重复跑旧探针；当前转到 `08-第2步_基于真实源码的安装与验收.md`。
> 目的：取得你当前 `D:\TrinityCore` 已修改源码的真实锚点，再生成精确 C++ 补丁。
> 安全性：脚本只读源码、Git 状态和文件哈希，不修改任何源码。

---

## 一、为什么必须先跑探针

GitHub 的 `wow-mecode` 目前主要保存规划、补丁、配置和上传证据，并不包含你正在编译的完整 `D:\TrinityCore` 源码。

G19 已经证明：上游行号与真实源码相差数百行时，按旧行号插代码会插进错误函数或 `if...else` 中间。G11 感知层需要同时改 `bot_ai.h/.cpp` 和 `botconfig.h/.cpp`，而这四个文件又已经装过多批自定义改动，不能靠上游行号猜。

本探针会一次收集：

- 当前 Git 分支、提交与未提交改动；
- 四个目标文件的编码、大小和 SHA256；
- `GlobalUpdate`、`CommonTimers`、构造函数与成员区真实上下文；
- G19 `GetCurrentScene` 的真实位置和数量；
- 现有 Companion/Wanderer 配置扩展；
- 已有玩家网格扫描 API；
- `PlayerDisconnected` / 项目自定义 `IsBotSession`；
- Group 的 Creature/Player 组队 API；
- 所有待新增符号的冲突计数。

---

## 二、执行方法

脚本文件：

```text
tc-bignum\规划\G11_bot自主冒险\probe_g11_step2.py
```

### 方法 A：脚本已在 D 盘项目里

打开 PowerShell 或 Git Bash：

```powershell
python "脚本所在完整路径\probe_g11_step2.py" "D:\TrinityCore" "D:\probe_g11_step2.txt"
```

成功时显示：

```text
[OK] 只读探针完成：D:\probe_g11_step2.txt
[OK] 报告字节数：...
```

### 方法 B：Python 命令是 py

```powershell
py -3 "脚本所在完整路径\probe_g11_step2.py" "D:\TrinityCore" "D:\probe_g11_step2.txt"
```

---

## 三、回传什么

请完整回传：

```text
D:\probe_g11_step2.txt
```

不要只截图最后几十行。最关键的构造函数、成员作用域和 `GlobalUpdate` 上下文在报告中间，只发结尾无法制作安全补丁。

如果报告开头的 Git 状态存在未提交修改，也不要先清理；原样回传。未提交修改很可能就是已经编译通过的 G19，不能随便 `git checkout --`。

---

## 四、探针自己的验证记录

`probe_g11_step2.py` 已对上游提交 `4e8762ee2b00948fa103d0cd1afd78ccdf4364fb` 实际运行通过：

- 8 个必需文件全部读取；
- Git 分支和提交成功输出；
- 文件编码与 SHA256 成功输出；
- `GlobalUpdate` / `CommonTimers` / 玩家网格 API / Group API 均能定位；
- 脚本未产生任何源码差异；
- 输出使用 UTF-8 BOM + CRLF，Windows 记事本和 VS 可直接打开。

注意：上游测试中没有 G19 的 `GetCurrentScene` 是正常的；你的真实源码应能找到它。若真实报告中计数为 0，说明运行探针的源码目录不是刚刚编译 G19 的那一份，必须先停止，不能继续装 G11。

---

## 五、探针回来后的下一交付

基于真实输出制作：

1. `08-第2步_感知层精确补丁.md`；
2. 自动备份并校验锚点的 Python 安装脚本；
3. 独立 `g11_perception.conf`；
4. 编译前符号计数与作用域自检；
5. 编译后的 `[G11-PERCEPTION]` 日志验收表；
6. 失败时一键回滚方法。

在探针回来前，不会让你手工猜插入点。
