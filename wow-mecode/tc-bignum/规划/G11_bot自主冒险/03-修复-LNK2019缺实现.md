# 修复 LNK2019：三个函数只声明没实现

## 先自查（10秒）

打开 `D:\TrinityCore\src\server\game\AI\NpcBots\bot_companion.cpp`

`Ctrl+F` 搜：

```text
BotCompanionMgr::GetPendingRequest
```

| 结果 | 说明 |
|---|---|
| **0 个** | 改动三没装，按下面补上 |
| 1 个 | 装了，那是别的问题，告诉我 |

---

## 原因

```text
LNK2019 无法解析的外部符号 BotCompanionMgr::GetPendingRequest
LNK2019 无法解析的外部符号 BotCompanionMgr::DoesGiftMatchRequest
LNK2019 无法解析的外部符号 BotCompanionMgr::MarkRequestMet
```

`LNK2019` = **链接期**错误，意思是"有人调用了这个函数，但找不到它的函数体"。

你装了：
```text
改动二  bot_companion.h    加了3个【声明】       <- 装了
改动三  bot_companion.cpp  加3个【实现】         <- 【漏了这个】
改动四  bot_ai.cpp         调用                  <- 装了
改动五  pbot_autoaccept.cpp 调用                 <- 装了
```

**声明告诉编译器"有这个函数"，所以编译能过；
链接时找不到函数体，才报错。**

---

## 修法

### 打开文件

```text
D:\TrinityCore\src\server\game\AI\NpcBots\bot_companion.cpp
```

### 找位置

`Ctrl+F` 搜（**全文件唯一**）：

```text
void BotCompanionMgr::ReloadAll()
```

你会看到：

```cpp
void BotCompanionMgr::ReloadAll()
{
    LoadCareTexts();
    LoadCareItems();
    LoadInventories();
    LoadGiftTexts();
    LoadRequestTexts();
}
```

### 在这个函数【上面】整段粘贴

```cpp
// ---------------------------------------------------------------------------
//  G11第1步：查最近10分钟有没有未满足的索要
// ---------------------------------------------------------------------------
uint8 BotCompanionMgr::GetPendingRequest(uint32 botId, uint8 botKind, uint32 ownerGuid)
{
    uint32 since = uint32(GameTime::GetGameTime()) - 600;

    QueryResult res = CharacterDatabase.PQuery(
        "SELECT request_type FROM npcbot_request_log WHERE bot_id = {} AND bot_kind = {} "
        "AND owner_guid = {} AND answered = 0 AND ask_time > {} ORDER BY id DESC LIMIT 1",
        botId, uint32(botKind), ownerGuid, since);

    return res ? res->Fetch()[0].GetUInt8() : 0;
}

// ---------------------------------------------------------------------------
//  G11第1步：你给的东西对不对口
// ---------------------------------------------------------------------------
bool BotCompanionMgr::DoesGiftMatchRequest(uint8 giftKind, uint8 reqType)
{
    switch (reqType)
    {
        case BOT_REQUEST_FOOD:    return giftKind == BOT_GIFT_KIND_FOOD;
        case BOT_REQUEST_DRINK:   return giftKind == BOT_GIFT_KIND_DRINK;
        case BOT_REQUEST_BANDAGE: return giftKind == BOT_GIFT_KIND_POTION || giftKind == BOT_GIFT_KIND_FOOD;
        case BOT_REQUEST_REPAIR:  return giftKind == BOT_GIFT_KIND_EQUIP;
        default:                  return false;
    }
}

// ---------------------------------------------------------------------------
//  G11第1步：标记满足 + 额外加分
// ---------------------------------------------------------------------------
void BotCompanionMgr::MarkRequestMet(uint32 botId, uint8 botKind, uint32 ownerGuid, uint8 reqType)
{
    uint32 now = uint32(GameTime::GetGameTime());
    uint32 since = now - 600;

    CharacterDatabase.PExecute(
        "UPDATE npcbot_request_log SET answered = 1, answer_time = {} "
        "WHERE bot_id = {} AND bot_kind = {} AND owner_guid = {} AND request_type = {} "
        "AND answered = 0 AND ask_time > {}",
        now, botId, uint32(botKind), ownerGuid, uint32(reqType), since);

    CharacterDatabase.PExecute(
        "UPDATE npcbot_bond SET requests_met = requests_met + 1, "
        "gift_points = gift_points + 15 "
        "WHERE bot_id = {} AND bot_kind = {} AND owner_guid = {}",
        botId, uint32(botKind), ownerGuid);
}

```

### 粘贴后应该长这样

```cpp
void BotCompanionMgr::MarkRequestMet(uint32 botId, uint8 botKind, uint32 ownerGuid, uint8 reqType)
{
    ...
}

void BotCompanionMgr::ReloadAll()
{
    LoadCareTexts();
    ...
}
```

---

## 装完自检

`bot_companion.cpp` 里 `Ctrl+F`，每个都应该是 **1 个**：

| 搜索词 | 应有次数 |
|---|---|
| `BotCompanionMgr::GetPendingRequest` | 1 |
| `BotCompanionMgr::DoesGiftMatchRequest` | 1 |
| `BotCompanionMgr::MarkRequestMet` | 1 |

`bot_companion.h` 里也各 1 个（声明）。

**头文件 1 个 + cpp 文件 1 个 = 正确。**

---

## 关于那两条 C1069

```text
C1069 无法读取编译器命令行  botdatamgr.cpp 4236
C1069 无法读取编译器命令行  bot_ai.cpp 22210
```

**不用管。** 这是 MSVC 在链接失败后的连带报错，
`LNK2019` 修好就消失。

---

## 我的问题

这是**同一个模式第三次出现**了：

```text
A37第3步  改动四(bot_ai.h声明) 漏做 -> C2065
A37第6步  枚举移动只做了一半    -> C2011
G11第1步  改动三(实现) 漏做     -> LNK2019
```

**共同点：同一个功能拆在多个文件、多个章节，中间隔着大段代码。**

### 从现在起我改用这个格式

每份改动清单**最前面**放一个勾选表：

```text
本次共 5 处改动，请逐项打勾：
[ ] 1. bot_companion.h    加声明（3行）
[ ] 2. bot_companion.cpp  加实现（3个函数）   <- 最容易漏
[ ] 3. bot_ai.cpp         改调用
[ ] 4. pbot_autoaccept.cpp 改调用
[ ] 5. 跑 SQL

装完自检：
[ ] bot_companion.h  搜 GetPendingRequest = 1 个
[ ] bot_companion.cpp 搜 BotCompanionMgr::GetPendingRequest = 1 个
```

**声明和实现必须成对检查** —— 这是 LNK2019 的唯一预防手段。
