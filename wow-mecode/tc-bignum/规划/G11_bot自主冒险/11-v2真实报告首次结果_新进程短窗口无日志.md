# G11 v2真实报告首次结果：新进程短窗口无日志

> 日期：2026-08-20（Asia/Shanghai）
> 总体执行入口：`tc-bignum/00-当前整体安装步骤_单文件入口.md`
> 报告：`g11_runtime_diag_v2_20260820_232023.txt`

## 一、有效证据

replacement探针SHA256 `5f7a200640d4f2af1571108a11cc4cfb00d861a7874a94f8c77b14e4256b3d98` 已先在Windows PowerShell 5.1 / Python 3.12.10通过完整self-test。真实报告结构完整，不再存在v1隐藏字段问题。

首次真实报告：

```text
CONFIG_EXACT=True
ACTIVE_PROCESS_COUNT=1
PROCESS PID=14980
PROCESS_EXPECTED_PATH_MATCH_COUNT=1
PROCESS_PATH_MATCH=True
EXE_G11_MARKER=True
LOGGER_INFO_ALLOWED=True
G11_LOG_MATCHES=0
G11_HUMAN_MATCHES=0
G11_PLAYERBOT_MATCHES=0
G11_LINES_WITH_MISSING_FIELDS=0
G11_HUMAN_MINIMUM_PASS=False
```

六项配置均唯一且精确：1 / 60 / 4000 / 8000 / 15000 / 1。活动exe SHA256为：

```text
d0fbc5e3cf429a3674a6b6b48ba6f436c0be7791bebcf5014942b804f077b474
```

## 二、为什么这与v1的至少20条命中不矛盾

进程CreationDate原始值：

```text
/Date(1787239198571)/
```

换算为Asia/Shanghai：

```text
2026-08-20 23:19:58.571 +08:00
```

报告文件时间为23:20:23，因此取证时该worldserver进程只运行约24秒，且其中还包含world初始化时间。v1的20条命中来自本次重启之前的Server.log；重启可能截断或轮转旧日志。当前0命中只能证明“新PID 14980的极短窗口尚未留下G11行”，不能推翻此前日志曾经存在，也不能据此判定源码失效。

## 三、当前唯一最小刺激

不重启worldserver，保持PID 14980：

1. 用真实WoW客户端登录真人角色；
2. 站到一只已确认的自由游荡、未招募NPCBot 60码内，建议10–30码；
3. 不招募、不让它跟随；
4. 保持双方在世界、存活且不传送；
5. 等待40秒；
6. 执行总体入口第9C步原子块，生成新的时间戳报告。

40秒覆盖4–8秒扫描间隔及15秒日志冷却。当前只测真人，PBot仍不是前置。

## 四、通过标准

```text
G11_LOG_MATCHES >= 1
G11_HUMAN_MATCHES >= 1
G11_LINES_WITH_MISSING_FIELDS=0
G11_HUMAN_MINIMUM_PASS=True
```

同时原始 `LOG` 行必须包含：

```text
kind=human
seen=first 或 seen=again
readonly=1
```

## 五、边界

- 不重复self-test；
- 不重装、不重编；
- 不改配置；
- 不执行SQL；
- 不先测PBot；
- 如果PID变化，停止本轮并记录新启动时间，不把两个进程窗口混在一起；
- 若同一PID、合格场景等待40秒后仍为0，再做精确的当前日志文件编码/写入目标与更新跳过原因取证，不直接猜改业务逻辑。

## 六、同一PID 40秒刺激结果

用户已回传：

```text
G11_LOG_MATCHES=28
G11_HUMAN_MATCHES=28
G11_PLAYERBOT_MATCHES=0
G11_LINES_WITH_MISSING_FIELDS=0
G11_HUMAN_MINIMUM_PASS=True
```

判定：G11第2步human最小验收通过。

- `G11_LOG_MATCHES >= 1` 和 `G11_HUMAN_MATCHES >= 1` 是最低门槛，不要求恰好等于1；实际28完全满足；
- 总数守恒：28条总命中 = 28条human + 0条playerbot；
- 本轮没有布置PBot，所以playerbot=0是预期，不是缺陷；
- 缺字段为0，证明28条命中均具有完整字段；
- 完整时间戳报告仍待上传归档，以便逐行核对用户指出的“至少两项数据不一样”具体指哪些字段。未看到原始LOG前不猜测差异含义。
