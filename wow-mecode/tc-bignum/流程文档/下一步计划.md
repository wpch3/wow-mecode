# 下一步计划

> 2026-07-29 · 战斗节奏已收工，重新理清全局

---

## 一、战斗节奏 —— ✅ 收工

**结论：GCD 和读条已生效，体验目标达成。**

| 项 | 结果 |
|---|---|
| GCD 60% | ✅ 生效（战士明显变快） |
| 读条 75% | ✅ 生效（施法变快，进度条显示滞后是客户端问题） |
| CD 分段 | ✅ 生效（盾墙、法术反射等正常） |
| 冲锋 / 寒冰屏障 | 不管了 —— 少数特殊技能，不影响整体体验 |
| 独立CD | **不做** —— 收益小、破坏平衡、有 conf 替代方案 |

> 后续想更爽，改一个数字就行：`Speed.Cd.Short` 从 85 调到 70。
> 全职业均等受益，`.reload config` 立刻生效。

---

## 二、你手上有 5 样东西还没装

这些都已经做好、编译验证过了，但你还没装到服务器上：

| # | 东西 | 装法 | 耗时 |
|---|---|---|---|
| A | **performance.conf** | 丢进 `worldserver.conf.d\` | 1 分钟 |
| B | **Lua 三件套** | 丢进 `lua_scripts\` + 1 个建表 SQL | 5 分钟 |
| C | **`.spell clean`** | 源文件 + 注册 + 2 SQL | 要编译 |
| D | **`.reloaditem`** | 源文件 + 注册 + 2 SQL | 要编译 |
| E | 两个工具脚本 | 放着备用，做地图时才用 | — |

**建议**：A 和 B 现在就装（不用编译，5 分钟搞定），C 和 D 攒一起下次编译时装。

---

## 三、接下来做什么 —— 三条路线，选一条

我不想再堆功能了，你现在缺的不是功能数量，是**方向**。三个选项：

### 路线一：把服务器"补完整"（推荐先做）

目标：让服务器现在就能舒服地玩，不再是半成品。

```
第1步  装 A+B（5分钟，不用编译）
第2步  装 AoE Loot 模块          ← 群体拾取，你掉落5-10倍，这个最急需
第3步  装 Portal Master（纯SQL） ← 传送NPC，130+传送点
第4步  编译时顺带装 C+D
```

**为什么推荐**：这四步做完，日常玩的体验就完整了 —— 捡东西不累、跑图方便、技能书干净、调数值不重启。

**耗时**：2-3 小时（主要花在装模块和编译）

---

### 路线二：做幻化（单点突破）

目标：把最有价值的那个遗漏功能补上。

```
.transmog set/slot/remove/clear/list
```

**为什么值得**：方案早就验证过了 —— 全服只有 `Player::SetVisibleItemSlot()`（Player.cpp:12170）一处写外观字段，调用点仅 5 处。在这一个函数里插「查幻化表」，**所有场景自动生效**（登录/换装/跨图/别人看你）。

配合 `OnLogin`/`OnSave` 钩子持久化，是**真幻化**，重登不失效。

**耗时**：我写代码 + 你编译测试，大概一轮

---

### 路线三：开始搞剧情内容

目标：从"改数值"转向"做内容"。

```
第1步  用 Lua 做世界之魂进度条原型（不用编译）
第2步  用 Lua 做源血采集机制原型
第3步  跑通 1 张最小自定义地图的完整流程
```

**为什么现在可以开始**：Lua 改完 `.reload eluna` 立刻生效，试错成本几乎为零。剧情那套机制（五数值/源血/熟识度）全都适合先用 Lua 做原型。

**耗时**：前两步各一轮，第三步要你手动做（DBC编辑+提取+打包）

---

## 四、我的建议

**先走路线一**，理由：

1. 你现在服务器上有一堆"做好了但没装"的东西，先清干净
2. AoE Loot 是真痛点 —— 掉落 5-10 倍不装这个会捡到手软
3. 装完之后服务器是个**能舒服玩的完整状态**，再往上加内容心里有底

路线一走完，再选路线二或三。

---

## 五、明确不做的事

写在这里，免得以后又绕回来：

| 不做 | 原因 |
|---|---|
| 独立CD（拆共享CD） | 收益小、破坏职业平衡、有 conf 替代 |
| 冲锋/寒冰屏障的CD | 特殊技能机制，不影响整体体验 |
| 客户端读条显示同步 | 要打 MPQ 补丁，等做地图时一起打包 |
| `vas-autobalance` 模块 | 和你已有的 Solocraft 冲突 |
| 反作弊模块 | 自己玩/好友服，会误伤 GM 指令 |

---

## 六、你现在回一个字就行

- **「一」** → 我给你路线一的详细步骤（含 AoE Loot 怎么装）
- **「二」** → 我开始写幻化模块
- **「三」** → 我开始写剧情机制的 Lua 原型
- **或者直接说你想做什么**
