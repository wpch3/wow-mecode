-- ============================================================
--  套装系统 v3 · RBAC 权限注册 1/2
--  【本文件只有一条语句，把光标放在语句里执行】
--  71005 已在 v2 用过；若已存在会被 REPLACE 覆盖，安全
-- ============================================================
REPLACE INTO auth.rbac_permissions (id, name) VALUES (71005, 'Command: gearset');
