# 当前整体安装步骤——单文件入口

> 最后更新：2026-08-24
> 适用源码：`D:\TrinityCore`
> 当前源码基线：`bignum-mod` / `ae60f5b6f6ffe9a0426dfeb6e227712de7d6c8b7`
> 编译环境：Visual Studio 2022 / RelWithDebInfo / x64
>
> **以后每一批源码、配置、SQL或客户端修改，都必须同步更新本文件。**
> 用户只需从本文件按顺序执行，不需要再翻多份历史文档。

---

## 一、这次到底要做什么

| 顺序 | 功能 | 当前状态 | 本次动作 |
|---:|---|---|---|
| 1 | G11 第2步只读感知 | human最小验收通过：28/28/0/0/True | 归档现有完整报告；不重复探针 |
| 2 | G17-P0 全世界飞行源码取证 | schema-2真实报告通过：42上下文、0缺失、0冲突、A/B READY | 已归档；禁止重复v1/v2探针 |
| 3 | G17-A 安全全世界飞行 | 第17步、第18步真实Apply和第18R步只读恢复验证均PASS；post/源码备份/exe/PDB备份/conf已锁定 | **Apply阶段关闭，禁止重复第17、18、18R步；不要回滚** |
| 4 | G17-A 编译与矩阵 | 第20B-S已PASS；第20C-1用户已声明执行完成并确认主城禁召、外飞入暴风城落地、PVP战场表现相同 | **20C-1关闭，不重复A3/PVP；未提供的A4原始地点/GPS与逐项前后检不补造** |
| 5 | G17-B 御龙术 + 纯飞行坐骑 | B1已关闭；B2R1真实Windows Apply/World迁移/MSBuild PASS，但用户真人Runtime否决技能2特效简陋、技能3固定方向转弯掉头、技能4全坐骑不可用；B2R2已修复这三项（待用户Windows编译与Runtime） | **只运行G17-B2R2：Apply单文件+执行52226绑定守护SQL+增量编译+三行验收；不重跑B1/B2/B2R1旧包或客户端R1–R5** |
| 6 | PBot/NPCBot完善 | 装备适配、职责自动选择/补缺、可玩BOSS缩放及40/100/1000一键编团均未完成 | 已登记到御龙术之后；先GEAR，再做ROLE/SCALING的探针与≤40人闭环，确认不会因人数增加令BOSS血量上亿不可击杀，之后才进超大团队线 |
| 7 | G11 完整运行验收 | human最小门槛已过，T1–T10/性能未完成 | G17-A后补矩阵；PBot T7未做 |
| 8 | G19 第3步情境对话 | 数据库和启动加载已验收 | 后续做主城/野外/副本与冷却运行验收，不重跑SQL |
| 9 | G16 每entry十条与传说过滤 | 需求已锁定，C++尚未生成 | 保留SQL `12`/`14` 和源码探针，不误写完成 |
| 10 | G22完美客户端/12.x | 权威总纲已加入当前12.1功能对标账与最终全世界可飞口径；仍只是计划，未做真实客户端CP0 | F44R1运行门槛关闭后排CP0只读审计；主城禁飞只算安全一期，不能当最终上限 |
| 11 | F44/F44R1 `.combo`系统修复 | 正确哈希新二进制已启动（PID 16556）；用户真人定性反馈“没什么太大的问题”并授权转下一步；未回传F44R1-A至D完整结构化矩阵 | **当前阻断门槛按用户定性烟雾可接受关闭；不重复Check/Apply/编译/启动，不扩写成全职业矩阵PASS** |
| 12 | G23 Lua与自动拾取可靠性 | F45正常；P2R1用户确认`.tp`菜单修复成功；P3A服务器助手+完整GM帮助本地/ZIP PASS | **当前按10号执行P3A Check→停服→Apply→正常启动；无需SQL/编译，禁止.reload eluna** |

### 当前不要混淆的三条线

1. **G11第2步human最小验收已经通过**；当前不重复Apply、编译或探针。完整T1–T10/性能仍待补做，G11第3步暂不启动。
2. **G17-P0、G17-A第17步、第18步真实Apply及第18R步恢复验证均已通过并归档**；不得重复v1/v2探针、第17步、原第18步或第18R步。第18R确认postimage、源码备份、exe/PDB备份、conf哈希/BOM/唯一默认关闭行全部正确。
3. G17旧01号方案只能作历史取证：禁止按错误DBC掩码、纯SQL、`CanFlyInZone`恒真或全局跳过属性施工；当前以02号计划、04号专项说明和统一入口为准。
4. 第20B-S已PASS并关闭；第20C-1也已按用户真实摘要归档并关闭：主城内召唤拒绝、从外飞入暴风城落地、PVP战场表现相同。禁止要求重复A3/PVP；A4具体地点/GPS和逐项前后检未在该消息中提供，保持该证据粒度。
5. 御龙术必须分开：普通3.3.5飞行≠R1可控载具骨架≠R5客户端DBC加载修复≠B1全坐骑接管≠B2/B2R1御空体验≠后续独立战斗/玩家施法/自动寻路。R5与B1均已由用户真实Runtime关闭，禁止重跑；B2旧包已经真实部署但Runtime体验FAIL。当前唯一新批次是B2R1：技能2四阶段反馈、技能3七节点曲线与平滑交还、技能4无降落伞类型化多段着陆。
6. PBot当前只算基础上线/手工管理通道，其它战斗、移动、任务、生活、社交和规模AI均未完成；NPCBot也不得因源码或计划文件存在而宣称完全完善。2026-08-22固定状态为`BOT_MASS_RAID_ONE_CLICK=NOT_IMPLEMENTED`、`NPCBOT_CONTEXT_GEAR_ADAPTATION=NOT_IMPLEMENTED`、`BOT_AUTO_ROLE_SELECTION_AND_FILL=NOT_IMPLEMENTED`和`MASS_RAID_BOSS_SCALING_PLAYABILITY=NOT_IMPLEMENTED`（后两者实现域别名分别为`BOT_ROLE_AUTO_ASSIGN_AND_FILL`和`BOT_INSTANCE_SCALING_CONTROL`）。装备线加入BASE临时覆盖/恢复和防复制；职责线加入真实能力识别、自动选职/补缺/备援；缩放线加入有效参战人数、递减曲线、目标TTK、防重复乘法和数值硬上限。顺序仍在G17-B0/B1后、超大团队线前。
7. G19 SQL不重跑；G16每entry十条与传说过滤仍未完成，不能被G17排期覆盖或误写完成。
8. 最终目标是完整定制客户端，不是永远停在纯服务端绕行：人物模型、ADT/WMO城市、新可玩种族、新职业、新地图/新世界、NPC专用种族和真龙主线都必须双端同步落地，并持续建立12.x差分账。主城落地只属G17-A安全一期；最终完成WMO/碰撞/Area/空域与客户端动态飞行后要实现真正全世界可飞。21/32槽等旧结论留到CP0实测裁决。
9. 旧F44全部关闭且真人FAIL。F44R1本地回归、Windows Check/Apply及VS2022 `RelWithDebInfo|x64`增量编译均PASS；两个cpp真实进入编译，新exe=`36734464`字节/SHA `07a8f952...a86c1`，PDB=`433344512`字节。该exe已以PID 16556真实运行，用户真人定性反馈“没什么太大的问题”并授权转下一步；没有A至D逐项数量/时序，不能补写全职业结构化矩阵PASS。治疗默认值不全局抬高，现用`.buff scene`选择副本/团本/高级团本档；个性阈值另立可回滚批次。

当前 G16 的最终规则已经确定，后续 C++ 包将加入：

```ini
AuctionHouseBot.Items.MinCopiesPerEntry = 10
```

验收含义：

```text
每个 eligible itemEntry >= 10 条有效独立 AHBot 挂单
```

注意：这个新配置键尚未安装到用户源码，现在不要提前把它写入运行 conf；必须等真实源码探针回传后的精确 C++ 包。

`item_instance.count` 堆叠总件数只作附加统计；一条 `count=20` 的拍卖仍只算1条独立挂单。

传说品质规则：

```text
只允许 Misc/Pet 和 Misc/Mount
其他传说装备及传说物品全部禁止上架
```

---

## 二、开始前准备

### 1. 确认目录

```text
源码：D:\TrinityCore
编译目录：D:\TC-Build
实际运行目录：D:\TC-Build\bin\RelWithDebInfo
实际配置目录：D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d
Arena导入工作区：C:\Users\Administrator\Downloads\workspace
仓库子目录：C:\Users\Administrator\Downloads\workspace\wow-mecode
上传子目录：C:\Users\Administrator\Downloads\workspace\uploads
```

工作区外层目录已经简化。以后命令统一定义：

```powershell
$Workspace = "C:\Users\Administrator\Downloads\workspace"
$Repo = Join-Path $Workspace "wow-mecode"
$Uploads = Join-Path $Workspace "uploads"
```

### 2. 本次要准备的仓库文件

G11 完整原始交付文件：

```text
tc-bignum\规划\G11_bot自主冒险\install_g11_step2.py
tc-bignum\规划\G11_bot自主冒险\g11_step2_windows.cmd
tc-bignum\规划\G11_bot自主冒险\probe_g11_runtime_v2.py
tc-bignum\规划\G11_bot自主冒险\10-运行探针v1格式缺陷_复盘与v2自检.md
tc-bignum\规划\G11_bot自主冒险\11-v2真实报告首次结果_新进程短窗口无日志.md
tc-bignum\conf\worldserver.conf.d\g11_perception.conf
```

`probe_g11_runtime.ps1` v1已fail-closed，不得再运行。G11安装器/助手只保留作审计和回滚，禁止重复Check/Apply。

G17当前完整交付：

```text
tc-bignum\规划\G17_飞行与移动\01-全世界飞行方案.md（历史取证，禁止施工）
tc-bignum\规划\G17_飞行与移动\02-重新审计_全世界飞行与御龙术分阶段实施计划.md
tc-bignum\规划\G17_飞行与移动\03-v1真实报告诊断与v2探针更正.md
tc-bignum\规划\G17_飞行与移动\证据\g17_source_probe_v1_20260821_123340.txt
tc-bignum\规划\G17_飞行与移动\证据\g17_source_probe_v2_20260821_125214.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step17_selftest_check_pass_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step18_apply_conf_validator_failure_20260821_151724.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step18r_recovery_verify_pass_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step19a_build_pass_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step19b_partial_pre_post_behavior_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step19b_default_off_acceptance_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_area_update_aura_recheck_source_audit_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step20a_enable_conf_pass_20260821_164848.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step20bs_enable_smoke_pass_20260821.txt
tc-bignum\规划\G17_飞行与移动\证据\g17a_step20c1_user_summary_pass_20260821.txt
tc-bignum\规划\G17_飞行与移动\probe_g17_source.py（schema-2已完成，禁止重复）
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\G17A_安全全世界飞行_交付包_20260821.zip
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\SHA256SUMS.txt
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\install_g17a_world_flight.py
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\源文件\SpellInfo.cpp
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\SpellInfo.cpp.g17a.patch
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\g17_world_flight.conf
tc-bignum\规划\G17_飞行与移动\G17A_安全全世界飞行\04-G17A_安全全世界飞行_安装验收与回滚.md
tc-bignum\规划\G17_飞行与移动\G17B0_Narrow_Probe_20260822.zip（已完成；禁止重复）
tc-bignum\规划\G17_飞行与移动\证据\G17B0_LOCK_RESULT_20260822_120243.zip（用户真实原包）
tc-bignum\规划\G17_飞行与移动\证据\g17b0_windows_lock_acceptance_20260822.md
tc-bignum\规划\G17_飞行与移动\证据\G17B0_DB_PROBE_RESULT_20260822_1340.txt（v2真实结果；PASS，禁止重复）
tc-bignum\规划\G17_飞行与移动\证据\g17b0_db_probe_v2_acceptance_20260822.md
tc-bignum\规划\G17_飞行与移动\G17B0_Preflight_20260822.zip（历史冻结；禁止重复）
tc-bignum\规划\G17_飞行与移动\证据\G17B0_SOURCE_APPLY_RESULT_20260822.txt（真实Apply PASS）
tc-bignum\规划\G17_飞行与移动\证据\g17b0_source_apply_acceptance_20260822.md
tc-bignum\规划\G17_飞行与移动\G17B0_Source_Apply_20260822.zip（已执行；禁止重复）
tc-bignum\规划\G17_飞行与移动\G17B0_World_Install_20260822.zip（v2真实1267失败；禁止重跑）
tc-bignum\规划\G17_飞行与移动\G17B0_World_Install_v3_20260822.zip（真实写后检查PASS；禁止重复）
tc-bignum\规划\G17_飞行与移动\G17B0_Windows_Build_20260822.zip（PS1编码ParserError；脚本主体未执行，禁止重跑）
tc-bignum\规划\G17_飞行与移动\G17B0_Windows_Build_v2_ASCII_20260822.zip（native stderr提前catch；MSBuild未开始，禁止重跑）
tc-bignum\规划\G17_飞行与移动\G17B0_Windows_Build_v3_ASCII_NativeSafe_20260822.zip（真实Windows构建PASS；禁止重复）
tc-bignum\补丁库\02_修复\G17R1_御龙入座与纯飞行坐骑\（原件、后文件、源码安装器、客户端DBC补丁器、8项测试）
tc-bignum\规划\G17_飞行与移动\G17R1_Runtime_Fix_Windows_20260822.zip（真实Windows构建PASS；禁止重复）
tc-bignum\规划\G17_飞行与移动\G17R1_Client_MPQ_Install_Windows_20260822.zip（有效客户端包；454641字节；SHA f59513e9...0e5ec；实际安装报告仍UNKNOWN，不重复执行）
tc-bignum\补丁库\02_修复\G17R2_纯飞行坐骑服务端严格门\（前/后镜像、幂等安装器、10项测试）
tc-bignum\规划\G17_飞行与移动\G17R2_Pure_Flying_Server_Gate_Windows_20260823.zip（Windows PASS用户确认；禁止重复；75105字节；SHA e63724d3...c003d）
tc-bignum\规划\G17_飞行与移动\G17R2A_Flight_Gate_Diagnostic_Windows_20260823.zip（真实报告PASS；阶段关闭，禁止重跑）
tc-bignum\补丁库\02_修复\G17R3_客户端旧世界AreaTable飞行门\（真实Windows安装PASS但客户端单标志不完整；服务端实时户外安全门保留）
tc-bignum\规划\G17_飞行与移动\G17R3_Pure_Flight_Client_AreaTable_Windows_20260823.zip（已执行；禁止重跑；客户端部分被R4取代）
tc-bignum\补丁库\02_修复\G17R4_客户端旧世界双飞行标志与双槽加载\（原版/R3/R4 Area、确定性补丁器与逐行测试）
tc-bignum\规划\G17_飞行与移动\G17R4_Pure_Flight_Dual_Area_Flags_Windows_20260823.zip（历史R4；真实安装PASS、Runtime FAIL）
tc-bignum\规划\G17_飞行与移动\G17R5_Effective_zhCN_Locale_DBC_Mirror_Windows_20260823.zip（历史R5；真实Runtime PASS，禁止重复）
tc-bignum\规划\G17_飞行与移动\G17B1_All_Mounts_Auto_Intercept_Windows_20260823.zip（旧安装器`$Args`冲突卡死；永久废弃，禁止重跑）
tc-bignum\规划\G17_飞行与移动\G17B1R1_Native_Runner_Fix_Windows_20260823.zip（真实构建已完成；最终时间戳断言假FAIL；禁止重跑）
tc-bignum\补丁库\02_修复\G17B1R2_PDB前时间戳快照假失败修复\（规范脚本修正；本次用户无需执行）
tc-bignum\补丁库\02_修复\G17B1R3_权威座位占用验证与误清理修复\（座位修复Runtime行为PASS）
tc-bignum\规划\G17_飞行与移动\G17B1R3_Authoritative_Seat_Verification_Windows_20260823.zip（已执行；禁止重复）
tc-bignum\补丁库\02_修复\G17B1R4_室内实时安全清理修复\（真实室内Runtime PASS；已被B1R5取代，禁止重复）
tc-bignum\规划\G17_飞行与移动\G17B1R4_Continuous_Indoor_Safety_Windows_20260823.zip（已执行；禁止重复）
tc-bignum\补丁库\02_修复\G17B1R5_包装坐骑与无降落伞清理修复\（已验收关闭的B1R5基线；禁止重复）
tc-bignum\规划\G17_飞行与移动\G17B1R5_Wrapper_Mount_No_Parachute_Windows_20260823.zip（已验收关闭；禁止重复）
tc-bignum\补丁库\02_修复\G17B2_完整御空术与特色降落\（历史B2；真实部署后Runtime体验FAIL，禁止重跑）
tc-bignum\规划\G17_飞行与移动\G17B2_Complete_Flight_Typed_Landing_Windows_20260823.zip（历史失败基线；禁止重跑）
tc-bignum\补丁库\02_修复\G17B2R1_三技能体验重制\（B2前像、B2R1后像、安全回滚、SQL、52项测试与证据）
tc-bignum\规划\G17_飞行与移动\G17B2R1_Runtime_Experience_Rework_Windows_20260824.zip（真实Windows构建已PASS；禁止重跑，当前只做客户端Runtime）
tc-bignum\规划\G17_飞行与移动\G17_通用御龙与战斗坐骑总设计_20260823.md
tc-bignum\规划\G17_飞行与移动\证据\g17r1_runtime_vehicle_pass_and_g17r2_root_cause_20260823.md
tc-bignum\规划\G17_飞行与移动\证据\g17r2_local_package_selftest_20260823.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r2_windows_package_delivery_20260823.md
tc-bignum\规划\G17_飞行与移动\证据\g17r2_windows_pass_runtime_still_not_here_20260823.md
tc-bignum\规划\G17_飞行与移动\证据\g17r2a_gate_diagnostic_delivery_20260823.md
tc-bignum\规划\G17_飞行与移动\证据\G17R1_WINDOWS_AND_CLIENT_STAGE_RESULT_20260822.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r1_windows_build_client_stage_acceptance_20260822.md
tc-bignum\规划\G17_飞行与移动\证据\g17r1_client_mpq_install_delivery_20260822.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r1_linux_syntax_warning_compile_20260822.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r1_real_zhcn_spell_dbc_patcher_validation_20260822.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r1_windows_package_selftest_20260822.txt
tc-bignum\规划\G17_飞行与移动\证据\g17r1_runtime_fix_delivery_20260822.txt
```

G16 取证文件：

```text
tc-bignum\规划\G16_bot经济与打工\12-共享拍卖行_只读诊断.sql
tc-bignum\规划\G16_bot经济与打工\14-拍卖行每物品库存与传说过滤_只读诊断.sql
tc-bignum\规划\G16_bot经济与打工\probe_g16_min_stock.py
```

G22长期客户端权威规划（当前只阅读，尚未进入CP0施工）：

```text
tc-bignum\规划\G22_客户端魔改整合\02-完美客户端总纲_模型城市种族职业世界主线.md
```

F44 `.combo`完整实施包：

```text
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\00-调查结论与修复设计.md
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\04-F44安装编译验收与回滚.md
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\install_f44_combo.py
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\ORIGINALS\（三个真实源文件+修复前生成器）
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\源文件\（三个完整后文件）
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\tools\（受控JSON+同步生成器）
tc-bignum\补丁库\02_修复\F44_combo道标与全职业目标策略\tests\run_f44_tests.sh
```

F44R1真人阻断修复包（当前权威执行包）：

```text
F44R1_delivery_20260821.zip（138224字节，SHA-256 0fb9f53fd2131701bf3b932117af4e3918a56e4dc8d354ae028737782be5c67f）
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\README.md
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\01-真人失败根因与全职业审计.md
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\02-安装编译与真人验收.md
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\install_f44r1_combo.py
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\原始文件\F44_真人缺陷版\（三个锁定前像）
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\源文件\（三个完整后文件）
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\工具\（受控JSON+生成器）
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\tests\run_f44r1_tests.sh
tc-bignum\tools\specdata_v3_base.json
tc-bignum\tools\gen_specdata_v3.py
```

旧F44目录及`01/02/probe/证据`继续保留作历史取证；旧探针、原件上传、旧Check/Apply/编译/启动均已关闭，不再执行。

### 3. 本轮禁止事项

- 不执行 `git reset --hard`；
- 不执行 `git clean`；
- 不覆盖整个 `D:\TrinityCore`；
- 不手工绕过安装器哈希；
- **不再运行 G11 `--apply`；四个备份已存在**；
- 不重复运行已经完成的 G19 安装脚本；
- 不重新安装 AHBot 0/0/250 conf；
- 不把 `.ahbot status` 的三行相加；
- 不把约99,250总量当作 G16 最终验收通过。

---

> **G17-B1已正式关闭：B1R5真实Windows部署、包装坐骑（含无头骑士）与室内无降落伞退出均由用户确认PASS。禁止重跑B1/B1R1–R5或客户端R1–R5。**

# 当前唯一执行：G17-B2R3 技能4施放修复 + 技能3防倒车（B2R2 用户真实运行后）

> B2R2 真实 Windows 一键运行：安装/编译 PASS、34/34 自检全过；用户实测技能2/3基本OK、**技能4完全无法使用**、技能3偶尔先倒一小段再冲刺。R3 修复这两项。

## 用户确认的环境路径

```text
客户端  D:\WOW
服务端  D:\TC-Build
源码    D:\TrinityCore
工作区  C:\Users\Administrator\Downloads\workspace
```

## 已锁定结果

```text
G17B2R2_WINDOWS_BUILD=PASS_USER_CONFIRMED          (上一批，保留)
G17B2R2_RUNTIME_SKILL2=OK_USER
G17B2R2_RUNTIME_SKILL3=OK_USER_BUT_OCCASIONAL_REVERSE
G17B2R2_RUNTIME_SKILL4=FAIL_COMPLETELY_UNUSABLE    (user-fixed root cause below)
G17B2R3_PRE_SHA256=3b92e815dc81ade4aa9927c19716dabddb8e8f93a6d0aff8b32c80dfbcbfc7f1  (R2后像)
G17B2R3_POST_SHA256=98446106309b45371f138d9c7bc707ee608d9a3db347e13d61cfd68cc97810f9
G17B2R3_SAFE_ROLLBACK_SHA256=3b92e815dc81ade4aa9927c19716dabddb8e8f93a6d0aff8b32c80dfbcbfc7f1  (R2)
G17B2R3_TESTS=PASS_42_OF_42
G17B2R3_ROOTCAUSE_SKILL4=Spell.dbc(52226) RequiresSpellFocus=1553 + CasterAuraSpell=52255 -> Spell::CheckCast rejects BEFORE script hooks; spell_dbc table has no such columns -> C++ runtime sanitizer (EnsureLandingCommandCastable) is the fix
G17B2R3_ROOTCAUSE_SKILL3=path sampled before old motion cleared -> old spline finalized back-most endpoint -> client reverse; fixed by StopMoving() before sample
G17B2R3_UPGRADE_SOURCES=3b92e815(R2)_ff185d99(B2R1)_3e4590da_61342067_03dd649d_adedfc58
G17B2R3_ZIP=G17B2R3_Runtime_Final_Fix_Windows_20260824.zip
G17B2R3_ZIP_SHA256=6ddd8afe5f0b42f663d6fa53afebf0cf80de6ace286c3172b507ff362fe65bdb
G17B2R3_WINDOWS_BUILD=PENDING_USER
G17B2R3_RUNTIME=PENDING_USER
```

## 本批修复

- **技能4（权威）**：`G17Dragonriding::EnsureLandingCommandCastable()` 在服务端启动时（AddSC + 乘客上车双保险）把 52226 的 SpellInfo 运行时清零 `RequiresSpellFocus/CasterAuraSpell/姿态/物品/试剂/图腾` 全部门槛；客户端 DBC/按钮/名称不动；OnCheckCast/AfterCast 保留。启动日志两行证明：
  `>> G17-B2R3 landing command 52226 cast-gates cleared (focus/aura/item/stance); ...`
  `>> G17-B2R3 dragonriding LOADED  build=20260824-r3 (skill4 castable + skill3 anti-reverse)`
- **技能3**：`StartForwardClimb()` 重排为 先 `RestoreClientFlightControl(true)`+`me->StopMoving()`（结算旧运动端点）→ 锁朝向 → 再取样路径 → 启动，消除"先倒车再冲刺"。

## 执行步骤（用户Windows，一键） B2R3

1. 完全退出 worldserver（任务管理器确认无 worldserver.exe）。
2. 解压`G17B2R3_Runtime_Final_Fix_Windows_20260824.zip`到无中文/空格目录。
3. 双击`01_Install_Build_G17B2R3.cmd`。看到`[G17B2R3] INSTALL/BUILD PASSED`；FAILED则把`uploads\G17B2R3_WINDOWS_BUILD_RESULT.txt`回传。
4. 启动`D:\TC-Build\bin\RelWithDebInfo\worldserver.exe`，worldserver.log 必须出现两行（逐字）：
   `>> G17-B2R3 landing command 52226 cast-gates cleared (focus/aura/item/stance); ...`
   `>> G17-B2R3 dragonriding LOADED  build=20260824-r3 (skill4 castable + skill3 anti-reverse)`
   并核对结果文件 `SOURCE_SHA256_AFTER=98446106...10f9`。
5. 客户端`D:\WOW`完全重启；本批不改客户端/MPQ。
6. 户外空旷处召唤坐骑，按下面A–C回传。

## 最短Runtime验收（只回三行）

```text
A 技能2：特效是否明显 / 重复按是否被拒
B 技能3：后退中/刹车中按，是否还有"先倒车再冲刺"
C 技能4：魔法/龙/机械/猛兽各试一次，是否都能分类型着陆且无降落伞
```

回滚（仅新回归时）：双击`02_Rollback_G17B2R3.cmd`，回到 R2 字节像（3b92e815...）。未知SHA零写入拒绝。

---
---

# 上一批：G17-B2R1客户端Runtime短验收（已由B2R2取代，仅供溯源）

> B2旧包已真实部署，技能3向前/向上和控制恢复由用户确认PASS；但技能2高速反馈、高速转向以及技能4类型化着陆被真实Runtime否决。**不要重跑B2旧ZIP。**

## 已锁定结果

```text
G17B1_OVERALL_STATUS=CLOSED_PASS
G17B2_WINDOWS_DEPLOYMENT=PASS_PROVEN_BY_RUNTIME
G17B2_SKILL3_FORWARD_UP_AND_CONTROL=PASS_USER_CONFIRMED
G17B2_OVERALL_RUNTIME=FAIL_EXPERIENCE_REFINEMENT_REQUIRED
G17B2R1_PRE_SHA256=8b47a5b507bc281198363972e10f91ab0ed3784ad920cf810bd20eacfb6ec1d5
G17B2R1_POST_SHA256=ff185d9987b8f4457d8380e1c662cd0313b33a7ae4be6b82974e7702d1fdc4fc
G17B2R1_SAFE_ROLLBACK_SHA256=e298a856edcf366b09934c3635ea8493b6d4e529d9fa2dbf2de2bce77b5b0203
G17B2R1_WORLD_SQL_SHA256=b4526c241e8c8a012f3463038c40d8ca251749f91305939449b1098732e88b66
G17B2R1_GCC14_CPP20_PAYLOAD=PASS_ZERO_DIAGNOSTICS
G17B2R1_GCC14_CPP20_SAFE_ROLLBACK=PASS_ZERO_DIAGNOSTICS
G17B2R1_TESTS=PASS_52_OF_52
G17B2R1_UNKNOWN_SHA_REJECTION=PASS_ZERO_WRITES
G17B2R1_WORLD_MODEL=PASS_LEGACY_REPEAT_FOREIGN_CONFLICT
G17B2R1_POWERSHELL_AST=PASS_BOTH_ZERO_ERRORS
G17B2R1_WINDOWS_BUILD_RESULT=PASS_USER_REPORT
G17B2R1_SOURCE_APPLY_GATE=PASS
G17B2R1_WORLD_SQL_GATE=PASS
G17B2R1_RUNTIME=PENDING_USER
```

## 本批改变

- **技能2**：启动Kit 44；加速中每700ms Kit 696；达到1150%时Kit 13709+极速提示；结束Kit 13709+结束提示。推进期间拒绝重复施放，不新增Aura/伤害/叠速，保留250%–1200%硬上限。
- **高速转向**：删除旧1.25倍硬切；速度越高转向率由1.00平滑收敛到0.82，每tick最多变化0.04，避免锐角折线且不变迟钝。
- **技能3**：保留已通过的向前/向上和控制恢复；改为7节点Catmull-Rom曲线，单次偏航不超过0.70弧度，20/12/7码三级LOS回退，末端切线平滑写回后交还客户端。
- **技能4**：动作条第4格从已知降落伞法术53208迁移到真实项目DBC中无视觉、Dummy、无Aura的52226。魔法/风保持平姿；龙45码长斜坡；机械/火箭分段反推；猛兽无火焰拱形扑落；通用类型前向避障。全部类型落地前归一Vehicle、速度、姿态、重力、fall信息和控制。
- **安全回滚**：不会恢复53208或猛兽喷火Kit 14475；回滚到安全下限源码并保持同一World安全迁移。

## 已执行PASS的冻结ZIP（禁止重跑）

```text
tc-bignum\规划\G17_飞行与移动\G17B2R1_Runtime_Experience_Rework_Windows_20260824.zip
size=73526
files=18
sha256=94822d393236db850b82972062281ec12cb709b75d70b8f56885b9a018aa8daa
```

已执行入口：`01_Install_Build_G17B2R1.cmd`（禁止重跑）  
实际小结果：`C:\Users\Administrator\Downloads\workspace\uploads\G17B2R1_WINDOWS_BUILD_RESULT.txt`

用户报告已证明：源码精确从B2前像应用到B2R1后像；World迁移PASS且52226动作/脚本各1、旧53208动作/脚本均0；MSBuild exit 0；fresh OBJ=1；新EXE SHA=`6a3d9d55...2f403`；最终`G17B2R1_WINDOWS_BUILD_RESULT=PASS`。

## 当前唯一执行步骤

1. 不再运行安装CMD；完全退出旧worldserver进程。
2. 启动新构建的`D:\TC-Build\bin\RelWithDebInfo\worldserver.exe`，不要使用可能指向旧目录的快捷方式或旧服务。
3. 完全重启现有R5客户端；本批`MODIFIES_CLIENT=False`，不安装新MPQ、不改R5 `Data\zhCN\patch-zhCN-Y.MPQ`。
4. 在户外空旷处正常召唤已学坐骑进入G17 Vehicle；动作条第4格应显示52226“飞行器着陆”，而不是旧降落伞动作。
5. 按下方A–E做最短Runtime；普通高速转向与技能3曲线/交还要分开观察，以便定位仍存在的锐角来自哪一层。

## 最短Runtime验收（只回五行）

```text
A 技能2：启动/持续加速/进入极速/结束四段反馈是否都清楚；重复按是否被拒绝；结束后是否无残留
B 高速转向与技能3分开看：先只用技能2高速普通左右转向是否连续；再按技能3看7节点爬升是否无锐角、结束交还是否无跳角，向前/向上和控制恢复是否正常
C 技能4-魔法或风：是否保持平姿托举落地；全程无降落伞
D 技能4-龙：是否长斜坡缓降、不垂直撞地/撞墙；全程无降落伞
E 技能4-机械或猛兽：机械反推角度是否顺滑，或猛兽是否扑落且绝不喷火；落地后Vehicle/速度/姿态/重力/控制是否正常
```

不用回传ZIP、完整日志或大矩阵。失败只需指出`A/B/C/D/E + 坐骑名 + 现象`；成功也只需五行。

## 安全回滚

仅B2R1出现新回归时，停服后双击同目录`02_Rollback_Build_G17B2R1.cmd`。它会运行同等包/测试/SQL/构建门，并输出：

```text
C:\Users\Administrator\Downloads\workspace\uploads\G17B2R1_WINDOWS_ROLLBACK_RESULT.txt
G17B2R1_WINDOWS_ROLLBACK=PASS
```

回滚是安全下限，不恢复53208降落伞或14475喷火。任何未知源码SHA都会被拒绝且零写入。

B2R1真实Runtime通过后才进入B3独立玩家/移动/类型攻击技能页；完整B3–B6完成后才恢复PBot/NPCBot优化。

# Windows待安装：G23-P3A Lua服务器助手与GM帮助完善

```text
G23P3A_delivery_20260822.zip（34756字节）
SHA-256 fc9d850f9b0a2cf0c4f1518b44c55f96a78423a6bb09cc76ac30074876415270
tc-bignum\规划\G23_Lua功能与自动拾取可靠性\10-G23-P3A-Lua服务器助手与GM帮助完善.md
```

唯一顺序：

1. 双击`G23P3A_Check.cmd`，必须为`G23P3A_STATE=READY_TO_APPLY`；
2. 正常停服；
3. 双击`G23P3A_Apply.cmd`；
4. 正常启动worldserver；
5. 简短确认`.server`、原生`.server info`、`.gmhelp`、`.gmhelp find pbot`和`.gmhelp core reload`。

无需SQL、无需VS2022编译，不执行`.reload eluna`。P2/P2R1禁止重复。`G23P3A_Rollback.cmd`仅在新批次异常时使用。

# 已关闭并被动观察：G23-F45自动群拾可靠性

权威入口：

```text
F45_delivery_20260822.zip（38016字节，SHA-256 469e55b028544d31cd9c812269a6229b99e080aef0ab8be76870955cf12982a8）
tc-bignum\规划\G23_Lua功能与自动拾取可靠性\07-G23-F45修复包与Windows实施.md
tc-bignum\补丁库\02_修复\F45_AoE拾取随机漏尸与组队金币语义\安装说明.md
```

用户已明确给出足够症状：同一环境有时完整、有时不完整、有时只剩一具，怪物种类越多越明显。禁止继续要求数尸体或重复`.gps`。

F45已落地：打开合法尸体即触发；金币先聚合后只走一次原分金链；roll/master逐格保护；纯NPCBot不冒充真人roll；邮件只接受明确库存空间不足；增加成功和保留原因计数。Lua不是AoE Loot主链，本包不修改Lua、`.ext`、conf或SQL。

完成状态：

1. [PASS，禁止重复] `F45_Check.cmd`；
2. [PASS，禁止重复] `F45_Apply.cmd`；
3. [PASS，禁止重复] 现有VS2022 RelWithDebInfo/x64 worldserver编译；
4. [PASS，用户正常使用确认] 新二进制已运行，群拾信息可见且观察到的拾取数量一致。

F45不再要求专项测试。任何重复物品/金币、异常邮件、roll物品被直接拿走或崩服，才立即停服并用`F45_Rollback.cmd`精确回滚后重新编译。P2R1已关闭，当前开发线为G23-P3A Lua服务器助手与GM帮助完善。

# 已关闭当前阻断：F44R1 `.combo`增益生命周期与治疗优先

权威逐步文件：

```text
tc-bignum\补丁库\02_修复\F44R1_combo增益生命周期与治疗优先\02-安装编译与真人验收.md
```

当前Windows路径固定为：

```text
C:\Users\Administrator\Downloads\workspace
C:\Users\Administrator\Downloads\workspace\wow-mecode
C:\Users\Administrator\Downloads\workspace\uploads
```

只按权威文件依次执行：

1. [已关闭] 旧worldserver已为Apply正常停服，旧事件队列不再活动；
2. [已PASS，禁止重复] F44R1 Windows真实只读`--check`：三文件均`state=ready`、`CHECK_STATE=ready`、`SOURCE_EDITS=0`；
3. [已PASS，禁止重复] F44R1真实Apply/postcheck：三文件`state=applied`、`SOURCE_EDITS=0`，旧exe/PDB已备份；
4. [已PASS，禁止重复] VS2022 `RelWithDebInfo/x64`增量编译：两个cpp命中，exe/PDB时间前进，新exe SHA=`07a8f952...a86c1`；
5. [已完成到用户定性烟雾粒度] 新二进制PID 16556、SHA=`07a8f952...a86c1`真实运行；用户测试反馈“没什么太大的问题了”并授权转下一步；
6. [证据边界] 未回传A至D逐项次数、时序和全职业矩阵，不补造这些字段；不重复旧F44探针、ZIP、自测、Check、Apply、编译、启动烟雾或G17第20C-1。

F44R1本地门槛、Windows Check/Apply、VS2022增量编译、正确哈希新二进制启动和用户真人定性烟雾均已有证据。当前按用户授权转G23-P0，不再次扰动稳定二进制。现有治疗线按场景递进，游戏内实际入口为`.buff scene`；个性血线与辅助设置见G23规划目录`03-治疗血线与辅助策略后续增强决策.md`，另立独立批次。

# 第零部分（历史已完成）：F44 `.combo`真实源码基线

## 第零A步（已PASS，禁止重复）：真实源码只读探针

先把仓库中的`probe_combo_source.py`单独下载到：

```text
C:\Users\Administrator\Downloads\probe_combo_source.py
```

不要求Windows上存在`wow-mecode`仓库，也不依赖当前PowerShell目录。然后在PowerShell一次性执行下面的**完整原子块**；不要拆成多条逐行继续执行：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Probe  = "$env:USERPROFILE\Downloads\probe_combo_source.py"
    $Out    = "$env:USERPROFILE\Desktop\actual_source_probe_$(Get-Date -Format 'yyyyMMdd_HHmmss').txt"
    $ExpectedProbe = "0b215db4be55f19b85d4112d7a77de0f9851371fff1219296cfcd6dd41969db8"

    foreach ($Path in @($Python, $Probe)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "找不到文件：$Path。请先把probe_combo_source.py下载到Downloads。"
        }
    }
    $ActualProbe = (Get-FileHash -LiteralPath $Probe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ActualProbe -ne $ExpectedProbe) {
        throw "F44探针SHA不符：实际=$ActualProbe，预期=$ExpectedProbe"
    }

    & $Python $Probe --self-test
    if ($LASTEXITCODE -ne 0) { throw "F44探针self-test失败，退出码=$LASTEXITCODE" }

    & $Python $Probe --source-root "D:\TrinityCore" | Tee-Object -FilePath $Out
    if ($LASTEXITCODE -ne 0) { throw "F44真实源码探针失败，退出码=$LASTEXITCODE" }

    $Report = Get-Content -LiteralPath $Out -Raw
    foreach ($Marker in @(
        "READY_FOR_EXACT_FIX=True",
        "SOURCE_OR_CONFIG_EDITS=0",
        "DATABASE_EDITS=0"
    )) {
        if (-not $Report.Contains($Marker)) { throw "报告缺少标志：$Marker" }
    }
    Write-Host "[OK] F44真实源码只读探针完成"
    Write-Host "[OK] 报告路径=$Out"
}
```

期望末尾：

```text
READY_FOR_EXACT_FIX=True
SOURCE_OR_CONFIG_EDITS=0
DATABASE_EDITS=0
```

真实报告`actual_source_probe_20260821_184147.txt`已经回传并PASS；本段禁止重复。结果：

```text
cs_combathelper.cpp = 123792 bytes / 2805de88...566e72d（与仓库一致）
CombatSpecData.h    =   8548 bytes / c01ab4cb...4651ca9d（与仓库一致）
CombatSpecData.cpp  =  62974 bytes / cd0d172e...87c1ebfd（与仓库不同）
READY_FOR_EXACT_FIX=True
SOURCE_OR_CONFIG_EDITS=0
DATABASE_EDITS=0
```

## 第零B步（已PASS，禁止重复）：只读复制真实数据cpp原件

真实原件已经回传并以62974字节/SHA `cd0d172e...ebfd`通过校验，且已归档到F44 `ORIGINALS/`。下方命令仅作历史审计，不再执行：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Source = "D:\TrinityCore\src\server\scripts\Commands\CombatSpecData.cpp"
    $Out = "$env:USERPROFILE\Desktop\CombatSpecData.actual.cd0d172e.cpp"
    $Expected = "cd0d172eb3546b36665174ea155bd8a99e5f245bb6ed3059ce7cec3a87c1ebfd"

    if (-not (Test-Path -LiteralPath $Source -PathType Leaf)) {
        throw "找不到真实源文件：$Source"
    }
    $Before = (Get-FileHash -LiteralPath $Source -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Before -ne $Expected) { throw "真实CombatSpecData.cpp已变化：$Before" }

    Copy-Item -LiteralPath $Source -Destination $Out -Force
    $After = (Get-FileHash -LiteralPath $Out -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($After -ne $Before) { throw "复制后哈希不一致：source=$Before copy=$After" }

    Write-Host "[OK] F44真实CombatSpecData.cpp原件只读复制完成"
    Write-Host "[OK] sha256=$After"
    Write-Host "[OK] 文件=$Out"
}
```

只需回传桌面的`CombatSpecData.actual.cd0d172e.cpp`。另外两个真实文件已与仓库逐字节一致，无需重复上传。

# 第一部分：G11第2步历史安装与验收记录（已完成，当前不要执行）

> **完成状态（2026-08-21）：同一PID 14980的40秒真人刺激返回 `28/28/0/0/True`；用户确认28条human原始日志的 `kind=human`、`readonly=1` 全齐。human最小验收已关闭，下面第1–9C步只保留作审计和回滚，禁止重复Check/Apply、编译、启停服或探针。完整T1–T10/性能另行后补。**

## 第1步（已完成）：只检查，不修改源码

### 历史方法：Windows 双击助手（当前不要执行）

把下面两个文件放在同一个文件夹，例如都放进 Downloads：

```text
install_g11_step2.py
g11_step2_windows.cmd
```

双击：

```text
g11_step2_windows.cmd
```

菜单第一次选择：

```text
1 = CHECK only
```

助手不会使用损坏的 `py -3`，会依次查找 Python 3.14/3.13/3.12/3.11/3.10 等真实 `python.exe`，并排除 WindowsApps 商店占位符。检查结果会保存在同一目录：

```text
g11_step2_check_result.txt
```

2026-08-20 用户已在 Python 3.12.10 下看到：

```text
[OK] Exact hashes and all patch anchors passed. --apply is safe for this source snapshot.
```

这证明当时四文件原始 SHA256 和全部唯一锚点通过；`--check` 没有修改源码。随后进行的 APPLY 也已成功。**当前不要再回到菜单选择 APPLY。**

历史通用命令如下，但本机 `py -3` 已确认错误指向不存在的 Python314，**不得重试**：

```powershell
py -3 "脚本完整路径\install_g11_step2.py" "D:\TrinityCore" --check
```

### 手工命令出现 `Unable to create process`

这表示 Python 或命令路径有问题，**还没有进入安装器的源码检查**。先检查命令中脚本文件名。

正确文件名必须完整等于：

```text
install_g11_step2.py
```

以下是被网页/聊天软件自动链接污染的错误形式，绝对不能使用：

```text
install_g11_[step2.py](http://step2.py)
```

也不要把 Git Bash 提示符 `$` 一起复制进命令。

推荐改用 **PowerShell**，先执行：

```powershell
$Script = "C:\Users\Administrator\Downloads\install_g11_step2.py"
$Source = "D:\TrinityCore"
$Python314 = "$env:LOCALAPPDATA\Programs\Python\Python314\python.exe"

Test-Path $Script
Test-Path $Source
Test-Path $Python314
```

前两个结果必须为 `True`。

如果第三个也是 `True`，绕过损坏的 `py` 启动器，直接执行：

```powershell
& $Python314 --version
& $Python314 $Script $Source --check
```

如果第三个是 `False`，说明 `py -3` 记录的是已经不存在的 Python 3.14。用户当前机器已确认存在：

```text
C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe
C:\Users\Administrator\AppData\Local\Programs\Python\Python310\python.exe
```

本批优先使用 Python 3.12；`WindowsApps\python.exe` 是商店占位符，不得使用。若以后环境变化，再用下面命令查找仍然存在的 Python：

```powershell
Get-Command python.exe -All -ErrorAction SilentlyContinue | Select-Object -ExpandProperty Source
Get-ChildItem "$env:LOCALAPPDATA\Programs\Python" -Filter python.exe -Recurse -File -ErrorAction SilentlyContinue | Select-Object -ExpandProperty FullName
```

当前机器直接执行：

```powershell
$Script = "C:\Users\Administrator\Downloads\install_g11_step2.py"
$Source = "D:\TrinityCore"
$Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"

Test-Path -LiteralPath $Script
Test-Path -LiteralPath $Source
Test-Path -LiteralPath $Python

& $Python --version
& $Python $Script $Source --check
```

这里特意使用 `-LiteralPath`，避免路径中出现方括号时被 PowerShell 当成通配符。三个结果必须全部为 `True`。

如果没有任何真实 `python.exe`，先修复或重新安装 Python，再继续；不要直接做 `--apply`。

如果仍想在 Git Bash 执行，并且 `python --version` 能成功，可使用正斜杠路径：

```bash
python "C:/Users/Administrator/Downloads/install_g11_step2.py" "D:/TrinityCore" --check
```

### 如果安装器已经启动，但报告 hash/anchor 检查失败

立即停止，不要手工改哈希或锚点。完整回传：

- PowerShell 全部输出；
- 当前 `git branch --show-current`；
- 当前 `git rev-parse HEAD`；
- 不要只截最后一行。

## 第2步（已完成）：安装四个 G11 源码改动

> 用户已完成本步。以下只保留历史命令和产物说明；**不要再次执行 APPLY**。

当时第1步通过后使用 `g11_step2_windows.cmd` 的 APPLY 菜单，完整输出可保存到：

```text
g11_step2_apply_result.txt
```

当时使用真实 Python 的等效命令为（仅作记录，当前不要执行）：

```powershell
& $Python $Script $Source --apply
```

必须看到：

```text
[OK] G11 step 2 installed into four source files.
```

安装器会自动修改：

```text
src/server/game/AI/NpcBots/bot_ai.h
src/server/game/AI/NpcBots/bot_ai.cpp
src/server/game/AI/NpcBots/botconfig.h
src/server/game/AI/NpcBots/botconfig.cpp
```

同时为四个文件建立：

```text
.g11_step2.bak
```

2026-08-20 用户侧 `--apply` 已成功，记录如下：

```text
bot_ai.h       f2e85fe100db83dcaae0ce3c4ecb6b72a19e19eb7e7de80089cfdfa675fe80f6
bot_ai.cpp     d34ad76b1fcfdbacfe0099fb791119993309d2030db0940feddc5b7bdb75e2cf
botconfig.h    6b09154b3566da76bf8aea08ebec791fd38709cd13c11386eb57f8bb55aae58e
botconfig.cpp  6ca80f8b201c90585cf5fd3fcc65eefeda1e02452ae07b07c4902bf12082bcf6
```

四个 `.g11_step2.bak` 均已创建。不要重复执行 `--apply`。

## 第3步：复制 G11 独立配置

来源文件：

```text
tc-bignum\conf\worldserver.conf.d\g11_perception.conf
```

先把该 conf 下载到 `C:\Users\Administrator\Downloads`，然后在 PowerShell 执行：

```powershell
$ConfSource = "C:\Users\Administrator\Downloads\g11_perception.conf"
$ConfDir = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d"
$ConfDest = Join-Path $ConfDir "g11_perception.conf"

Test-Path -LiteralPath $ConfSource
Test-Path -LiteralPath "D:\TC-Build\bin\RelWithDebInfo"

New-Item -ItemType Directory -Path $ConfDir -Force | Out-Null
Copy-Item -LiteralPath $ConfSource -Destination $ConfDest -Force
Get-Item -LiteralPath $ConfDest | Select-Object FullName, Length, LastWriteTime
Get-Content -LiteralPath $ConfDest |
    Where-Object { $_ -match '^NpcBot\.Autonomy\.Perception\.' }
```

前两个 `Test-Path` 必须为 `True`。最终文件必须是：

```text
D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g11_perception.conf
```

2026-08-20 用户已复制成功：1258字节，时间 `2026/8/20 15:54:47`。首次筛选输出只显示 Enable/Range/IntervalMax/LogCooldown，因此编译前还要用精确检查确认 `IntervalMin=4000` 与 `ReadOnly=1`；不要凭缺行输出直接假定。

第一次编译和部署前保持：

```ini
NpcBot.Autonomy.Perception.Enable = 0
NpcBot.Autonomy.Perception.Range = 60
NpcBot.Autonomy.Perception.IntervalMin = 4000
NpcBot.Autonomy.Perception.IntervalMax = 8000
NpcBot.Autonomy.Perception.LogCooldown = 15000
NpcBot.Autonomy.Perception.ReadOnly = 1
```

复制后运行六项精确检查：

```powershell
# 自包含：即使换了 PowerShell 窗口，也不依赖旧会话中的变量。
$ConfDest = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g11_perception.conf"
if (-not (Test-Path -LiteralPath $ConfDest -PathType Leaf)) {
    throw "G11 conf not found: $ConfDest"
}

$Required = @(
    "NpcBot.Autonomy.Perception.Enable = 0"
    "NpcBot.Autonomy.Perception.Range = 60"
    "NpcBot.Autonomy.Perception.IntervalMin = 4000"
    "NpcBot.Autonomy.Perception.IntervalMax = 8000"
    "NpcBot.Autonomy.Perception.LogCooldown = 15000"
    "NpcBot.Autonomy.Perception.ReadOnly = 1"
)
$Content = @(Get-Content -LiteralPath $ConfDest)
$Missing = @($Required | Where-Object { $Content -notcontains $_ })
if ($Missing.Count -eq 0) {
    "[OK] All six G11 settings are exact."
} else {
    "[STOP] Missing or changed settings:"
    $Missing
}
```

只有看到 `[OK] All six G11 settings are exact.` 才进入编译。若旧命令提示 `LiteralPath` 参数为空，只表示当前 PowerShell 会话没有 `$ConfDest` 变量，不会修改或损坏任何文件；重新执行上面的自包含命令即可。`ReadOnly` 必须为1；C++ 即使读到0也会强制恢复为1，不会解锁行为层。

## 第4步：增量编译

本批只修改四个已经存在的 `.h/.cpp`，**不需要重跑 CMake，也不要 Clean/Rebuild**。直接：

1. 打开 `D:\TC-Build\TrinityCore.sln`；
2. 顶部选择 `RelWithDebInfo`；
3. 平台选择 `x64`；
4. 按 `Ctrl + Shift + B`，即“生成解决方案”。

编译前不要删除 G19 已经安装的代码。新产物应同时包含：

```text
G19 情境对话第3步
G11 只读感知第2步
```

2026-08-20 用户已确认六项配置检查正确，且 `D:\TC-Build\TrinityCore.sln` 的 RelWithDebInfo/x64 增量编译成功；具体 VS 最终成功/失败/跳过计数尚未回传。

### 编译失败时回传（本次未触发）

必须回传：

1. 第一条真正的 C/C++ 错误；
2. 文件名和行号；
3. 错误前后至少20行；
4. 完整错误列表开头部分。

不要只回传最后的：

```text
LNK1181 game.lib
```

它通常只是前面编译失败造成的连锁结果。

---

# 第二部分：部署包含 G19 + G11 的新二进制

## 第5步（历史已完成）：确认新编译产物

本项目的 RelWithDebInfo 构建输出和实际运行目录都是：

```text
D:\TC-Build\bin\RelWithDebInfo
```

因此本次编译成功后，新 `worldserver.exe` 已直接生成到实际运行目录，**不要再从同一路径复制到自己，也不要使用 Bash 路径转换**。继续用 Windows PowerShell：

```powershell
$RunDir = "D:\TC-Build\bin\RelWithDebInfo"
$Exe = Join-Path $RunDir "worldserver.exe"
$Pdb = Join-Path $RunDir "worldserver.pdb"

Test-Path -LiteralPath $Exe
Test-Path -LiteralPath $Pdb
Get-Item -LiteralPath $Exe, $Pdb |
    Select-Object FullName, Length, LastWriteTime
Get-FileHash -LiteralPath $Exe -Algorithm SHA256
```

两个 `Test-Path` 应为 `True`，`worldserver.exe`/PDB 时间应是本次编译时间。若已有旧版 `*.before_g11_step2.bak`，保留它且不要覆盖；如果编译前没有旧二进制备份，现在不能把新 exe 冒充旧备份，源码仍可通过 `.g11_step2.bak` 回滚后重新编译。

## 第6步：无需额外复制

因为构建输出就是实际运行目录，本次没有单独的“复制新 exe”动作。只需确认第5步的时间和哈希，然后进入首次启动。

## 第7步（已完成）：第一次启动保持 G11 关闭

2026-08-20 用户确认新 worldserver 能够启动；未回传完整启动日志，因此只记录“基础启动成功”，不把 G19/G11 游戏内验收误记为通过。

当时确认：

```ini
NpcBot.Autonomy.Perception.Enable = 0
```

启动 worldserver，检查：

- 配置文件能正常读取；
- 没有 G11 配置错误；
- 没有 G19 schema/tag 错误；
- worldserver 能正常进入可连接状态；
- 没有新增崩溃或持续刷屏。

---

# 第三部分：G19 运行验收

G19 已经完成：

```text
完整编译成功
数据库字段 4/4
总文本 179
scene 标签 54
G19 场景台词 14/14
cooldown 900–900
```

本次不重复执行 G19 安装脚本和数据库安装 SQL，只做运行验收。

## 第8步：场景检查

至少检查：

| 场景 | 要确认的内容 |
|---|---|
| 主城 | 能选择主城/城市语境台词 |
| 普通野外 | 不错误使用副本语境 |
| 副本 | 能进入副本场景类别 |
| 战场或特殊地图 | 场景分类不崩溃、不串到普通主城 |

同时检查：

- 短时间内不会持续重复同一句；
- 900秒冷却配置被读取；
- 台词池能正常轮换；
- 没有 G19 相关启动错误。

---

# 第四部分：G11 只读运行验收

## 第9步（历史已完成，禁止重复）：开启G11最小只读日志验收

1. 先通过当前 worldserver 控制台正常停服；
2. 继续使用 Windows PowerShell，不用 Bash；
3. 备份当前 Enable=0 配置并打开文件：

```powershell
$Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g11_perception.conf"
Copy-Item -LiteralPath $Conf -Destination "$Conf.enable0.bak" -Force
notepad.exe $Conf
```

只把这一行：

```ini
NpcBot.Autonomy.Perception.Enable = 0
```

改为：

```ini
NpcBot.Autonomy.Perception.Enable = 1
```

`ReadOnly` 必须继续为1。保存后检查：

```powershell
Select-String -LiteralPath $Conf -Pattern '^NpcBot\.Autonomy\.Perception\.(Enable|ReadOnly)\s*='
```

必须显示 `Enable = 1`、`ReadOnly = 1`。然后按刚才成功的相同方式重启 worldserver。

首次只做最小测试：让一个真人角色站到一只**自由游荡且未被招募**的 NPCBot 60码内，等待约10–30秒。先取得1–5行 `[G11-PERCEPTION]` 日志并确认 `kind=human`、`seen=first`、`readonly=1`，同时观察bot没有因G11而移动、说话、攻击、治疗或组队。最小测试通过后才展开T1–T10。

### 第9步启动截取结果（历史阶段；不是当前运行日志结论）

用户上传的 `conf完整日志.txt` 共749行，已确认：

- `g11_perception.conf` 确实作为additional config加载；
- `NPCBots config loaded`、`NPCBots system enabled`；
- 加载179条care texts，G19数据库内容进入运行时；
- 建立750只wandering bot的生成任务；
- `World initialized in 0 minutes 13 seconds`并ready；
- 但 `[G11-PERCEPTION]`、`kind=`、`seen=`、`readonly=`均为0次；
- 上传文件主要是启动阶段，没有能证明真人靠近合格自由游荡bot的测试窗口。

以上只适用于当时的749行启动截取。后续用户已确认 `Enable=1`、活动exe/logger/资格均正确，且v1报告证明至少20条G11日志存在；当前不再按“无日志”分支处理。

## 第9A步：v1探针结果与缺陷（已完成、已停用）

用户回传的v1报告确认：

- 配置、活动进程路径和exe标记均由用户确认正确；
- exe中明确存在 `[G11-PERCEPTION]`；
- 第7节扫描5个日志文件，并输出20行 `Server.log` 路径。

这20行路径不是“0命中”，而是证明v1实际找到了**至少20条**G11日志。旧PowerShell探针把不同对象统一交给延迟表格格式化，导致配置值、logger值、LineNumber和Line正文列被隐藏。v1因此不能用于验收，现已fail-closed；复盘见：

```text
tc-bignum\规划\G11_bot自主冒险\10-运行探针v1格式缺陷_复盘与v2自检.md
```

## 第9B步（历史已完成，禁止重复）：Python v2验收

下载：

```text
tc-bignum\规划\G11_bot自主冒险\probe_g11_runtime_v2.py
```

到：

```text
C:\Users\Administrator\Downloads\probe_g11_runtime_v2.py
```

只使用Windows PowerShell 5.1，不使用Git Bash/CMD/WSL。旧SHA256 `6bb8e1...` 的文件已被Windows self-test证实存在fixture规范化缺陷，必须重新下载。先把预检和self-test放在同一个原子脚本块中，防止中途失败后仍打印完成：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Probe = "C:\Users\Administrator\Downloads\probe_g11_runtime_v2.py"
    $ExpectedSha256 = "5f7a200640d4f2af1571108a11cc4cfb00d861a7874a94f8c77b14e4256b3d98"

    if ($PSVersionTable.PSEdition -ne "Desktop" -or $PSVersionTable.PSVersion.Major -ne 5) {
        throw "当前不是Windows PowerShell 5.1"
    }
    if (-not (Test-Path -LiteralPath $Python -PathType Leaf)) {
        throw "找不到Python 3.12：$Python"
    }
    if (-not (Test-Path -LiteralPath $Probe -PathType Leaf)) {
        throw "找不到新版探针：$Probe"
    }

    $ActualSha256 = (Get-FileHash -LiteralPath $Probe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ActualSha256 -ne $ExpectedSha256) {
        throw "探针SHA256不一致。实际=$ActualSha256，预期=$ExpectedSha256。停止并重新下载。"
    }

    & $Python --version
    if ($LASTEXITCODE -ne 0) { throw "Python启动失败，退出码=$LASTEXITCODE" }
    & $Python $Probe --self-test
    if ($LASTEXITCODE -ne 0) { throw "探针self-test失败，退出码=$LASTEXITCODE" }

    Write-Host "[OK] 第一步完成：环境、SHA256和self-test全部通过"
}
```

必须同时看到：

```text
[OK] Positive fixture + final report rendering passed.
[OK] Negative fixtures: bad prerequisites/no-log and malformed human line did not false-pass.
[OK] G11 runtime probe v2 self-test passed.
[OK] 第一步完成：环境、SHA256和self-test全部通过
```

**现场结果：以上四项已由用户在Windows PowerShell 5.1 / Python 3.12.10完整确认，第一步完成，无需重复。**

然后在worldserver仍运行时生成真实报告。为避免旧报告覆盖或误传，使用时间戳文件名，并在同一个原子块中验证报告结构：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Probe = "C:\Users\Administrator\Downloads\probe_g11_runtime_v2.py"
    $ExpectedSha256 = "5f7a200640d4f2af1571108a11cc4cfb00d861a7874a94f8c77b14e4256b3d98"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Out = "$env:USERPROFILE\Desktop\g11_runtime_diag_v2_$(Get-Date -Format 'yyyyMMdd_HHmmss').txt"

    $ActualSha256 = (Get-FileHash -LiteralPath $Probe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ActualSha256 -ne $ExpectedSha256) {
        throw "探针SHA256变化：实际=$ActualSha256，预期=$ExpectedSha256"
    }

    $Worldservers = @(Get-CimInstance Win32_Process -Filter "Name='worldserver.exe'")
    if ($Worldservers.Count -ne 1) {
        throw "活动worldserver数量必须为1，实际=$($Worldservers.Count)"
    }

    & $Python $Probe $RunDir $Out
    if ($LASTEXITCODE -ne 0) {
        throw "真实只读探针失败，退出码=$LASTEXITCODE"
    }
    if (-not (Test-Path -LiteralPath $Out -PathType Leaf)) {
        throw "报告未生成：$Out"
    }

    $Report = Get-Content -LiteralPath $Out -Raw
    foreach ($Key in @(
        "CONFIG_EXACT",
        "ACTIVE_PROCESS_COUNT",
        "PROCESS_EXPECTED_PATH_MATCH_COUNT",
        "PROCESS_PATH_MATCH",
        "EXE_G11_MARKER",
        "LOGGER_INFO_ALLOWED",
        "G11_LOG_MATCHES",
        "G11_HUMAN_MATCHES",
        "G11_PLAYERBOT_MATCHES",
        "G11_LINES_WITH_MISSING_FIELDS",
        "G11_HUMAN_MINIMUM_PASS"
    )) {
        $Pattern = "(?m)^" + [regex]::Escape($Key) + "="
        if ($Report -notmatch $Pattern) {
            throw "报告缺少字段：$Key"
        }
    }

    $LogCountMatch = [regex]::Match($Report, "(?m)^G11_LOG_MATCHES=(\d+)\r?$")
    if (-not $LogCountMatch.Success) {
        throw "无法解析G11_LOG_MATCHES数值"
    }
    $LogCount = [int]($LogCountMatch.Groups[1].Value)
    if ($LogCount -gt 0 -and $Report -notmatch "(?m)^LOG ") {
        throw "报告有G11命中计数，但没有原始LOG正文"
    }

    Write-Host "[OK] 真实只读报告已生成且结构完整"
    Write-Host "[INFO] 尚未代表G11 human验收通过，必须回传并读取最终判定"
    Write-Host "[OK] 报告路径=$Out"
}
```

v2使用稳定的纯文本 `KEY=VALUE` 协议，直接输出最多50条日志正文，不依赖PowerShell表格宽度。当前human最小通过标准是：

```text
CONFIG_EXACT=True
ACTIVE_PROCESS_COUNT=1
PROCESS_EXPECTED_PATH_MATCH_COUNT=1
PROCESS_PATH_MATCH=True
EXE_G11_MARKER=True
LOGGER_INFO_ALLOWED=True
G11_LOG_MATCHES >= 1
G11_HUMAN_MATCHES >= 1
G11_LINES_WITH_MISSING_FIELDS=0
G11_HUMAN_MINIMUM_PASS=True
```

完整上传终端最后显示的时间戳报告，例如 `g11_runtime_diag_v2_20260820_231500.txt`；不要上传旧的无时间戳报告。

## 第9C步（历史已完成，禁止重复）：同一PID真人最小刺激

首次真实报告 `g11_runtime_diag_v2_20260820_232023.txt` 已确认全部配置/进程/exe/logger前置通过，但 `G11_LOG_MATCHES=0`。进程CreationDate换算为 `2026-08-20 23:19:58.571 +08:00`，报告时进程仅运行约24秒。v1的至少20条命中属于本次重启前日志；重启可能截断或轮转旧Server.log，因此不矛盾。

先在游戏里让真人角色站到已确认的自由游荡、未招募NPCBot 10–30码内，保持双方存活、在世界、不传送；不要招募。然后切回同一个Windows PowerShell窗口，一次性运行以下原子块。它先锁定PID 14980，再自动等待40秒：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $ExpectedPid = 14980
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Probe = "C:\Users\Administrator\Downloads\probe_g11_runtime_v2.py"
    $ExpectedSha256 = "5f7a200640d4f2af1571108a11cc4cfb00d861a7874a94f8c77b14e4256b3d98"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Out = "$env:USERPROFILE\Desktop\g11_runtime_diag_v2_$(Get-Date -Format 'yyyyMMdd_HHmmss').txt"

    $Worldservers = @(Get-CimInstance Win32_Process -Filter "Name='worldserver.exe'")
    if ($Worldservers.Count -ne 1) {
        throw "活动worldserver数量必须为1，实际=$($Worldservers.Count)"
    }
    if ([int]$Worldservers[0].ProcessId -ne $ExpectedPid) {
        throw "worldserver已重启：预期PID=$ExpectedPid，实际PID=$($Worldservers[0].ProcessId)。停止本轮，不混用进程窗口。"
    }

    $ActualSha256 = (Get-FileHash -LiteralPath $Probe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ActualSha256 -ne $ExpectedSha256) {
        throw "探针SHA256变化：实际=$ActualSha256，预期=$ExpectedSha256"
    }

    Write-Host "[INFO] 保持真人与自由NPCBot位置不变，等待40秒..."
    Start-Sleep -Seconds 40

    & $Python $Probe $RunDir $Out
    if ($LASTEXITCODE -ne 0) {
        throw "真实只读探针失败，退出码=$LASTEXITCODE"
    }
    if (-not (Test-Path -LiteralPath $Out -PathType Leaf)) {
        throw "报告未生成：$Out"
    }

    $Report = Get-Content -LiteralPath $Out -Raw
    $Metrics = @{}
    foreach ($Key in @(
        "ACTIVE_PROCESS_COUNT",
        "PROCESS_PATH_MATCH",
        "G11_LOG_MATCHES",
        "G11_HUMAN_MATCHES",
        "G11_PLAYERBOT_MATCHES",
        "G11_LINES_WITH_MISSING_FIELDS",
        "G11_HUMAN_MINIMUM_PASS"
    )) {
        $Pattern = "(?m)^" + [regex]::Escape($Key) + "=([^\r\n]*)\r?$"
        $Match = [regex]::Match($Report, $Pattern)
        if (-not $Match.Success) {
            throw "报告缺少或无法解析字段：$Key"
        }
        $Metrics[$Key] = $Match.Groups[1].Value
        Write-Host "$Key=$($Metrics[$Key])"
    }

    $LogCount = [int]($Metrics["G11_LOG_MATCHES"])
    if ($LogCount -gt 0 -and $Report -notmatch "(?m)^LOG ") {
        throw "报告有G11命中计数，但没有原始LOG正文"
    }

    Write-Host "[OK] 同一PID的40秒真人刺激报告已生成"
    Write-Host "[INFO] 是否通过只以报告内G11_HUMAN_MINIMUM_PASS为准"
    Write-Host "[OK] 报告路径=$Out"
}
```

现场返回：

```text
G11_LOG_MATCHES=28
G11_HUMAN_MATCHES=28
G11_PLAYERBOT_MATCHES=0
G11_LINES_WITH_MISSING_FIELDS=0
G11_HUMAN_MINIMUM_PASS=True
```

机器判定已通过，且计数守恒：`28 = 28 + 0`。文档中的 `>=1` 表示最低门槛，实际28不是差异或错误；`playerbot=0`符合本轮只测真人。用户随后确认28条原始human日志的 `kind=human`、`readonly=1` 均齐全；现仅归档已有报告，不重新运行。

### PBot现在是否需要

**不是当前前置。** G11观察者始终是自由游荡且未招募的NPCBot；human最小验收已经通过。PBot只用于后续可选T7：把无客户端PlayerBot放到该NPCBot 60码内，预期 `kind=playerbot`；真人同时更近时会优先选择真人，所以T7必须单独布置。

## 第10步：确认日志

把真人角色移动到自由游荡 NPCBot 的60码内，预期日志包含：

```text
[G11-PERCEPTION]
```

日志应包含：

```text
bot
name
scene
map
zone
player
playerName
kind
dist
levelDelta
hp
combat
group
candidates
seen
helpCandidate
readonly=1
```

无客户端 PlayerBot 应显示：

```text
kind=playerbot
```

## 第11步：G11 验收表

| 勾选 | 测试 | 预期 |
|---|---|---|
| [ ] | 玩家在60码外 | 不记录该玩家 |
| [ ] | 玩家进入60码 | 出现 `seen=first` |
| [ ] | 玩家原地等待 | 受 LogCooldown 限制后才出现 `seen=again` |
| [ ] | 两名玩家靠近 | 选择最近合法玩家，显示候选数量 |
| [ ] | 玩家战斗且生命低于50% | `helpCandidate=1`，但 bot 不行动 |
| [ ] | 玩家已经组队 | `group=1` |
| [ ] | 无客户端 PlayerBot | `kind=playerbot` |
| [ ] | bot 死亡、传送或不在世界 | 不扫描、不崩服 |
| [ ] | 普通固定/随行 bot | G11 第2步不运行 |
| [ ] | 观察移动、聊天和战斗 | G11 不改变原有行为 |

本阶段即使出现 `helpCandidate=1`，仍然禁止：

```text
移动
说话
攻击
治疗
邀请组队
调用 Group::AddMember
改变羁绊
写数据库
```

## 第12步：性能阶梯

按顺序观察 world update：

```text
10只自由游荡 bot
100只自由游荡 bot
500只自由游荡 bot
玩家密集主城
没有玩家的野外
```

如果开销明显升高，先改为：

```ini
NpcBot.Autonomy.Perception.IntervalMin = 8000
NpcBot.Autonomy.Perception.IntervalMax = 15000
```

不要直接删除感知层。

---

# 第五部分：G16 补货后诊断与源码取证（独立工作流，当前先不执行）

> 这里的SQL `12`/`14`与G11/G17无关，全部是AHBot只读SELECT诊断。G11 human最小验收已过；当前按用户优先级先做G17-P0，本部分继续保留，不得误写完成。

AHBot 当前 conf 已经由用户确认安装：

```ini
AuctionHouseBot.Alliance.Items.Amount.Ratio = 0
AuctionHouseBot.Horde.Items.Amount.Ratio = 0
AuctionHouseBot.Neutral.Items.Amount.Ratio = 250
AuctionHouseBot.Class.Misc = 2
AuctionHouseBot.Update.Interval = 60
```

本次不要重复复制或改回 `100/100/100`。

## 第13步：执行补货后总量诊断

在 DBeaver 中完整执行：

```text
12-共享拍卖行_只读诊断.sql
```

需要回传全部结果集，不要只回传 `.ahbot status` 或总行数。

## 第14步：执行每 entry 与传说过滤诊断

完整执行：

```text
14-拍卖行每物品库存与传说过滤_只读诊断.sql
```

重点回传：

- `distinct_active_entries`；
- `min_copies_per_visible_entry`；
- `pct_meeting_min_10`；
- 库存最低的500种 entry；
- 传说品质全部 entry 清单；
- `forbidden_entries`；
- `forbidden_rows`；
- 允许的传说宠物/坐骑清单。

当前活动橙色基线是：

```text
468条有效挂单
3个不同 itemEntry
```

必须确认它们到底是允许的坐骑/宠物，还是应禁止的传说物品。

## 第15步：运行 G16 只读源码探针

PowerShell：

```powershell
$Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
$G16Probe = "脚本完整路径\probe_g16_min_stock.py"
& $Python $G16Probe "D:\TrinityCore" "D:\probe_g16_min_stock.txt"
```

本机 `py -3` 已确认失效，不得用于G16。

必须看到：

```text
[OK] Wrote complete UTF-8-BOM report: D:\probe_g16_min_stock.txt
[OK] Source files read: 6; source edits: 0
```

完整回传：

```text
D:\probe_g16_min_stock.txt
```

不要删减，不要只截最后几十行。

收到 SQL `12`、SQL `14` 和源码探针报告后，下一批会生成 G16 精确 C++ 安装器，实现：

```text
先把每个 eligible entry 补到至少10条有效独立挂单
然后才进入原品质/类别加权随机填充
过滤全部禁止传说物品
只允许传说 Misc/Pet 与 Misc/Mount
forceIncludeItems 也不能绕过传说规则
容量不足时明确报错，不悄悄牺牲覆盖率
```

---

# 第六部分：G17全世界飞行与御龙术（当前主线）

## 第16步（已完成，禁止重复）：schema-2真实源码报告

用户真实报告已完整接收并原样归档：

```text
文件：g17_source_probe_v2_20260821_125214.txt
大小：83044 bytes
SHA-256：13dc5682cefdf0bc7057f1ddf5defcacd024951fc540350d37b7e5c58ce107d0
编码/行尾：UTF-8-BOM / CRLF
```

权威结果：

```text
PROBE_SCHEMA=2
SOURCE_CONTEXT_BLOCK_COUNT=42
SPELL_ATTR4_CAST_ONLY_IN_OUTLAND_MASK=0x04000000
MISSING_REQUIRED_COUNT=0
MISSING_OPTIONAL_COUNT=0
G17_EXISTING_CONFLICT_COUNT=0
G17_A_WORLD_FLIGHT_PROBE_READY=True
G17_B_VEHICLE_PROBE_READY=True
SOURCE_FILE_EDIT_COUNT=0
CONFIG_EDIT_COUNT=0
DATABASE_EDIT_COUNT=0
```

这一步已经关闭；除非用户真实源码身份或目标文件哈希后来变化，否则不得重跑v1或v2。

## 第17步（已完成，禁止重复）：G17-A包自测 + 真实源码只读Check

> **2026-08-21用户完整回传已验收通过。** ZIP与包内五文件哈希正确；普通和`-O` self-test均PASS；真实目标为`ready`前镜像；`CHECK_READY_TO_APPLY=True`、`Source/config/database edits: 0`、前后哈希不变全部齐全。聊天转录归档：`规划/G17_飞行与移动/证据/g17a_step17_selftest_check_pass_20260821.txt`。这一步没有修改用户真实源码/config/数据库，当前不要重跑。

### 17.1 本步不会做什么

本步：

- 不Apply；
- 不修改 `D:\TrinityCore`；
- 不编译；
- 不停服；
- 不复制运行配置；
- 不修改DBC、MPQ或数据库。

只验证交付包内部一致性和用户真实 `SpellInfo.cpp` 是否仍是已锁定前镜像。

### 17.2 下载唯一ZIP

下载：

```text
G17A_安全全世界飞行_交付包_20260821.zip
```

放到：

```text
C:\Users\Administrator\Downloads\G17A_安全全世界飞行_交付包_20260821.zip
```

ZIP锁定值：

```text
size=42209
sha256=f33d485153108927accbf236b426e34aa0d61f574e37c512269630543beb2275
```

### 17.3 在Windows PowerShell 5.1执行一个原子块

> 若 `Downloads\G17A_安全全世界飞行` 已存在，先手工改名保留；脚本会安全停止，不会擅自删除旧目录。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Zip = "C:\Users\Administrator\Downloads\G17A_安全全世界飞行_交付包_20260821.zip"
    $ZipSha256 = "f33d485153108927accbf236b426e34aa0d61f574e37c512269630543beb2275"
    $Root = "D:\TrinityCore"
    $Target = Join-Path $Root "src\server\game\Spells\SpellInfo.cpp"
    $ExpectedPreSha256 = "78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753"
    $ExtractRoot = "C:\Users\Administrator\Downloads"
    $PackageDir = Join-Path $ExtractRoot "G17A_安全全世界飞行"

    foreach ($Path in @($Python, $Zip, $Target)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "找不到必需文件：$Path"
        }
    }
    if (Test-Path -LiteralPath $PackageDir) {
        throw "解压目标已存在，请先改名保留后再重跑：$PackageDir"
    }

    $ActualZipSha256 = (Get-FileHash -LiteralPath $Zip -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ActualZipSha256 -ne $ZipSha256) {
        throw "ZIP SHA256不一致：实际=$ActualZipSha256，预期=$ZipSha256"
    }
    Write-Host "[OK] ZIP SHA256=$ActualZipSha256"

    Expand-Archive -LiteralPath $Zip -DestinationPath $ExtractRoot

    $Installer = Join-Path $PackageDir "install_g17a_world_flight.py"
    $ExpectedFiles = @{
        "install_g17a_world_flight.py" = "926585da6a363b7c2ae4b2340d5227f1a6d444a1ff1e6297dd939508111893c4"
        "源文件\SpellInfo.cpp" = "537e5c350baa5f4a90bd0ec38c6b6858360e287aeabd75ab54050b4432e50755"
        "SpellInfo.cpp.g17a.patch" = "79226a47d420e8b42017e60f62b60a4871c7c64184edc466bc4ad32eb0f99f81"
        "g17_world_flight.conf" = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
        "04-G17A_安全全世界飞行_安装验收与回滚.md" = "812a288295d9cc604a798ab12c7bfff4b9cb0af6eda2e4a896596700fc991cc3"
    }
    foreach ($Relative in $ExpectedFiles.Keys) {
        $Path = Join-Path $PackageDir $Relative
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "ZIP内缺少文件：$Relative"
        }
        $Actual = (Get-FileHash -LiteralPath $Path -Algorithm SHA256).Hash.ToLowerInvariant()
        if ($Actual -ne $ExpectedFiles[$Relative]) {
            throw "包内文件SHA256不一致：$Relative 实际=$Actual 预期=$($ExpectedFiles[$Relative])"
        }
        Write-Host "[OK] package hash $Relative = $Actual"
    }

    $Before = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Before -ne $ExpectedPreSha256) {
        throw "真实SpellInfo.cpp已变化，禁止继续：实际=$Before 预期=$ExpectedPreSha256"
    }

    $SelfNormalLines = @(& $Python $Installer --self-test 2>&1)
    $SelfNormalExit = $LASTEXITCODE
    $SelfNormalLines | ForEach-Object { Write-Host $_ }
    if ($SelfNormalExit -ne 0) {
        throw "G17-A普通self-test失败，退出码=$SelfNormalExit"
    }

    $SelfOptimizedLines = @(& $Python -O $Installer --self-test 2>&1)
    $SelfOptimizedExit = $LASTEXITCODE
    $SelfOptimizedLines | ForEach-Object { Write-Host $_ }
    if ($SelfOptimizedExit -ne 0) {
        throw "G17-A -O self-test失败，退出码=$SelfOptimizedExit"
    }

    $SelfMarkers = @(
        "[OK] Package full-source/preimage/postimage hashes and CRLF passed.",
        "[OK] Exact apply/already-applied/rollback fixture passed.",
        "[OK] Duplicate/changed-anchor negative fixtures did not false-pass.",
        "[OK] G17-A installer self-test passed."
    )
    foreach ($SelfCase in @(
        @{ Name = "普通"; Text = ($SelfNormalLines | Out-String) },
        @{ Name = "-O"; Text = ($SelfOptimizedLines | Out-String) }
    )) {
        foreach ($Marker in $SelfMarkers) {
            if (-not $SelfCase.Text.Contains($Marker)) {
                throw "$($SelfCase.Name) self-test缺少成功标志：$Marker"
            }
        }
        Write-Host "[OK] G17-A $($SelfCase.Name) SELF_TEST_PASS=True"
    }

    $CheckLines = @(& $Python $Installer --check $Root 2>&1)
    $CheckExit = $LASTEXITCODE
    $CheckLines | ForEach-Object { Write-Host $_ }
    if ($CheckExit -ne 0) {
        throw "G17-A真实源码check失败，退出码=$CheckExit"
    }
    $CheckText = $CheckLines | Out-String
    foreach ($Marker in @(
        "[OK] CHECK_READY_TO_APPLY=True",
        "[OK] Source/config/database edits: 0"
    )) {
        if (-not $CheckText.Contains($Marker)) {
            throw "check缺少成功标志：$Marker"
        }
    }

    $After = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($After -ne $Before) {
        throw "只读check前后源码哈希发生变化：before=$Before after=$After"
    }

    Write-Host "[OK] G17-A SELF_TEST_AND_CHECK_PASS=True"
    Write-Host "[OK] SpellInfo.cpp unchanged sha256=$After"
    Write-Host "[STOP] 当前到此为止；不要Apply、不要编译，先完整回传本窗口输出。"
}
```

### 17.4 成功门槛

末尾必须同时看到：

```text
[OK] G17-A 普通 SELF_TEST_PASS=True
[OK] G17-A -O SELF_TEST_PASS=True
[OK] CHECK_READY_TO_APPLY=True
[OK] Source/config/database edits: 0
[OK] G17-A SELF_TEST_AND_CHECK_PASS=True
[OK] SpellInfo.cpp unchanged sha256=78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753
```

普通与`-O`两次输出中还必须分别出现`[OK] G17-A installer self-test passed.`；不能只跑其中一种解释器模式。

这项完整回传要求已经满足；第17步关闭，禁止重复。

## 第18步（已执行但最终断言红字，禁止重跑）：Apply与部署默认关闭配置

> 2026-08-21 15:17:24回传表明：真实Apply、源码备份、二进制/PDB备份均已成功；脚本也已完成conf复制及精确SHA校验，随后在`WorldFlight.Enable = 0`文本行断言处抛错，因此没有打印总PASS。当前不要重跑本步；转到第18R步只读核验。

### 18.0 conf文件名与格式结论（已复核）

`g17_world_flight.conf` **不需要改名**，下划线`_`是安全的。架构基础`Config.cpp::LoadAdditionalDir`对文件名没有“只能字母数字”的正则限制，只检查：

```cpp
if (configFile.extension() != ".conf")
    continue;
```

而且用户真实服务器此前已经打印过：

```text
Loaded additional config file ...g11_perception.conf
```

这直接证明实际运行端可以加载带下划线的conf文件。此前容易混淆的警示实际是：

1. 文件扩展名保持小写`.conf`；`.conf.off`会被故意忽略；
2. `.conf`不能带UTF-8 BOM；当前文件首字节为`23 23 23`（`###`），无BOM；
3. 必须有且只有正确的`[worldserver]`段；当前文件具备；
4. 不使用emoji、中文引号等高风险符号作文件名；建议只用ASCII字母、数字、`_`、`-`；
5. 不要依赖多个conf的文件名字母顺序覆盖同名键；G17键必须只保留一份；
6. 旧版`切换档位.bat`若使用`*.conf`通配符，会误把功能conf改成`.off`。当前仓库版已改成五档白名单；本步不要运行任何旧切档bat。

包内文件已复核：SHA-256=`a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17`、无BOM、无NUL、带`[worldserver]`、`WorldFlight.Enable = 0`恰好一条。

### 18.1 原始Apply块（用户已执行；仅留作审计，禁止再次运行）

> 下方文本行验证已改成`ReadAllLines`精确比较，避免管道/编码环境差异；这不改变用户已经成功落盘的源码或conf。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $PackageDir = "C:\Users\Administrator\Downloads\G17A_安全全世界飞行"
    $Installer = Join-Path $PackageDir "install_g17a_world_flight.py"
    $ConfSource = Join-Path $PackageDir "g17_world_flight.conf"
    $Root = "D:\TrinityCore"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $ConfDir = Join-Path $RunDir "worldserver.conf.d"
    $ConfDest = Join-Path $ConfDir "g17_world_flight.conf"
    $Target = Join-Path $Root "src\server\game\Spells\SpellInfo.cpp"
    $Backup = "$Target.g17a.bak"
    $ExpectedPre = "78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753"
    $ExpectedPost = "537e5c350baa5f4a90bd0ec38c6b6858360e287aeabd75ab54050b4432e50755"
    $ExpectedConf = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) {
        throw "worldserver仍在运行；请正常关闭后再Apply"
    }
    foreach ($Path in @($Python, $Installer, $ConfSource, $Target)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "找不到必需文件：$Path"
        }
    }
    $Current = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Current -ne $ExpectedPre) {
        throw "Apply前SpellInfo.cpp不是锁定前镜像：实际=$Current"
    }

    $Stamp = Get-Date -Format "yyyyMMdd_HHmmss"
    foreach ($Name in @("worldserver.exe", "worldserver.pdb")) {
        $Path = Join-Path $RunDir $Name
        if (Test-Path -LiteralPath $Path -PathType Leaf) {
            Copy-Item -LiteralPath $Path -Destination "$Path.before_g17a_$Stamp.bak"
            Write-Host "[OK] binary backup=$Path.before_g17a_$Stamp.bak"
        }
    }

    & $Python $Installer --apply $Root
    if ($LASTEXITCODE -ne 0) {
        throw "G17-A Apply失败，退出码=$LASTEXITCODE"
    }

    $Post = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    $Bak = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Post -ne $ExpectedPost -or $Bak -ne $ExpectedPre) {
        throw "Apply后哈希不符：post=$Post backup=$Bak"
    }

    New-Item -ItemType Directory -Path $ConfDir -Force | Out-Null
    Copy-Item -LiteralPath $ConfSource -Destination $ConfDest -Force
    $ConfHash = (Get-FileHash -LiteralPath $ConfDest -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ConfHash -ne $ExpectedConf) {
        throw "运行conf哈希不符：实际=$ConfHash"
    }
    $ConfLines = [System.IO.File]::ReadAllLines($ConfDest)
    $EnableKeyLines = @($ConfLines | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    $EnableExactLines = @($ConfLines | Where-Object { $_ -ceq "WorldFlight.Enable = 0" })
    if ($EnableKeyLines.Count -ne 1 -or $EnableExactLines.Count -ne 1) {
        throw "首次部署必须且只能有一条精确的WorldFlight.Enable = 0；key=$($EnableKeyLines.Count) exact=$($EnableExactLines.Count)"
    }

    Write-Host "[OK] G17-A APPLY_AND_CONF_STAGE_PASS=True"
    Write-Host "[OK] postimage sha256=$Post"
    Write-Host "[OK] backup sha256=$Bak"
    Write-Host "[OK] conf default remains disabled: $ConfDest"
}
```

原块预期看到总PASS，但用户实际在最后文本行断言处停止；**已有`APPLY_CHANGED_FILES=1`不能再次Apply**。失败转录：

```text
规划/G17_飞行与移动/证据/g17a_step18_apply_conf_validator_failure_20260821_151724.txt
```

### 第18R步（已PASS并关闭，禁止重复）：Apply后状态只读恢复验证

> 2026-08-21用户真实回传已经确认：Enable key/exact均为1；exe备份36696576字节；PDB备份432328704字节；post/pre/conf三个SHA精确匹配；conf无BOM且唯一默认关闭；`STEP18_RECOVERY_VERIFY_PASS=True`。下方只保留作审计，不再执行。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Target = "D:\TrinityCore\src\server\game\Spells\SpellInfo.cpp"
    $SourceBackup = "$Target.g17a.bak"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $BinaryBackups = @(
        "D:\TC-Build\bin\RelWithDebInfo\worldserver.exe.before_g17a_20260821_151724.bak",
        "D:\TC-Build\bin\RelWithDebInfo\worldserver.pdb.before_g17a_20260821_151724.bak"
    )
    $ExpectedPost = "537e5c350baa5f4a90bd0ec38c6b6858360e287aeabd75ab54050b4432e50755"
    $ExpectedPre = "78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753"
    $ExpectedConf = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) {
        throw "worldserver正在运行；当前不要启动，请先正常关闭"
    }
    foreach ($Path in @($Target, $SourceBackup, $Conf) + $BinaryBackups) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "缺少第18步应留下的文件：$Path"
        }
    }

    $Post = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    $Bak = (Get-FileHash -LiteralPath $SourceBackup -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Post -ne $ExpectedPost) { throw "postimage不符：$Post" }
    if ($Bak -ne $ExpectedPre) { throw "源码备份不符：$Bak" }
    if ($ConfHash -ne $ExpectedConf) { throw "运行conf不符：$ConfHash" }

    $Bytes = [System.IO.File]::ReadAllBytes($Conf)
    if ($Bytes.Length -ge 3 -and $Bytes[0] -eq 239 -and $Bytes[1] -eq 187 -and $Bytes[2] -eq 191) {
        throw "运行conf意外带UTF-8 BOM"
    }
    $ConfLines = [System.IO.File]::ReadAllLines($Conf)
    $EnableKeyLines = @($ConfLines | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    $EnableExactLines = @($ConfLines | Where-Object { $_ -ceq "WorldFlight.Enable = 0" })
    Write-Host "[INFO] WorldFlight.Enable key count=$($EnableKeyLines.Count) exact-default-off count=$($EnableExactLines.Count)"
    if ($EnableKeyLines.Count -ne 1 -or $EnableExactLines.Count -ne 1) {
        throw "运行conf的Enable行不唯一或不是精确默认关闭值"
    }

    foreach ($Path in $BinaryBackups) {
        $Size = (Get-Item -LiteralPath $Path).Length
        if ($Size -le 0) { throw "二进制备份为空：$Path" }
        Write-Host "[OK] binary backup exists size=$Size path=$Path"
    }

    Write-Host "[OK] postimage sha256=$Post"
    Write-Host "[OK] source backup sha256=$Bak"
    Write-Host "[OK] conf sha256=$ConfHash; UTF8_BOM=0; WorldFlight.Enable=0 unique"
    Write-Host "[OK] G17-A STEP18_RECOVERY_VERIFY_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要编译、不要启动，先完整回传本窗口输出。"
}
```

第18R步证据已归档：

```text
规划/G17_飞行与移动/证据/g17a_step18r_recovery_verify_pass_20260821.txt
```

本批只改一个既有`.cpp`，不需要重跑CMake，不执行SQL，不改DBC/MPQ。

## 第19A步（已PASS并关闭，禁止重复）：VS2022 RelWithDebInfo/x64编译

> 2026-08-21用户真实回传确认：MSBuild 18.8.2明确编译`SpellInfo.cpp`、生成`game.lib`并链接`worldserver.exe`；新exe=36699136字节/SHA=`02bfe1e6...a5dd`，新PDB=432877568字节，时间均前进；`STEP19A_BUILD_PASS=True`。下方只保留作审计，不再执行。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Root = "D:\TrinityCore"
    $Sln = "D:\TC-Build\TrinityCore.sln"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Exe = Join-Path $RunDir "worldserver.exe"
    $Pdb = Join-Path $RunDir "worldserver.pdb"
    $Target = Join-Path $Root "src\server\game\Spells\SpellInfo.cpp"
    $Conf = Join-Path $RunDir "worldserver.conf.d\g17_world_flight.conf"
    $ExpectedPost = "537e5c350baa5f4a90bd0ec38c6b6858360e287aeabd75ab54050b4432e50755"
    $ExpectedConf = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) {
        throw "worldserver正在运行；编译前必须先正常关闭"
    }
    foreach ($Path in @($Sln, $Exe, $Pdb, $Target, $Conf)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) {
            throw "找不到编译门槛文件：$Path"
        }
    }
    $SourceHash = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($SourceHash -ne $ExpectedPost) { throw "编译前postimage不符：$SourceHash" }
    if ($ConfHash -ne $ExpectedConf) { throw "编译前conf不符：$ConfHash" }
    $EnableLines = @([System.IO.File]::ReadAllLines($Conf) | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    if ($EnableLines.Count -ne 1 -or $EnableLines[0] -cne "WorldFlight.Enable = 0") {
        throw "编译前WorldFlight.Enable不是唯一精确默认关闭值"
    }

    $VsWhere = Join-Path ${env:ProgramFiles(x86)} "Microsoft Visual Studio\Installer\vswhere.exe"
    if (-not (Test-Path -LiteralPath $VsWhere -PathType Leaf)) {
        throw "找不到vswhere.exe：$VsWhere"
    }
    $MSBuild = @(& $VsWhere -latest -products * -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Where-Object { $_ })[0]
    if (-not $MSBuild -or -not (Test-Path -LiteralPath $MSBuild -PathType Leaf)) {
        throw "找不到Visual Studio 2022 MSBuild.exe"
    }

    $BeforeExe = Get-Item -LiteralPath $Exe
    $BeforePdb = Get-Item -LiteralPath $Pdb
    $BeforeExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $BeforeExeUtc = $BeforeExe.LastWriteTimeUtc
    $BeforePdbUtc = $BeforePdb.LastWriteTimeUtc
    Write-Host "[INFO] MSBuild=$MSBuild"
    Write-Host "[INFO] before exe size=$($BeforeExe.Length) utc=$($BeforeExeUtc.ToString('o')) sha256=$BeforeExeHash"
    Write-Host "[INFO] before pdb size=$($BeforePdb.Length) utc=$($BeforePdbUtc.ToString('o'))"

    & $MSBuild $Sln /m /t:worldserver /p:Configuration=RelWithDebInfo /p:Platform=x64 /verbosity:minimal
    $BuildExit = $LASTEXITCODE
    if ($BuildExit -ne 0) {
        throw "MSBuild失败，退出码=$BuildExit；回传第一条真实C++错误及其上下文"
    }

    $AfterExe = Get-Item -LiteralPath $Exe
    $AfterPdb = Get-Item -LiteralPath $Pdb
    $AfterExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($AfterExe.Length -le 0 -or $AfterPdb.Length -le 0) { throw "构建产物为空" }
    if ($AfterExe.LastWriteTimeUtc -le $BeforeExeUtc) { throw "worldserver.exe时间未更新" }
    if ($AfterPdb.LastWriteTimeUtc -le $BeforePdbUtc) { throw "worldserver.pdb时间未更新" }
    if ($AfterExeHash -eq $BeforeExeHash) { throw "worldserver.exe哈希未变化，不能证明新源码已链接" }

    Write-Host "[OK] after exe size=$($AfterExe.Length) utc=$($AfterExe.LastWriteTimeUtc.ToString('o')) sha256=$AfterExeHash"
    Write-Host "[OK] after pdb size=$($AfterPdb.Length) utc=$($AfterPdb.LastWriteTimeUtc.ToString('o'))"
    Write-Host "[OK] source postimage sha256=$SourceHash"
    Write-Host "[OK] conf remains default-off sha256=$ConfHash"
    Write-Host "[OK] G17-A STEP19A_BUILD_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要启动worldserver，先完整回传本窗口输出。"
}
```

第19A步证据已归档：

```text
规划/G17_飞行与移动/证据/g17a_step19a_build_pass_20260821.txt
```

构建输出已确认是：

```text
D:\TC-Build\bin\RelWithDebInfo\worldserver.exe
```

## 第19B步（已PASS并关闭，禁止重复）：默认关闭启动门槛

> 2026-08-21用户已回传19B.1预检PASS、真实游戏“主世界无法飞、外域能飞”和19B.3后检PASS，并确认按既定流程在验证后关闭服务器。能够进入游戏执行两地测试证明服务器已可连接；不再为了已经关闭的控制台重复启动。未留存的loaded-conf/ready原始行及精确定位不伪造，改为第20B启用后首次启动的强制捕获项。

### 19B.1 启动前只读预检（已PASS，禁止重复）

下方只保留作审计：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Exe = Join-Path $RunDir "worldserver.exe"
    $Pdb = Join-Path $RunDir "worldserver.pdb"
    $Target = "D:\TrinityCore\src\server\game\Spells\SpellInfo.cpp"
    $Conf = Join-Path $RunDir "worldserver.conf.d\g17_world_flight.conf"
    $ExpectedExe = "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd"
    $ExpectedSource = "537e5c350baa5f4a90bd0ec38c6b6858360e287aeabd75ab54050b4432e50755"
    $ExpectedConf = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver已经在运行；不要重复启动" }
    foreach ($Path in @($Exe, $Pdb, $Target, $Conf)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "缺少启动门槛文件：$Path" }
    }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $SourceHash = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe) { throw "活动exe不是第19A步新产物：$ExeHash" }
    if ($SourceHash -ne $ExpectedSource) { throw "postimage漂移：$SourceHash" }
    if ($ConfHash -ne $ExpectedConf) { throw "默认关闭conf漂移：$ConfHash" }
    $Lines = [System.IO.File]::ReadAllLines($Conf)
    $Enable = @($Lines | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    if ($Enable.Count -ne 1 -or $Enable[0] -cne "WorldFlight.Enable = 0") { throw "Enable不是唯一精确默认关闭值" }
    $ExeItem = Get-Item -LiteralPath $Exe
    $PdbItem = Get-Item -LiteralPath $Pdb
    if ($ExeItem.Length -ne 36699136 -or $PdbItem.Length -ne 432877568) { throw "第19A步产物大小漂移" }

    Write-Host "[OK] active exe size=$($ExeItem.Length) utc=$($ExeItem.LastWriteTimeUtc.ToString('o')) sha256=$ExeHash"
    Write-Host "[OK] active pdb size=$($PdbItem.Length) utc=$($PdbItem.LastWriteTimeUtc.ToString('o'))"
    Write-Host "[OK] source postimage sha256=$SourceHash"
    Write-Host "[OK] conf default-off sha256=$ConfHash; WorldFlight.Enable=0 unique"
    Write-Host "[OK] G17-A STEP19B_PREFLIGHT_PASS=True"
    Write-Host "[NEXT] 现在按既有正常方式启动该RunDir中的worldserver.exe；不要修改conf。"
}
```

只有看到`STEP19B_PREFLIGHT_PASS=True`才继续。

### 19B.2 正常启动与A0默认关闭对照（已PASS；原始行改由第20B强制捕获）

1. 按你此前已验证的正常方式启动`D:\TC-Build\bin\RelWithDebInfo\worldserver.exe`，不要使用其它目录的旧exe；
2. 等待服务器完整ready/可连接，不要只看到数据库连接就判定成功；
3. 保存或拍下加载`g17_world_flight.conf`的控制台行和最终ready行；
4. 确认本次启动没有`duplicate key`、conf解析、`WorldFlight`或G17相关error；若有，保留第一条真实错误并停止后续游戏测试；
5. 使用一个已经具备飞行骑术、且该飞行坐骑在外域可正常使用的角色；
6. 在东部王国或卡利姆多普通户外区域（非主城、非室内）记录`.gps`的map/zone/area和飞行坐骑法术ID，然后尝试召唤：**Enable=0时必须保持原版拒绝，不能起飞移动**；
7. 建议再到外域普通可飞区域用同一角色、同一飞行坐骑作对照：应维持原版允许。这能排除角色未学骑术或坐骑本身不可用造成的假阳性；
8. 验证完成后通过worldserver控制台按既有方式**正常停服**，不要强杀进程；暂时不要把Enable改成1。

必须按以下格式回传真实值，不要只写“正常”：

```text
[RUN] loaded-conf line=<包含g17_world_flight.conf的原始控制台行>
[RUN] ready line=<最终ready/可连接原始控制台行>
[RUN] startup G17/WorldFlight/conf/duplicate errors=0
[A0] character=<角色名>; map=<ID>; zone=<ID>; area=<ID>; mount-spell=<ID>; summon=<rejected>; movement=<no flight>
[CONTROL] Outland map/zone/area=<实际值>; same mount-spell=<ID>; summon=<allowed>; movement=<flight works>
[STOP] graceful shutdown completed=True
```

如果当前没有满足条件的外域对照角色，不要临时改源码/conf，也不要把无法判断写成PASS；如实写`CONTROL_NOT_RUN`及原因。

### 19B.3 停服后的只读后检（已PASS；只在19B-R分支B再次启动后重跑）

下方保留作审计；若19B-R需要重新启动一次，则新一轮正常停服后必须再执行：

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Exe = "D:\TC-Build\bin\RelWithDebInfo\worldserver.exe"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $ExpectedExe = "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd"
    $ExpectedConf = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver仍在运行；尚未正常停服" }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe) { throw "停服后exe漂移：$ExeHash" }
    if ($ConfHash -ne $ExpectedConf) { throw "停服后conf漂移：$ConfHash" }
    $Enable = @([System.IO.File]::ReadAllLines($Conf) | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    if ($Enable.Count -ne 1 -or $Enable[0] -cne "WorldFlight.Enable = 0") { throw "停服后Enable不是唯一默认关闭值" }
    Write-Host "[OK] stopped; exe sha256=$ExeHash"
    Write-Host "[OK] conf unchanged sha256=$ConfHash; WorldFlight.Enable=0 unique"
    Write-Host "[OK] G17-A STEP19B_POSTFLIGHT_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要Enable=1，先完整回传19B.1/19B.2/19B.3结果。"
}
```

### 第19B-R步（已取消，不执行）：避免无价值重复启动

原计划为补抄已关闭控制台的原始行。用户正确指出既定流程本就要求游戏验证后关闭服务器；因此不再为此单独重启。补证方案取消，缺失的原始启动/定位字段转为第20B启用后首次启动的强制门槛。

第19B原始转录与关闭判定分别归档为：

```text
规划/G17_飞行与移动/证据/g17a_step19b_partial_pre_post_behavior_20260821.txt
规划/G17_飞行与移动/证据/g17a_step19b_default_off_acceptance_20260821.txt
```

## 第20A步（已PASS并关闭，禁止重复）：备份默认关闭conf并精确切换Enable=1

> 2026-08-21 16:48:9de1`、无BOM、Enable=1唯一；`STEP20A_ENABLE_CONF_PASS=True`。下方只保留作审计，不再执行。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $ExpectedOff = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
    $ExpectedOn = "712784428878560d51d6235591f23f9a46df6c36679e1505e6b0b513d9d59de1"
    $OldLine = "WorldFlight.Enable = 0"
    $NewLine = "WorldFlight.Enable = 1"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver仍在运行；必须先正常停服" }
    if (-not (Test-Path -LiteralPath $Conf -PathType Leaf)) { throw "找不到运行conf：$Conf" }
    $BeforeHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($BeforeHash -ne $ExpectedOff) { throw "切换前conf不是锁定默认关闭镜像：$BeforeHash" }

    $Bytes = [System.IO.File]::ReadAllBytes($Conf)
    if ($Bytes.Length -ge 3 -and $Bytes[0] -eq 239 -and $Bytes[1] -eq 187 -and $Bytes[2] -eq 191) { throw "切换前conf意外带BOM" }
    $Utf8Strict = [System.Text.UTF8Encoding]::new($false, $true)
    $Text = $Utf8Strict.GetString($Bytes)
    $KeyCount = ([regex]::Matches($Text, '(?m)^WorldFlight\.Enable[ \t]*=')).Count
    $OldCount = ([regex]::Matches($Text, [regex]::Escape($OldLine))).Count
    if ($KeyCount -ne 1 -or $OldCount -ne 1) { throw "切换前Enable行不唯一：key=$KeyCount exact0=$OldCount" }

    $Stamp = Get-Date -Format "yyyyMMdd_HHmmss"
    $Backup = "$Conf.enable0_before_g17a_$Stamp.bak"
    if (Test-Path -LiteralPath $Backup) { throw "备份路径意外已存在：$Backup" }
    Copy-Item -LiteralPath $Conf -Destination $Backup
    $BackupHash = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($BackupHash -ne $ExpectedOff) { throw "默认关闭备份哈希不符：$BackupHash" }

    try {
        $EnabledText = $Text.Replace($OldLine, $NewLine)
        [System.IO.File]::WriteAllText($Conf, $EnabledText, [System.Text.UTF8Encoding]::new($false))
        $AfterHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
        if ($AfterHash -ne $ExpectedOn) { throw "Enable=1 conf哈希不符：$AfterHash" }
        $AfterBytes = [System.IO.File]::ReadAllBytes($Conf)
        if ($AfterBytes.Length -ge 3 -and $AfterBytes[0] -eq 239 -and $AfterBytes[1] -eq 187 -and $AfterBytes[2] -eq 191) { throw "Enable=1 conf意外带BOM" }
        $AfterLines = [System.IO.File]::ReadAllLines($Conf)
        $EnableKeys = @($AfterLines | Where-Object { $_.StartsWith("WorldFlight.Enable") })
        $EnableOn = @($AfterLines | Where-Object { $_ -ceq $NewLine })
        if ($EnableKeys.Count -ne 1 -or $EnableOn.Count -ne 1) { throw "Enable=1行不唯一" }
    }
    catch {
        Copy-Item -LiteralPath $Backup -Destination $Conf -Force
        $RestoredHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
        if ($RestoredHash -ne $ExpectedOff) { throw "写入失败且自动恢复也失败：restored=$RestoredHash" }
        throw
    }

    Write-Host "[OK] enable0 backup=$Backup sha256=$BackupHash"
    Write-Host "[OK] enabled conf sha256=$AfterHash; UTF8_BOM=0; WorldFlight.Enable=1 unique"
    Write-Host "[OK] G17-A STEP20A_ENABLE_CONF_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要启动worldserver，先完整回传本窗口输出。"
}
```

第20A证据：

```text
规划/G17_飞行与移动/证据/g17a_step20a_enable_conf_pass_20260821_164848.txt
```

## 第20B-S步（已PASS并关闭，禁止重复）：Enable=1启动与A1/A2/A9烟雾

> 2026-08-21真实回传：A1 map0/zone11/area11、A2 map1/zone491/area491、A9 map530/zone3703/area3703；Enable=1后能飞，起飞/移动/降落无问题；后检确认exe、启用conf和默认关闭备份均未漂移。下方只保留作审计。

### 20B-S.1 启动前只读预检

```powershell
& {
    $ErrorActionPreference = "Stop"
    $RunDir = "D:\TC-Build\bin\RelWithDebInfo"
    $Exe = Join-Path $RunDir "worldserver.exe"
    $Conf = Join-Path $RunDir "worldserver.conf.d\g17_world_flight.conf"
    $Backup = "$Conf.enable0_before_g17a_20260821_164848.bak"
    $ExpectedExe = "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd"
    $ExpectedOn = "712784428878560d51d6235591f23f9a46df6c36679e1505e6b0b513d9d59de1"
    $ExpectedOff = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver已经在运行；不要重复启动" }
    foreach ($Path in @($Exe, $Conf, $Backup)) {
        if (-not (Test-Path -LiteralPath $Path -PathType Leaf)) { throw "缺少启用启动门槛文件：$Path" }
    }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    $BackupHash = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe) { throw "活动exe漂移：$ExeHash" }
    if ($ConfHash -ne $ExpectedOn) { throw "Enable=1运行conf漂移：$ConfHash" }
    if ($BackupHash -ne $ExpectedOff) { throw "Enable=0备份漂移：$BackupHash" }
    $Enable = @([System.IO.File]::ReadAllLines($Conf) | Where-Object { $_.StartsWith("WorldFlight.Enable") })
    if ($Enable.Count -ne 1 -or $Enable[0] -cne "WorldFlight.Enable = 1") { throw "运行conf不是唯一精确Enable=1" }
    Write-Host "[OK] active exe sha256=$ExeHash"
    Write-Host "[OK] enabled conf sha256=$ConfHash; WorldFlight.Enable=1 unique"
    Write-Host "[OK] enable0 backup sha256=$BackupHash"
    Write-Host "[OK] G17-A STEP20B_SMOKE_PREFLIGHT_PASS=True"
    Write-Host "[NEXT] 启动后先把loaded-conf与ready原始行复制到记事本，再进入游戏；不要关闭后才补抄。"
}
```

### 20B-S.2 正常启动并执行三项烟雾

1. 正常启动锁定RunDir中的新`worldserver.exe`；
2. **在进入游戏前**把以下两条实际控制台原文复制到记事本：
   - `Loaded additional config file ...g17_world_flight.conf`
   - 包含`(worldserver-daemon) ready...`的最终ready行；
3. 确认启动阶段`G17`、`WorldFlight`、conf parse、duplicate key相关error为0；
4. 使用同一名已有飞行骑术、同一飞行坐骑的角色，角色名可部分遮盖，但每项必须先执行`.gps`并记录实际map/zone/area与mount-spell；
5. A1：东部王国map 0普通户外、非主城/室内——召唤、起飞、水平移动、降落均成功；
6. A2：卡利姆多map 1普通户外、非主城/室内——同A1；
7. A9控制：外域map 530普通可飞户外——原版飞行继续成功；
8. 完成后通过控制台正常停服，不强杀；暂不做A3–A12。

回传格式：

```text
[RUN] loaded-conf line=<完整原始行>
[RUN] ready line=<完整原始行>
[RUN] startup G17/WorldFlight/conf/duplicate errors=0
[A1] character=<可遮盖>; map=0; zone=<ID>; area=<ID>; mount-spell=<ID>; summon=allowed; takeoff=yes; horizontal-movement=yes; landing=yes
[A2] character=<同一角色>; map=1; zone=<ID>; area=<ID>; mount-spell=<同一ID>; summon=allowed; takeoff=yes; horizontal-movement=yes; landing=yes
[A9] character=<同一角色>; map=530; zone=<ID>; area=<ID>; mount-spell=<同一ID>; summon=allowed; movement=flight works
[STOP] graceful shutdown completed=True
```

### 20B-S.3 正常停服后的只读后检

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Exe = "D:\TC-Build\bin\RelWithDebInfo\worldserver.exe"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $Backup = "$Conf.enable0_before_g17a_20260821_164848.bak"
    $ExpectedExe = "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd"
    $ExpectedOn = "712784428878560d51d6235591f23f9a46df6c36679e1505e6b0b513d9d59de1"
    $ExpectedOff = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver仍在运行；尚未正常停服" }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    $BackupHash = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe -or $ConfHash -ne $ExpectedOn -or $BackupHash -ne $ExpectedOff) {
        throw "烟雾后文件漂移：exe=$ExeHash conf=$ConfHash backup=$BackupHash"
    }
    Write-Host "[OK] stopped; exe sha256=$ExeHash"
    Write-Host "[OK] enabled conf unchanged sha256=$ConfHash"
    Write-Host "[OK] enable0 backup unchanged sha256=$BackupHash"
    Write-Host "[OK] G17-A STEP20B_SMOKE_POSTFLIGHT_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要做A3-A12，先完整回传20B-S全部结果。"
}
```

第20B-S证据：

```text
规划/G17_飞行与移动/证据/g17a_step20bs_enable_smoke_pass_20260821.txt
```

## 第20C-1步（用户已声明执行完成，关闭且禁止重复）：历史A3/A4/A6安全边界步骤

> 用户已明确确认：主城内召唤拒绝、从外飞入暴风城会落地、PVP战场表现相同，并声明第20C-1执行完成。该摘要已归档；不要求重做主城/PVP。用户本次消息没有单独提供A4室内地点/`.gps`、某个精确A6世界竞技场地点或逐项前后检标志，均保持未知，不能补造。以下命令只保留作历史审计，当前不要执行。

### 20C-1.1 启动前只读预检

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Exe = "D:\TC-Build\bin\RelWithDebInfo\worldserver.exe"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $Backup = "$Conf.enable0_before_g17a_20260821_164848.bak"
    $ExpectedExe = "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd"
    $ExpectedOn = "712784428878560d51d6235591f23f9a46df6c36679e1505e6b0b513d9d59de1"
    $ExpectedOff = "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17"
    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver已经在运行；不要重复启动" }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    $BackupHash = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne $ExpectedExe -or $ConfHash -ne $ExpectedOn -or $BackupHash -ne $ExpectedOff) {
        throw "C-1预检漂移：exe=$ExeHash conf=$ConfHash backup=$BackupHash"
    }
    Write-Host "[OK] G17-A STEP20C1_PREFLIGHT_PASS=True"
    Write-Host "[NEXT] 正常启动；三项均贴地低空，任何意外允许立即停测。"
}
```

### 20C-1.2 游戏测试

使用20B-S已经成功飞行的同一角色和同一飞行坐骑；每个测试点都先执行`.gps`并保存完整输出。

**A3 主城边界（推荐暴风城或奥格瑞玛）：**

1. 站在主城内部，确认map为0或1；尝试召唤飞行坐骑，必须被拒绝；
2. 到紧邻主城的普通许可户外召唤并起飞；保持离地约1–2米，缓慢飞入主城边界；
3. Area更新后飞行/坐骑光环必须被移除，角色落地，不能继续飞；
4. 记录主城内`.gps`及飞入前后实际现象。

**A4 明确室内（选择`.gps`首行明确显示`You are indoors.`的旅店、建筑或洞穴）：**

1. 必须先取得`You are indoors.`原始行；若候选点显示outdoors，换地点，不能算A4；
2. 室内尝试召唤飞行坐骑，必须被拒绝；
3. 如果建筑入口允许安全贴地飞入，可附加观察光环是否移除；受门框/碰撞限制时不强求飞入，室内禁召通过即可。

**A6 世界竞技场（推荐荆棘谷古拉巴什竞技场）：**

1. 在竞技场内部保存`.gps`，尝试召唤飞行坐骑，必须被拒绝；
2. 从竞技场外许可户外贴地低空飞入；进入竞技场Area后飞行/坐骑光环必须被移除；
3. 若所选地点没有触发拒绝，不能写PASS；停止并回传完整`.gps`，不要自行换代码。

回传格式：

```text
[A3-CAST] location=<主城>; map=<0或1>; zone=<ID>; area=<ID>; summon=rejected
[A3-ENTRY] from-zone/area=<ID/ID>; to-zone/area=<ID/ID>; altitude=low; aura-removed=yes; continued-flight=no
[A4-CAST] location=<名称>; gps-first-line=You are indoors.; map=<0或1>; zone=<ID>; area=<ID>; summon=rejected
[A4-ENTRY] optional=<aura-removed=yes或NOT_RUN_COLLISION>
[A6-CAST] location=<竞技场>; map=<0或1>; zone=<ID>; area=<ID>; summon=rejected
[A6-ENTRY] from-zone/area=<ID/ID>; to-zone/area=<ID/ID>; altitude=low; aura-removed=yes; continued-flight=no
[STOP] graceful shutdown completed=True
```

### 20C-1.3 正常停服后的只读后检

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Exe = "D:\TC-Build\bin\RelWithDebInfo\worldserver.exe"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $Backup = "$Conf.enable0_before_g17a_20260821_164848.bak"
    if (Get-Process worldserver -ErrorAction SilentlyContinue) { throw "worldserver仍在运行；尚未正常停服" }
    $ExeHash = (Get-FileHash -LiteralPath $Exe -Algorithm SHA256).Hash.ToLowerInvariant()
    $ConfHash = (Get-FileHash -LiteralPath $Conf -Algorithm SHA256).Hash.ToLowerInvariant()
    $BackupHash = (Get-FileHash -LiteralPath $Backup -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($ExeHash -ne "02bfe1e6373bf8f21b136863280ad12290407d5450ea2f844647c19f2cf7a5dd" -or $ConfHash -ne "712784428878560d51d6235591f23f9a46df6c36679e1505e6b0b513d9d59de1" -or $BackupHash -ne "a96afb09409f1a3080c62e2df7f6f02d91efc17270106b64339efa1529d30f17") {
        throw "C-1后检文件漂移：exe=$ExeHash conf=$ConfHash backup=$BackupHash"
    }
    Write-Host "[OK] G17-A STEP20C1_POSTFLIGHT_PASS=True"
    Write-Host "[STOP] 当前到此为止；不要做其它矩阵，先完整回传A3/A4/A6。"
}
```

已复核架构基础`Player::UpdateAreaDependentAuras`：Area变化时会对所有已有光环以`strict=false`再次调用`SpellInfo::CheckLocation`，失败即`RemoveOwnedAura`。因此A3/A4/A12的飞入后移除有核心调用链依据，但仍必须以上述游戏实测为准。证据：`证据/g17a_area_update_aura_recheck_source_audit_20260821.txt`。

### 第20C总矩阵（C-1用户摘要已关闭；其余项尚未安排）

| 编号 | 场景 | 预期 |
|---|---|---|
| A0 | Enable=0的既有结果 | 东部王国/卡利姆多保持原版不可飞 |
| A1 | Enable=1，东部王国普通户外 | 飞行坐骑可召唤、起飞、移动、降落 |
| A2 | Enable=1，卡利姆多普通户外 | 同A1 |
| A3 | 主城/主城子区，BlockCapitalAreas=1 | 禁止召唤；从野外飞入后飞行光环被移除 |
| A4 | 明确室内，BlockIndoorAreas=1 | 禁止召唤或进入后移除飞行光环 |
| A5 | `AREA_FLAG_NO_FLY_ZONE` | 始终禁止，不能由conf绕过 |
| A6 | 世界竞技场/竞技场实例 | 始终禁止 |
| A7 | 普通副本/团队副本 | map不是0/1，不走G17放行 |
| A8 | 战场 | 不走G17放行 |
| A9 | 外域 | 原版飞行逻辑不变 |
| A10 | 北裂境未学54197 | 仍受寒冷天气飞行限制 |
| A11 | 北裂境已学54197 | 原版允许 |
| A12 | `BlockedAreaIds`加入当前十进制Zone/Area ID | 该Area禁止；移除ID并重启后恢复 |

每项完整记录：

```text
A编号 / map / zone / area / 坐骑法术ID / 召唤结果 / 是否能移动 / 进入禁区是否移除光环 / 第一条相关error
```

`BlockedAreaIds`示例（逗号分隔，不要加空格）：

```ini
WorldFlight.BlockedAreaIds = 12,1519
```

矩阵全通过才把G17-A标为完成。此后才进入G17-B0原生可控龙载具与独立技能条；B1/B2另立安装和验收，不能用三项骑术技能替代。

---

# 第七部分：回滚

## G17-A源码与运行配置回滚

仅在第18步已经Apply后使用；第17步是只读检查，不需要回滚。

1. 正常关闭worldserver；
2. 在G17-A解压目录执行锁定回滚；
3. 删除或移走运行目录conf；
4. 重新编译，使worldserver链接回原版 `SpellInfo.cpp`；
5. 若新构建不可用，可临时恢复第18步建立的同一时间戳exe/PDB备份，但exe/PDB/DLL必须属于同一批构建。

```powershell
& {
    $ErrorActionPreference = "Stop"
    $Python = "C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe"
    $Installer = "C:\Users\Administrator\Downloads\G17A_安全全世界飞行\install_g17a_world_flight.py"
    $Root = "D:\TrinityCore"
    $Target = Join-Path $Root "src\server\game\Spells\SpellInfo.cpp"
    $Conf = "D:\TC-Build\bin\RelWithDebInfo\worldserver.conf.d\g17_world_flight.conf"
    $ExpectedPre = "78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753"

    if (Get-Process worldserver -ErrorAction SilentlyContinue) {
        throw "worldserver仍在运行；请正常关闭后回滚"
    }
    & $Python $Installer --rollback $Root
    if ($LASTEXITCODE -ne 0) {
        throw "G17-A rollback失败，退出码=$LASTEXITCODE"
    }
    $Actual = (Get-FileHash -LiteralPath $Target -Algorithm SHA256).Hash.ToLowerInvariant()
    if ($Actual -ne $ExpectedPre) {
        throw "回滚后SpellInfo.cpp哈希不符：实际=$Actual"
    }
    if (Test-Path -LiteralPath $Conf) {
        Move-Item -LiteralPath $Conf -Destination "$Conf.off" -Force
    }
    Write-Host "[OK] G17-A ROLLBACK_SOURCE_AND_CONF_PASS=True"
    Write-Host "[OK] restored SpellInfo.cpp sha256=$Actual"
}
```

必须看到：

```text
[OK] ROLLBACK_CHANGED_FILES=1
[OK] G17-A ROLLBACK_SOURCE_AND_CONF_PASS=True
[OK] restored SpellInfo.cpp sha256=78dbc810f8afa080275d7fabcb2bc75ff52652295a25677b447cbe5f3ca89753
```

回滚后备份 `SpellInfo.cpp.g17a.bak`会保留供审计；不要手工改备份，不要恢复旧01号DBC/SQL方案。

## G11 源码回滚

关闭 worldserver。推荐双击 `g11_step2_windows.cmd`：

```text
选择 3 = ROLLBACK
输入大写 ROLLBACK 确认
```

输出保存为：

```text
g11_step2_rollback_result.txt
```

手工 Python 备用命令：

```powershell
& $Python $Script $Source --rollback
```

它会验证并恢复四个 `.g11_step2.bak` 原文件。

## G11 配置回滚

二选一：

```ini
NpcBot.Autonomy.Perception.Enable = 0
```

或者删除实际运行目录中的：

```text
worldserver.conf.d\g11_perception.conf
```

## 运行二进制回滚

恢复之前备份的：

```text
worldserver.exe.before_g11_step2.bak
worldserver.pdb.before_g11_step2.bak
```

如果回滚到旧 exe，必须确认它和对应 PDB/DLL 是同一批构建产物。

AHBot SQL `12`/`14` 和 G16 源码探针全部只读，不需要数据库或源码回滚。

---

# 第八部分：这次需要完整回传什么

## 当前唯一优先回传

F45 Check/Apply/编译/正常使用烟雾均已由用户确认，不再回传、不再重复。若以后异常，只发当次F45单行播报；重复物品/金币、异常邮件、roll被直接拿走或崩服才立即停服回滚。

B1已关闭；B2已真实部署但三技能体验验收FAIL。B2R1真实`G17B2R1_WINDOWS_BUILD_RESULT.txt`已PASS，禁止重跑安装；当前唯一动作是启动`D:\TC-Build\bin\RelWithDebInfo\worldserver.exe`并按A–E五行回报客户端Runtime。不要重复B1/B1R1–R5、B2旧包或客户端R1–R5。

## 后续保留但不与G17-A检查混跑

```text
G17-A第20C-1已按用户摘要关闭；A4/A6精确原始字段保持未提供，不要求重做
G17-A其余A5/A7/A8/A10/A11/A12以后另行分批，不与F44混跑
G11 T1–T10和10/100/500性能结果
G19主城/野外/副本及900秒冷却结果
SQL 12 / SQL 14完整结果
D:\probe_g16_min_stock.txt
PBot-P0真实源码完整度探针（G17-A后）
```

---

# 第九部分：最终勾选清单

```text
[x] G11 --check / --apply / 四份备份完成
[x] G11 conf六项精确，Enable=1、ReadOnly=1
[x] RelWithDebInfo/x64编译与实际worldserver启动成功
[x] 活动exe、路径、marker、logger全部正确
[x] G11第2步human最小验收：28总/28 human/0缺字段/PASS=True
[ ] G11完整时间戳报告归档及T1–T10/性能完成
[ ] G19游戏内场景与900秒冷却验收完成
[x] G17旧计划完成重新审计，已阻止错误DBC掩码和过宽放行
[x] G17 v1真实报告已回传归档；确认无冲突、B READY及正确mask树命中
[x] G17-P0 schema-2真实报告已归档：42上下文、0缺失、0冲突、A/B READY
[x] G17-A完整源码/conf/安装器/审计patch/SHA256SUMS/ZIP已生成
[x] G17-A本地普通与-O self-test、正反fixture、回滚和G++ mock边界通过
[x] G17-A用户侧普通/-O self-test + D:\TrinityCore只读--check通过
[x] G17-A真实Apply、源码备份、exe/PDB备份成功
[x] G17-A第18R步确认post/backup/conf/BOM/唯一Enable行
[x] G17-A第19A步VS2022 RelWithDebInfo/x64编译通过
[x] G17-A第19B默认关闭启动、主世界A0拒飞、外域控制可飞及后检通过
[x] G17-A第20A步备份Enable=0并精确切换Enable=1
[x] G17-A第20B-S A1/A2/A9启用烟雾与后检通过
[x] G17-A第20C-1用户功能摘要已接受并关闭：主城禁召、外飞入暴风城落地、PVP同样；未提供的A4/A6原始字段不补造
[ ] G17-A第20C其余A5/A7/A8/A10/A11/A12通过
[x] G17-B0 Windows真实源码SelfTest/只读Check通过：READY_TO_APPLY、SOURCE_EDITS=0（禁止重复CMD）
[x] G17-B0 live world只读资源探针v2通过：world/MySQL 8.0.46、PREIMAGE READY（禁止重复）
[x] G17-B0受控源码Apply真实通过：2项改动、POST/payload/备份/7上下文PASS（禁止重复）
[x] G17-B0 world v3安装/独立写后检查PASS并关闭（禁止重复SQL）
[x] G17-B0 Windows v3真实完整构建PASS并关闭；不等于Runtime PASS
[x] G17-B0最小Runtime已运行并确定FAIL：异步可控座位尚未建立即被同步误判清理
[x] G17-R1异步入座、可控座位发现、延迟控制权验收、可见日志与C4018源码修复已生成并自动验证
[x] G17-R1客户端纯飞行坐骑Spell.dbc窄补丁器已用真实zhCN build-12340 DBC验证：110行、10个始祖幼龙ID
[x] G17-R1 Windows真实Apply/CMake/MSBuild/fresh OBJ/新EXE与C4018=0构建PASS（禁止重复）
[x] G17-R1客户端DBC真实staging/verify PASS：110行、10个始祖幼龙、服务器DBC未改
[x] G17-R1客户端packed MPQ、安装状态及有效Spell.dbc由R2A真实报告确认PASS
[x] G17-R1原生御龙载具入座/控制权/动作条/4技能最终Runtime PASS
[x] G17-R2服务端严格区域门根因定位、完整前后镜像、幂等安装器与10项本地测试
[x] G17-R2 Windows构建包装器结果PASS（用户确认结果行；原始全文未附，不补造OBJ/EXE字段）
[x] G17-R2后普通按钮59961湿地提示“无法在这里使用”；随后未triggered的`.cast self 59961`成功召唤上马，完整服务端施法链PASS
[x] G17-R2A活动EXE/source/config、R1状态/自有MPQ/有效Spell.dbc只读分流报告PASS
[x] G17-R3客户端AreaTable 948行窄补丁、服务端实时IsOutdoors安全门、9项测试及Windows组合包已完成
[x] G17-R3 Windows构建/客户端安装PASS；其单槽Runtime失败只作历史，后由R5取代
[x] G17-R5红色始祖幼龙59961普通按钮召唤/起飞/水平移动及真实室内解除Runtime PASS
[x] G17-B1R1旧安装器卡死根因和零写入边界确认；安全原生参数修复已通过离线门槛
[x] G17-B1真实Windows构建：源码后像、MSBuild0、fresh OBJ、新EXE/PDB通过；末尾时间戳假FAIL已机器接受
[x] G17-B1首次Runtime根因：actualVehicle/charmer正确，movementSeat=-1被误当权威seat并清理
[x] G17-B1R3权威Vehicle::Seats验证修复、GCC14、9项新测试、8项回归及Windows包完成
[x] G17-B1R3权威座位修复Runtime：地面/飞行接管、起飞、降落用户确认PASS
[x] G17-B1室内不解除根因：同zone VMap变化不稳定触发OnUpdateZone
[x] G17-B1R4 250ms活动Vehicle安全检查、GCC14、11项测试及Windows包完成
[x] G17-B1R4真实室内自动退出Runtime（用户确认PASS；降落伞被拒绝）
[x] G17-B1R5包装链根因、通用metadata识别、无降落伞清理、GCC14和18项离线测试
[x] G17-B1R5 Windows部署、包装坐骑/无头骑士接管及同一室内无降落伞Runtime用户确认PASS；B1关闭
[x] G17-B2 Windows部署及技能3向前/向上和控制恢复已由真实Runtime证明；整体体验因技能2反馈、高速转向和类型化着陆FAIL
[~] G17-B2R1技能2四阶段反馈、技能3曲线转向/平滑交还、技能4无降落伞类型化多段着陆：真实源码应用/World迁移/MSBuild/fresh产物全部PASS；当前只待新worldserver下的客户端Runtime验收
[ ] G17-B3玩家/移动/四类型攻击独立技能页及真实战斗循环
[ ] G17-B4玩家骑乘移动读条/引导与脱战吃喝
[ ] G17-B5自动寻路/固定不消失/异常恢复
[ ] G17-B6客户端反馈、网络节流与压力加固
[ ] NPCBot场景装备GEAR-P0–P5；当前未实现
[ ] Bot职责自动选择/补缺/备援ROLE-P0–P2；当前未实现
[ ] BOSS人数缩放防爆与目标TTK SCALING-P0–P2；当前未实现
[ ] PBot-P0真实源码完整度审计；当前仅基础，其它完善未完成
[x] G22完美客户端跨领域权威总纲建立，加入当前12.1对标账与最终全世界飞行口径；尚未开始客户端施工
[ ] G22-CP0用户真实客户端只读基线审计
[x] 旧F44真实探针、三原件、Windows Apply/VS2022编译/新exe启动完成并关闭，禁止重复
[x] 旧F44-A道标稳定主坦单项通过；祝福耗材循环和治疗不工作使整体真人验收FAIL
[x] F44R1全职业生命周期/互斥家族/非Aura分类/队列GCD/治疗优先修复、受控生成链和本地全回归完成
[x] F44R1 Windows真实只读Check通过：整组preimage、CHECK_STATE=ready、SOURCE_EDITS=0（禁止重复）
[x] F44R1 Windows真实Apply/postcheck通过：三文件postimage、SOURCE_EDITS=0、旧exe/PDB已备份（禁止重复）
[x] F44R1 VS2022 RelWithDebInfo/x64增量编译与新exe/PDB通过：两个cpp命中，exe SHA=07a8f952...a86c1（禁止重复）
[x] F44R1正确哈希新二进制已运行；用户真人定性烟雾反馈“没什么太大的问题”并授权转下一步
[ ] F44R1-A至D全职业结构化次数/时序矩阵（未回传，不补造；不作为当前转G23的重复测试要求）
[x] G23-P0首次Windows报告回传并分析：False因v1固定路径与真实game/Custom不符；修改0
[x] G23-P0R Windows真实补充报告PASS并归档；CustomAoELoot/Lua/.ext/Loader逐行审计完成
[-] G23 R1–R4结构化矩阵（按用户纠正取消，不再执行）
[x] 独立F45 AoE Loot三文件修复包、本地源码/模型/安装器/CORE+ELUNA语法门槛
[x] F45 Windows Check/Apply/VS2022编译（用户明确确认成功；禁止重复）
[x] F45新二进制正常使用烟雾（播报可见、观察数量一致）
[x] G23-P2独立Lua稳定性包及本地/ZIP门槛
[x] G23-P2 Windows Check原始输出PASS、Apply用户确认PASS；Lua已运行但发现.tp分类菜单假超时
[ ] G23-P2原子奖励SQL末尾summary_table/claim_table结果（未单独回传，不补造）
[x] G23-P2R1 `.tp`无状态菜单热修及本地/ZIP门槛
[x] G23-P2R1 Windows Check、Apply及修后分类菜单（用户确认“可以了”）
[x] G23-P3A `.server`助手、153条GM帮助及本地/ZIP门槛
[ ] G23-P3A Windows Check、Apply、正常启动与简短指令验收
[ ] SQL 12 / SQL 14 / G16真实源码报告回传
[ ] G16每eligible entry至少10条独立挂单及传说过滤完成
```

只有实际勾选完成的项目才能标记完成。G17-A安全一期不能冒充最终“真正全世界可飞”。R1车辆入座/控制权/动作条/4技能Runtime已PASS；R5有效zhCN locale槽已使59961普通按钮召唤、上马、起飞、移动和室内解除全部PASS，纯飞行门正式关闭。通用御龙仍按B1-B6分期：B1R3座位接管已真实PASS，B1R4室内退出已真实PASS；B1R5包装坐骑/无降落伞已真实Runtime PASS并关闭；B2完整移动源码/离线包已完成，待Windows/Runtime，B2整体在真实验收前不得标为最终完成。独立战斗页、骑乘施法、自动寻路和固定模式仍不得提前写完成。旧F44虽已Apply/编译/启动，但真人功能FAIL；F44R1本地回归、Windows Check/Apply、真实增量编译、正确哈希新二进制启动均已PASS，用户真人定性反馈“没什么太大的问题”并授权转G23；未回传的A至D全职业结构化矩阵仍不得补造。PBot状态固定写为“仅基础，其它完善未完成”，直到源码、编译和运行证据逐层通过。
